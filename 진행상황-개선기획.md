# QuantAgent 개선 기획 III — 백테스트 품질을 올려서 수익을 올리는 방법

- **작성일:** 2026-08-07
- **선행 문서:** `진행상황-문제정의.md`(08-01), `진행상황-문제정의-2.md`(08-04), 그리고 그 위에서 돌린 실측 리포트
- **범위:** ① 무엇을 고칠 것인가(기획) ② 그 기획이 실제로 수익을 올리는지 검증한 실험과 결과
- **실험 환경:** 프로덕션 평가기(`_CandidateBacktestSession`)를 그대로 호출. 합성 시장 12개(독립 시드) × 비중첩 1년 테스트 블록 6개 = **72 윈도우**. 실데이터 재현 절차는 §7.
- **핵심 방법론:** 시장 6개는 규칙을 **찾는 데** 쓰고, 나머지 6개는 규칙을 확정한 뒤에야 열었습니다. 이 문서에서 "확인됨"은 두 표본 모두에서 같은 방향이라는 뜻입니다.

---

## 0. 결론 먼저

**백테스트 품질 개선으로는 수익률이 올라가지 않습니다.** 72개 무손상 테스트연도에서, 사전에 확정한 어떤 선택 규칙도 현재 규칙을 재현 가능하게 이기지 못했습니다.

품질 개선이 하는 일은 세 가지고, 그중 수익과 관련된 건 하나뿐입니다:

| | 무엇을 하는가 | 측정된 효과 | 확인 |
|---|---|---|---|
| **가짜 수익 제거** | 생존편향 + 워밍업 부재가 만든 수익을 걷어냄 | 헤드라인 +90.4% → +3.4% | ✅ |
| **다른 전략 추천** | 편향 제거는 상수 오프셋이 아니라 **후보 순위를 뒤집음**(ρ=−0.28) | 선택 후보 4/4 변경 | ✅ |
| **결과 분산 감소** | 기권 게이트가 사용자가 받는 결과의 표준편차를 낮춤 | 9.94% → 5.99% (**−40%**) | ✅ 두 표본 모두 |
| ~~기대수익 증가~~ | ~~선택 규칙 교체~~ | +2.2%p (p=0.053), **확인 표본에서 −0.6%p** | ❌ 탐색 인공물 |

그리고 앞선 리포트의 진단 하나를 정정해야 합니다 — **"오라클 vs 랜덤 격차 = 미개발 알파"는 성립하지 않습니다.** §1이 그 근거입니다.

---

## 1. 정정: 오라클 격차는 실력이 아니라 분산입니다

앞선 리포트의 결론은 **"오라클 vs 랜덤 +16.9%p (t=5.08, 6/6 승) → 후보 간 진짜 성과 차이가 있는데 목적함수가 못 잡고 있다"** 였습니다.

실력이 정확히 0인 후보(가격 데이터는 진짜, 액션 벡터는 난수) 8개로 같은 계산을 하면:

```
평균 후보 수익률          : +2.88%
사후 최선(best-of-8)      : +15.53%
오라클 − 평균             : +12.65%p   t=8.28  p<0.0001
```

**실력이 하나도 없어도 +12.7%p가 나옵니다.** 최대값은 언제나 평균보다 큽니다 — 그게 격차의 전부입니다.

실제 후보로 측정한 72윈도우 풀링 값은 **+10.85%p**로, 널 벤치마크(+12.65%p)보다 **오히려 작습니다.**

```
오라클 − 랜덤 (실후보, 72윈도우) : +10.85%p   t=18.51
같은 계산의 널 기준선            : +12.65%p
```

그러면 진짜 지속 성분은 얼마인가 — 72개 테스트연도 × 후보 8개를 `연도 효과 + 후보 효과`로 분해했습니다:

```
어느 해인가       : 분산의 69.2%   F=16.80  p<1e-16
어느 후보인가     : 분산의  2.0%   F= 4.94  p<0.0001
설명 안 됨        : 분산의 28.8%

가장 좋은 후보의 지속 우위 : 풀 평균 대비 +2.62%p/년
```

후보 효과는 **통계적으로 유의하지만 2%입니다.** 먹을 것은 있지만 **연 2.6%p**이지, 17%p가 아닙니다. §4 E3이 보여주듯 그 크기는 어떤 선택 규칙으로도 검출되지 않습니다.

---

## 2. 기획 — 왜 "품질"이 "수익"이 되는가, 그리고 어디까지만 되는가

사용자가 실제로 받는 수익은 화면의 백테스트 숫자가 아니라 **선택된 전략의 미래 성과**입니다. 백테스트 품질은 두 경로로만 실현 수익에 닿습니다.

- **경로 ① 측정 편향** — 편향된 데이터 위에서 후보를 비교하면 다른 후보가 이깁니다. 실제로 순위가 뒤집힙니다(§4 E4).
- **경로 ② 추정량** — 노이즈가 큰 점수의 argmax는 승자의 저주에 걸립니다. 다만 실험 결과 **이 경로에서 회수 가능한 기대수익은 0에 가깝고**, 대신 분산은 크게 줄일 수 있습니다(§4 E2).

표시(headline·walk-forward 문구)는 수익을 바꾸지 않습니다. 그래도 해야 하지만 "수익 작업"으로 분류하면 안 됩니다.

### 기획 항목

| # | 항목 | 무엇을 바꾸나 | 근거 | 효과 종류 |
|---|------|---------------|------|-----------|
| **P-1** | 유니버스 시점정합화 | `data_sources/db.py:383`을 오늘 시총 top200 → 백테스트 시작 시점 거래대금 top200 | E4 | **결정 변경** (ρ=−0.28, 4/4 교체) |
| **P-2** | 기권 게이트 | 폴드 일관성 미달이면 추천하지 않음(현금) | E2 | **분산 −40%** (두 표본 확인) |
| **P-3** | 헤드라인 홀드아웃 한정 + N 노출 | 전 구간 값 대신 무손상 구간 값, 시도 후보 수와 deflated Sharpe 표기 | E5 | 신뢰 (수익 무관) |
| **P-4** | FE walk-forward 문구 철거 | `app.mock.ts:242` "Walk-forward 6개 윈도우 평균" | 코드 확인 | **즉시** — 하지 않은 검증 |
| **P-5** | 지표 워밍업 | `backtest_features.py:167`에 min_periods | E4 | 수준 −21%p, 결정은 대체로 보존(ρ=0.89) |
| ~~P-x~~ | ~~목적함수 재설계~~ | ~~argmax를 CV/일관성 점수로~~ | E2 확인 실패 | **권장하지 않음** (§3) |

---

## 3. 목적함수 재설계를 권하지 않는 이유 — 제 자신의 탐색이 그 예시입니다

탐색 표본(시장 6개, 36윈도우)에서 규칙 7개를 겨뤄 승자를 찾았습니다:

```
R3b_cv_hitrate   OOS +0.74%   현재 규칙 대비 +4.99%p   t=3.07  p=0.004   6개 시장 중 5개 승
```

p=0.004, Bonferroni 보정(7개 비교) 후에도 0.028. 여기서 멈췄다면 "폴드 일관성으로 바꾸면 연 5%p"라고 썼을 것입니다.

규칙을 고정하고, 한 번도 열지 않은 **새 시장 6개**에서 다시 쟀습니다:

```
                 탐색표본   확인표본    풀링(72)   vs R0 풀링      t       p
R0_current       -4.25%     -0.47%     -2.36%       —           —       —
R1_recent2y      -1.13%     -2.59%     -1.86%     +0.50%p      0.36   0.719
R2_cv_mean       -0.27%     -0.22%     -0.24%     +2.11%p      1.75   0.085
R3b_hitrate      +0.74%     -1.05%     -0.15%     +2.20%p      1.97   0.053
R4_ensemble      -3.23%     -2.09%     -2.66%     -0.30%p     -0.35   0.725
R5_ens_top3      -2.95%     -1.56%     -2.26%     +0.10%p      0.11   0.915
R6_abstain       -0.29%     -0.46%     -0.38%     +1.98%p      1.67   0.099
oracle           +8.29%     +8.09%     +8.19%    +10.55%p     10.54   0.000
```

**+4.99%p가 확인 표본에서 −0.58%p가 됐습니다.** 탐색 인공물이었습니다.

이건 실패가 아니라 이 문서의 논지 자체입니다. **제가 규칙 7개에 대해 argmax를 한 것이, 파이프라인이 후보 8개에 대해 argmax를 하는 것과 정확히 같은 실수입니다.** 확인 표본이 없었다면 저도 같은 방식으로 틀렸을 것입니다. 파이프라인에는 확인 표본이 없습니다 — 그게 P-3이 필요한 이유입니다.

풀링해도 어느 규칙도 유의하지 않습니다(최선 p=0.053). 현재 목적함수를 바꿔서 기대수익을 올리는 경로는 **측정되지 않았습니다.**

### 다만, 신호가 아예 없는 건 아닙니다

```
순위 정보계수 (old70 점수 → 무손상 테스트연도), 72윈도우
  평균 IC +0.127   t=2.80   p=0.007
```

현재 목적함수의 **순위 전체에는** 약한 신호가 있습니다. 그런데 그 argmax는 랜덤을 못 이깁니다. 이유는 명확합니다 — **순위의 꼭대기는 노이즈가 가장 큰 자리**이고, argmax는 신호가 아니라 극단값을 뽑습니다. IC 0.127을 돈으로 바꾸려면 후보 수를 크게 늘리고 상위 분위를 통째로 담아야 하는데, 후보 8~15개 규모에서는 그 방법이 성립하지 않습니다.

---

## 4. 실험 결과

### 실험 설계와 현실성 근거

실데이터로는 "진짜 실력이 있는 후보"를 만들 수 없어 검출력을 측정할 수 없습니다. 그래서 합성 시장을 쓰되, 실데이터의 성질을 넣었습니다: 3-상태 레짐 시장 팩터, GARCH(1,1) 개별 변동성, Student-t 혁신, 중도 상장(25%)·상장폐지(15%, 폐지 전 120봉 bleed).

**현실성 검증:** 이 시장에서 "오늘 살아있는 상위 60종목"과 "시점정합 상위 60종목"의 겹침이 **41.2%**. 실데이터 실측치는 **42%(84/200)**. 생존편향 크기를 결정하는 바로 그 수치가 일치합니다.

각 테스트 블록마다 `old70`(현재 코드가 쓰는 앞 70%), `recent2y`, `fold1..3`(직전 1년 블록 3개), `test`(무손상)를 따로 평가했습니다.

### E1 — 널 실험: 실력 0에서의 best-of-N 격차

§1에 인용. **+12.65%p (t=8.28)**. 이 널 조건에서 각 선택 규칙이 "표시된 후보"를 고르는 비율은 0.08~0.17로 우연 수준(0.125) — 규칙 자체에 사전 편향은 없습니다.

### E0 — 순위는 미래로 살아남는가

```
신호                탐색표본 IC   확인표본 IC   풀링 IC (72)      t       p
old70_score            +0.138        +0.116        +0.127       2.80   0.007
recent2y_score         +0.062        +0.013            —          —      —
cv_mean_sharpe         +0.099        +0.132            —          —      —
테스트연도 t → t+1     +0.105        +0.007            —        0.09   0.926
```

순위 신호는 약하지만 있습니다(p=0.007). **연도 간 지속성은 없습니다**(t→t+1 IC ≈ 0). 즉 "작년에 좋았던 후보가 올해도 좋다"는 성립하지 않습니다.

### E2 — 선택 규칙 토너먼트

§3의 표. 요약: **기대수익 개선은 확인되지 않음.** 확인된 것은 분산뿐:

```
사용자가 받는 결과의 표준편차     탐색표본   확인표본   풀링
R0_current (현재)                  9.16%     10.33%    9.94%
R6_abstain (기권 게이트)           5.87%      6.10%    5.99%   ← −40%, 두 표본 모두
R4_ensemble (전체 균등)           10.01%     10.91%   10.49%
```

기권 게이트는 `직전 1년 블록 3개 전부에서 수익 양수 && 폴드 평균 Sharpe ≥ 0.5`일 때만 추천하고, 아니면 현금입니다. 72년 중 23년(32%)만 매매합니다. **기대수익은 그대로 두고 꼬리를 자릅니다.**

### E2b — 후보 선택이라는 추첨

```
72윈도우 풀링
한 해 안에서 후보 간 수익률 표준편차 : 7.06%p   (최고−최저 평균 20.63%p)
무작위 단일 픽의 표준편차            : 12.62%
균등가중 앙상블의 표준편차           : 10.56%   (−16.3%)
표본 내 최악의 단일 픽 연도          : −34.15%
표본 내 최악의 앙상블 연도           : −22.87%
```

같은 해에 어느 후보를 뽑느냐만으로 평균 21%p가 갈리는데, §1에서 봤듯 그 21%p의 대부분은 예측 불가능한 성분입니다. 앙상블은 **기대수익을 바꾸지 않고** 이 추첨에서 빠지는 방법입니다. (다만 연도 효과가 분산의 69%라서 전체 표준편차 감소는 −17%에 그칩니다. 기권 게이트가 −40%로 더 강합니다.)

### E3 — 검출력: 진짜 우위가 얼마나 커야 찾아내는가

후보 8개 중 정확히 하나에게만 진짜 지속 우위를 심었습니다(매수의 일정 비율이 5일 선행수익 상위 구간에 떨어짐). 우위는 학습 폴드와 테스트 연도 양쪽에서 동일하게 작동 — 운이 아니라 실력입니다.

```
 edge   실력자 수익   풀 평균     R0    R2    R3   R3b   top3에 포함
 0.00       +5.9%     +2.9%    0.17  0.17  0.08  0.17     0.50
 0.05       +5.2%     +2.8%    0.00  0.33  0.08  0.25     0.50
 0.10      +11.7%     +3.6%    0.67  0.67  0.67  0.67     0.92
 0.20      +20.6%     +4.7%    1.00  1.00  1.00  1.00     1.00
 0.40      +40.2%     +7.2%    1.00  1.00  1.00  1.00     1.00
                                             (우연 수준 = 0.125)
```

**최소 검출 가능 우위 ≈ 동료 대비 연 8%p입니다.**

- edge 0.10에서 실력자는 풀 평균보다 +8.1%p 좋고, 그제서야 검출률이 0.67로 올라갑니다.
- edge 0.05(풀 평균 대비 +2.4%p)는 **어떤 규칙으로도 우연과 구별되지 않습니다.**
- §1에서 측정한 실제 후보 간 지속 격차는 **연 +2.62%p**입니다.

**즉 현재 후보 풀이 가진 진짜 차이는, 검출 한계보다 세 배 작습니다.** 이것이 §3의 확인 실패를 설명합니다 — 규칙이 나빠서가 아니라, 찾을 것이 검출 한계 아래에 있습니다.

한편 상위 3개에 **포함**시키는 것은 지목보다 훨씬 쉽습니다(0.50 → 0.92). 후보 풀에 진짜 알파가 생기면 앙상블이 먼저 그걸 잡습니다.

### E4 — 측정 편향은 숫자를 바꾸는가, 결정을 바꾸는가

4개 독립 시장, 10년 전 구간, 유니버스 60종목:

```
셀                          헤드라인   홀드아웃    격차
survivor|warmup=0           +90.40%   +15.47%   74.93%p
survivor|warmup=1           +69.09%    +8.13%   60.96%p
pit|warmup=0                +16.56%    -1.10%   17.66%p
pit|warmup=1                 +3.41%    +0.57%    2.85%p
```

```
survivor|warmup=0 대비    Δ헤드라인   순위 ρ   같은 후보 선택
survivor|warmup=1           -21.31%p   0.887      2/4
pit|warmup=0                -73.84%p  -0.256      0/4
pit|warmup=1                -86.99%p  -0.304      0/4

유니버스 겹침 survivor vs pit : 41.2%   (실데이터 실측 42%)
pit 유니버스가 보존한 폐지 종목: 60개 중 10.2개
```

1. **수준 효과:** 헤드라인 +90.4% → +3.4%. 홀드아웃 기준 +15.5% → +0.6%. 앞선 실데이터 실험의 +20.7% → −1.7%와 같은 이야기입니다. (합성 시장의 폐지 bleed를 세게 넣었으므로 크기는 **상한**으로 읽어야 합니다.)
2. **결정 효과 — 이게 새로운 부분입니다.** 두 유니버스에서 후보 순위 상관이 **ρ = −0.26 ~ −0.30**. 상수 오프셋이 아니라 **순위가 뒤집힙니다.** 선택 후보가 4/4 전부 달라집니다. P-1은 "숫자를 낮추는 작업"이 아니라 **"다른 전략을 추천하게 만드는 작업"** 입니다.
3. **워밍업은 성격이 다릅니다.** 순위 ρ=0.887, 같은 후보 2/4. 수준은 21%p 움직이지만 결정은 대체로 보존합니다 — 앞선 실험의 "승자 유지, 중위권 재배열"과 일치. 그래서 우선순위가 뒤입니다.

### E5 — 다중검정 보정을 게이트로 쓸 수 있는가

Bailey/López de Prado deflated Sharpe를 후보 8개 최적자에게 적용:

```
학습창 old70     (36회) : 승자 DSR 중앙값 0.264 → 0.95 통과 0/36
학습창 recent2y  (36회) : 승자 DSR 중앙값 0.397 → 0.95 통과 0/36
```

**전량 탈락합니다.** DSR≥0.95를 하드 게이트로 넣으면 제품은 아무것도 추천하지 못합니다. 그러니 P-3은 **게이트가 아니라 표시**여야 합니다: "후보 N개 중 선택, deflated Sharpe 0.26 — 무작위 탐색과 통계적으로 구별되지 않습니다." 하드 게이트가 필요하면 P-2(폴드 일관성)를 쓰십시오 — 32% 통과, 분산 −40%로 실제 작동합니다.

참고로 현재 운영 기준은 `MIN_OBJECTIVE_SHARPE = 0.0`(`backtest.py:60`, 주석에 TEMPORARY)입니다. 후보 수도, 분산도, 보정도 들어가지 않습니다.

---

## 5. 착수 순서

| 순위 | 항목 | 왜 이 순서인가 | 완료 판정 |
|------|------|----------------|-----------|
| 1 | **P-4 FE walk-forward 문구 철거** | 하지 않은 검증을 했다고 표시 중. 코드 한 줄, 리스크 0 | `app.mock.ts:242` 및 관련 카피 제거 |
| 2 | **P-1 유니버스 시점정합화** | 결정을 바꾸는 유일한 데이터 수정(ρ=−0.28). 쿼리 한 개, 재적재 불필요. 이걸 안 고치면 아래 전부가 잘못된 입력 위에서 돎 | 같은 하니스에서 survivor/pit 셀의 선택 후보가 갈리는지 실데이터로 재현 |
| 3 | **P-3 헤드라인 홀드아웃 한정 + N·DSR 노출** | 2번이 만든 낮은 숫자를 설명 가능하게 만듦. 그리고 §3에서 제가 겪은 실수를 사용자가 겪지 않게 함 | 리포트에 N과 DSR이 뜨고, 헤드라인이 무손상 구간 값인지 |
| 4 | **P-2 기권 게이트** | 재현된 유일한 개선(분산 −40%). 단, **기대수익은 안 올라간다는 걸 명시**하고 도입 | 발동률 30% 내외인지, 미발동 시 UI가 "추천 없음"을 제대로 말하는지 |
| 5 | **P-5 워밍업** | 국소 수정, 순위 안정성 개선. 결정 영향 작음 | 창 길이 미만 구간에서 지표가 NaN인지 검사하는 회귀 테스트 |

**2~5를 하면 화면 수익률이 크게 떨어집니다** (+90% → +3% 규모). 회귀가 아니라 지금까지의 숫자가 틀렸던 것입니다. 릴리스 전 합의가 필요합니다.

---

## 6. 그래서 수익률은 어디서 올리나

E3이 답을 정해 줍니다:

- 현재 후보 풀의 지속적 성능 차이: **연 +2.62%p**
- 어떤 선택 규칙이든 안정적으로 검출하려면: **연 +8%p**

**선택 규칙 쪽은 닫혀 있습니다.** 열려 있는 건 후보 풀 쪽입니다:

1. **후보 다양성** — 지금 프로파일 8개는 전부 롱온리 추세 계열입니다. 분산 분해에서 후보 효과가 2%밖에 안 나오는 이유가 이것입니다. 서로 상관이 낮은 알파 계열(밸류·퀄리티·리버설·계절성)을 넣으면 후보 효과 자체가 커지고, 그때 비로소 선택이 의미를 갖습니다.
2. **벤치마크 상대 목적함수** — "KOSPI200을 이겼는가"에 답할 경로가 구조적으로 없습니다(A-11). 지수 시계열 확보가 선행 조건이고, 그 뒤엔 절대수익이 아니라 초과수익으로 후보를 비교할 수 있습니다. 연도 효과가 분산의 69%인데, 벤치마크 차감은 그 69%를 직접 걷어냅니다 — **검출력이 올라가는 가장 큰 단일 레버입니다.**
3. **포지션 사이징 실집행** — 리스크 매니저의 상관·집중도가 confidence 스칼라만 깎고 실제 비중에 반영되지 않습니다(A-13).

**순서는 분명합니다: 측정(P-1)과 표시(P-3)를 먼저 고쳐야 새 알파 후보가 진짜인지 판정할 수 있습니다.** 지금 상태로 새 전략군을 추가하면 +12.7%p짜리 노이즈 격차 위에서 고르게 됩니다.

특히 2번(벤치마크 상대)은 이 문서의 실험이 직접 지지합니다 — 연도 효과를 제거하면 후보 효과 2.0%가 상대적으로 커지고, E3의 검출 한계 8%p가 내려갑니다. **다음 실험으로 권합니다: 지수 시계열을 확보한 뒤 같은 72윈도우 하니스에서 초과수익 기준으로 E3의 검출 한계를 다시 재는 것.**

---

## 7. 구현 지점과 재현

### P-1 — `ai/ai_graph/data_sources/db.py:383`

현재 쿼리는 `core.symbol_master`의 **오늘** MKTCAP 상위 200입니다. `listing_status`(3238/3238 전부 `delisted`), `delisted_at`(벌크 버그), `symbol_listing_history`(전부 `first_seen=2026-05-20`) 셋 다 사용 불가이므로 **가격 시계열 자체를 시점 신호로** 씁니다:

```sql
SELECT split_part(ticker, '#', 1) AS symbol
FROM feature.kis_adjusted_ohlcv_daily
WHERE time >= :as_of - INTERVAL '90 days' AND time < :as_of
GROUP BY 1
HAVING count(*) >= 30
ORDER BY avg(adj_close * adj_volume) DESC
LIMIT 200
```

`:as_of` = 백테스트 시작일. MKTCAP은 오늘값 스칼라라 과거 재구성이 불가능하므로 거래대금이 최선의 대체입니다. 상류에서 `listing_status`가 복구돼도 이 쿼리는 **교차검증용으로 남겨야 합니다** — 같은 벌크 버그 재발을 감지할 유일한 수단입니다.

### P-2 — `ai/ai_graph/nodes/backtest.py:1167`

argmax는 그대로 두되, 선택 후 **폴드 일관성 검사**를 통과하지 못하면 추천을 내보내지 않습니다. 직전 1년짜리 폴드 3개에서 각각 평가(후보당 8.5초, 현재 전 구간 1회 27.6초보다 쌉니다 — 120종목·후보 6개 실측)하고, `수익 양수 폴드 3/3 && 평균 Sharpe ≥ 0.5`를 요구합니다. `_objective_score`(`:1863`)는 폴드 단위 점수 함수로 재사용하십시오 — 회전율·MDD 페널티는 그대로 유효하고, 바뀌는 건 "어느 구간에서 몇 번 재는가"입니다.

### 재현

실험 스크립트는 `/private/tmp/claude-501/.../scratchpad/`:

| 파일 | 내용 |
|------|------|
| `market.py` | 레짐 팩터 + GARCH + t분포 + 상장/폐지 합성 시장 |
| `evalkit.py` | 프로덕션 평가기 래퍼(액션 벡터 주입 포함) |
| `exp_selection_rules.py` / `..._confirm.py` | 워크포워드 레코드 (탐색 6시장 / 확인 6시장) |
| `exp_power.py` | 널 + 심어놓은 알파 (E1/E3) |
| `exp_quality_fixes.py` | 유니버스 × 워밍업 교차 (E4) |
| `analyze_pooled.py` | **탐색 vs 확인 vs 풀링 판독 — 이 문서의 핵심 표** |
| `analyze_rules.py` / `analyze_power.py` / `analyze_dsr.py` / `analyze_dispersion.py` | 개별 판독 |

**실데이터 재현이 남아 있습니다.** 이 세션에서는 `AI_DATABASE_DSN`이 환경에 없어 qt_db에 접속하지 못했습니다. 앞선 세션의 `db.py`/`loader.py`(시점정합 유니버스 쿼리 포함)를 그대로 두었으므로 DSN이 있는 셸에서 돌리면 재계산됩니다. 실데이터로 확인해야 하는 것:

1. **E4의 순위 반전** — 실데이터 유니버스 교체에서도 ρ가 음수인가 (겹침 42%는 이미 일치). 가장 중요합니다.
2. **E2의 분산 감소** — 기권 게이트가 실데이터에서도 −40% 수준인가.
3. **E1/E3** — 합성 결과를 그대로 사용(실데이터에는 알파를 심을 수 없음).

---

# 부록 A — 수정 적용 후: 수익률을 올리려면 어디를 건드려야 하는가

- **추가일:** 2026-08-07 (P0/P1 수정 전부 적용된 HEAD `d7c767e`에서 재측정)
- **주의:** 본문 §1~§7의 실험은 세션 중 브랜치가 바뀌기 전, **수정 전 코드**에서 돌았습니다. 아래 F4~F6은 **수정 후 코드**에서 다시 잰 것입니다. F1~F3은 수정 전 레코드를 씁니다(시장 노출 구조는 수정에 영향받지 않는 부분만 인용).
- **표본:** 합성 시장 12개 × 비중첩 1년 = 72 테스트연도. 탐색 6개 / 확인 6개 분리 유지.

## F1 — 이 전략은 무엇에 노출되어 있는가

선택된 후보의 OOS 수익률을 같은 창의 시장(동일가중 buy&hold) 수익률에 회귀:

```
                        beta    alpha/년      R²
선택된 후보 (72창)      0.33     -2.95%     45.2%    p(beta)=1e-10
풀 평균                 0.44     -3.47%     73.9%
```

**절반이 시장이고, 시장이 아닌 부분은 마이너스입니다.**

## F2 — 벤치마크 상대 목적함수는 선택을 바꾸지 않습니다 (본문 §6 정정)

본문 §6에서 "벤치마크 상대 목적함수가 검출력을 올리는 가장 큰 단일 레버"라고 썼습니다. **틀렸습니다.**

```
argmax(수익률) == argmax(수익률 − 벤치마크)  :  72 / 72 창
```

벤치마크는 창당 스칼라 하나라 모든 후보를 똑같이 이동시킵니다. 창 안에서의 순위는 불변이고, 창을 가로질러 평균 내도 상수항은 공통이라 불변입니다. **순위를 바꾸는 건 beta 조정(정보비율)뿐이고, 그건 후보별 일별 수익률이 필요한데 선택 경로는 그걸 보관하지 않습니다.**

벤치마크 상대는 여전히 **게이트와 표시**에는 필요합니다("지수를 이겼는가"). 다만 **선택 품질을 올리는 레버는 아닙니다.**

## F3 — 시장 타이밍 오버레이: 기각

사전 확정 규칙 "직전 126봉 시장 수익률이 양수일 때만 보유":

```
표본       상시보유    오버레이     차이      t       p    매매창
탐색        -4.25%     -2.28%    +1.97%p   2.68   0.011   22/36
확인        -0.47%     -0.97%    -0.50%p  -0.44   0.665   20/36
풀링        -2.36%     -1.62%    +0.73%p   1.06   0.291   42/72
```

확인 표본에서 재현되지 않았고, 전제부터 성립하지 않습니다 — 직전 시장 추세는 이후 시장 수익률을 예측하지 못합니다(r=−0.116, p=0.330). **추세 기반 마켓 타이밍은 이 데이터에서 근거 없음.**

## F4 — buy & hold 대비 손실의 분해 (수정 후 코드)

각 테스트연도를 두 번 돌렸습니다: 프로덕션 비용 모델 그대로 / 수수료·세금·슬리피지 0.

```
                             beta   alpha/년      R²
프로덕션 비용 적용            0.24    -1.21%    48.6%
비용 0                        0.24    +0.52%    47.6%   ← p=0.528, 0과 구별 안 됨

시장 (동일가중 buy & hold)        : +1.25% /년
전략이 실제로 낸 값               : -0.91% /년
buy & hold 대비 격차              : -2.16%p
  ├─ 거래비용                     : -1.72%p   (격차의 80%)
  ├─ 현금으로 놀고 있는 자본      : -0.95%p   (beta 0.24 → 자본의 76%가 미투입)
  └─ 신호 자체 (비용 전 알파)     : +0.52%p   (p=0.528, 사실상 0)

후보당 연평균 매매 47회
```

**세 줄 요약: 비용이 80%, 미투입 자본이 44%, 신호는 0입니다.** 이 셋은 서로 다른 수정이 필요합니다.

## F5 — 더 많이 매매하면 무언가를 사는가 (72창, 탐색/확인 모두 재현)

```
후보    매매/년   beta   비용부담   비용전 알파   순수익
A6           8    0.03    -0.29%      +0.61%     +0.40%
A7          27    0.10    -0.91%      +1.11%     +0.35%
A3          27    0.12    -1.03%      +0.72%     -0.10%
A8          28    0.04    -1.06%      +0.60%     -0.36%
A5          56    0.29    -2.22%      +0.33%     -1.58%
A2          58    0.30    -2.18%      -0.19%     -1.91%
A4          82    0.32    -3.02%      -0.01%     -2.65%
A1          86    0.51    -3.07%      +0.96%     -1.43%

매매횟수 대비 (풀링 72창)
  비용전 알파   : r = -0.12   p = 0.771    ← 매매가 사는 게 없음
  비용부담      : r = +1.00   p < 0.001
  순수익        : r = -0.88   p = 0.004    ← 추가 매매 1회 = 순손실
  beta          : r = +0.95   p < 0.001    ← 노출을 얻는 유일한 방법이 회전
```

```
회전율만 낮춰도 (최저회전 후보 vs 풀 평균)
  탐색 6시장  : +0.40% vs -0.91%   차이 +1.31%p
  확인 6시장  : -0.14% vs -1.29%   차이 +1.15%p
  풀링 72창   : +0.13% vs -1.10%   차이 +1.23%p   t=1.84  p=0.070
```

효과 크기가 두 표본에서 **+1.31 / +1.15%p로 거의 동일**합니다. 방향과 크기 모두 재현됐습니다.

## F6 — 현재 목적함수는 이걸 볼 수 없습니다

```python
# ai/ai_graph/nodes/backtest.py:2040-2041, 2048
annual_turnover = trade_count * 252.0 / trading_days
turnover_penalty = min(1.0, annual_turnover / 24.0)
...  - 0.08 * turnover_penalty
```

```
 매매/년    페널티   페널티 기울기        실제 비용/년
      12   -0.0400   -0.00333             -0.44%
      24   -0.0800   포화 — 기울기 0      -0.88%
      47   -0.0800   포화 — 기울기 0      -1.72%
      86   -0.0800   포화 — 기울기 0      -3.15%
```

**연 24회에서 포화됩니다. 후보 8개 중 7개가 그 위에 있습니다.** 즉 목적함수는 연 24회와 86회를 구별하지 못하는데, 그 구간이 실제 비용이 −0.88%에서 −3.15%로 벌어지는 바로 그 구간입니다.

## 그래서 무엇을 고치나 — 우선순위

| # | 수정 | 근거 | 측정된/추정 효과 |
|---|------|------|------------------|
| **A-1** | 회전율을 실제 비용으로 가격 매김. `min(1, turnover/24)` 포화 페널티를 제거하고, 모델링된 왕복비용(수수료+세금+슬리피지)을 후보 점수에서 직접 차감 | F5, F6 | **+1.23%p/년** (두 표본 재현, p=0.070) |
| **A-2** | 후보 생성기에 최소 보유기간 / 재진입 쿨다운 도입 | F5의 `beta vs 회전율 r=+0.95` | 현재 풀에 **없는 조합**을 만드는 작업 — 아래 참조 |
| **A-3** | 게이트를 절대 Sharpe → 비용 후 초과수익 기준으로 | F1, F2 | 시장이 좋은 해에 통과시키는 것을 막음. 선택은 안 바뀜(F2) |
| **A-4** | 신호 연구 | F4 (비용전 알파 = 0) | 천장은 가장 크지만, E3의 검출 한계 연 8%p. A-1/A-2 이후에만 보임 |

### A-2가 왜 별도 항목인가

`beta vs 회전율 r=+0.95`가 뜻하는 것: **현재 후보 풀에서 자본을 시장에 넣는 유일한 방법이 자주 매매하는 것입니다.** 저회전 후보(A6, 8회/년)는 beta 0.03 — 거의 항상 현금입니다. 고노출 후보(A1, beta 0.51)는 86회/년을 매매합니다.

우리가 원하는 조합 — **적게 매매하면서 많이 투입** — 은 풀에 존재하지 않습니다. 그래서 A-1만 하면 회전율은 내려가지만 노출도 같이 내려가고, 이익의 일부가 현금 드래그로 상쇄됩니다(F5에서 +1.23%p는 그 상쇄를 이미 반영한 순효과입니다).

A-2는 선택 문제가 아니라 **후보 생성 문제**입니다: 진입 신호를 줄이는 게 아니라 **청산을 늦춰야** 합니다. 프로파일 파라미터에 `min_holding_days`를 넣고 탐색 축에 올리는 것이 가장 작은 수정입니다.

### 기대 상한 (추정)

```
현재                                        -1.10% /년
A-1로 회전율을 A6 수준(8회/년)까지          +1.23%p   (측정)
A-2로 같은 회전율에서 beta 0.24 → 0.6       +0.65%p   (추정, 검증 안 됨 — 그런 후보가 없음)
                                            ─────────
                                            약 +0.8% /년  ≈ 시장(+1.8%)에 여전히 못 미침
```

**메커니즘 수정만으로는 시장을 못 이깁니다.** 거기까지가 A-1~A-3이고, 그 위는 A-4(신호)입니다. 그리고 A-4의 성패를 판정하려면 A-1이 먼저 되어 있어야 합니다 — 지금은 비용 −1.7%p가 신호 ±0.5%p를 완전히 덮고 있습니다.

## 실데이터에서 먼저 확인할 것

1. **회전율 분포** — 실제 후보들의 연 매매 횟수. 47회 근처라면 A-1의 +1.23%p가 그대로 적용됩니다. `effective_trade_count`가 이미 요약에 있으므로 재실행 없이 확인 가능합니다.
2. **beta** — `_benchmark_return`(동일가중 buy&hold)에 대한 회귀. 0.24 근처라면 A-2가 필요합니다.
3. **비용 전/후 재실행** — `CostModel(0,0,0)`으로 한 번 더 돌려 F4의 80% 분해를 실데이터에서 재현. 엔진 실행 2배 외에 추가 작업 없습니다.

---

# 부록 B — 적용한 수정 (2026-08-07)

## B-1. 회전율을 실제 비용으로 가격 매김 — `backtest.py:_objective_score`

```
이전 : turnover_penalty = min(1.0, annual_turnover / 24.0)      # 24회에서 포화
이후 : _turnover_cost_penalty() = annual_turnover * round_trip / max_positions
       round_trip = 2*commission + tax + 2*slippage  (엔진 cost_model에서 읽음)
       가중치 3.7 — 관측 회전율(연 47회)에서 이전 페널티 0.08과 같은 크기가 되도록 보정
```

상수를 새로 튜닝한 게 아니라 **엔진이 이미 부과하는 비용 모델을 그대로 씁니다.** 요약에 `cost_model`이 없으면 엔진 기본값으로 폴백합니다.

## B-2. 종목별 매수/매도/보류 — 없던 것을 만듦

문제: `ScreeningMatch`에는 ticker/name/close/matched_rules만 있었고 **액션이 없었습니다.** FE는 이 사실을 알고 `signal`을 의도적으로 비워 두고 있었습니다(`quantAgentClient.ts` 주석: "A screening match is not a per-ticker verdict").

만든 경로 — **성과 숫자를 낸 바로 그 런에서 파생**시킵니다:

| 계층 | 변경 |
|------|------|
| 엔진 | `summary["open_position_tickers"]` 추가 — 보유 종목 수만 있고 목록이 없었음 |
| AI | `_ticker_actions()` — 마지막 봉의 신호 × 최종 포지션 장부 |
| 스키마 | `TickerAction`(ticker/name/action/reason/as_of_date/close/source_candidate_id), `CandidateBacktestResult.ticker_actions`, `UserPayload.ticker_actions` |
| 그래프 | 백테스트가 판단하지 않은 스크리닝 종목을 `WATCH`로 채움 |
| 백엔드 | `fe_contract.py` → `tickerActions` |
| FE | `AITickerAction` 타입, `TradingCandidate.signal` 채움(BUY/HOLD/SELL→DROP, WATCH→미설정), 스크린에 없지만 **보유 중인 종목 행 추가** |

판정 규칙:

```
보유 O + 청산신호 O → SELL
보유 O + 청산신호 X → HOLD
보유 X + 진입신호 O → BUY
보유 X + 진입신호 X → (백테스트는 판단 안 함) → 스크리닝 목록이면 WATCH
```

**보유 여부는 신호가 아니라 포지션 장부에서 봅니다.** 엔진은 현금·슬롯이 없으면 매수를 건너뛰므로 "BUY 신호가 났다"와 "포지션이 있다"는 다른 사실이고, 이걸 섞으면 없는 포지션에 SELL을 냅니다.

## B-2a. A/B 검증 결과 — 페널티는 기각, 상한은 채택

바꾼 목적함수가 실제로 나은지 72개 무손상 테스트연도에서 쟀습니다. 규칙은 실행 전에 확정했고, 탐색 6시장 / 확인 6시장을 분리했습니다.

```
              탐색표본   확인표본   풀링(72)   vs 이전      t       p   회전율/년
이전 페널티     -1.88%    -2.62%    -2.25%       —         —       —      59
새 페널티       -2.30%    -1.63%    -1.96%    +0.29%p    0.77   0.446     51
회전율 상한24   +0.40%    -0.14%    +0.13%    +2.38%p    2.80   0.007     13
무작위 선택     -0.91%    -1.29%    -1.10%    +1.15%p    1.69   0.095     62
오라클          +8.36%    +7.04%    +7.70%    +9.95%p   11.81   0.000     62
```

**페널티만으로는 부족합니다.** 72년 중 픽이 바뀐 건 10년뿐이고 차이는 노이즈입니다(p=0.446). Sharpe 항이 지배하기 때문입니다.

**상한은 다릅니다.** 픽이 65/72년에서 바뀌고, 연 +2.38%p (t=2.80, p=0.007). 탐색 +2.29%p / 확인 +2.48%p로 **효과 크기가 거의 같게 재현**됐습니다. 사용자가 받는 결과의 표준편차도 7.41% → 2.88%로 **61% 감소**합니다.

그래서 페널티는 유지하되(비용 모델을 읽는 것 자체가 옳고, 상한 안쪽에서 미세 조정 역할), **선택 단계에 하드 상한을 넣었습니다** — `_within_turnover_cap`, 연 24회. 24는 이전 페널티가 이미 쓰던 knee라서 이번 실험에 맞춰 새로 고른 값이 아닙니다. 전 후보가 상한을 넘으면 아무도 거르지 않습니다(추천 없음보다 단서를 단 추천이 낫습니다).

⚠️ 상한 후보의 평균도 +0.13%/년으로, 시장(+1.82%)에는 여전히 못 미칩니다. 이건 **현재 규칙 대비 개선**이지 시장 초과가 아닙니다.

## B-3. 검증에서 잡힌 버그 2개

테스트를 먼저 쓴 덕분에 커밋 전에 잡혔습니다.

1. **디스크 캐시가 액션을 삼킴** — 캐시 페이로드에 `ticker_actions`가 없어서, 같은 전략을 재실행하면 성과는 나오고 추천만 조용히 사라졌습니다. 스키마 v2 → v3으로 올려 기존 항목도 무효화.
2. **`_rebind_evaluation`이 액션을 버림** — 동일 코드는 한 번만 평가하고 후보 id마다 재바인딩하는데, 그 경로에서 액션이 누락됐습니다. 재바인딩 시 `source_candidate_id`도 함께 교정.

## B-4. 테스트

```
ai/            379 passed, 6 skipped   (기존 375 + 신규 18: 종목별 액션 14 + 회전율 상한 3 + 통합 1)
backtest_module 18 passed, 10 failed   (10건은 TA-Lib 미설치 — 베이스라인 동일)
fe             28 passed, typecheck 통과, build 통과
backend        fe_contract 9 passed
```

신규 테스트가 고정하는 계약:

- 보유 종목에 BUY 신호가 나도 **BUY가 되지 않음** (엔진이 스킵할 수 있으므로)
- 보유하지 않은 종목의 SELL 신호는 **지시가 아님** (누구에게도 줄 수 없는 명령)
- **마지막 봉만** 판정 — 이틀 전 매수는 오늘의 추천이 아님
- 스크리닝 종목이 백테스트 판정 없이 **사라지지 않음** (사라지면 "매도"로 읽힘)
- 통합: `HOLD ∪ SELL == 엔진의 최종 보유 종목` — 두 경로가 서로 어긋나면 실패
- 회전율 페널티가 24회 위에서도 **계속 증가**하고, 모델링된 왕복비용과 일치

---

# 부록 C — 실데이터 검증 (2026-08-07, qt_db)

시점정합 유니버스 200종목(2016-08-18 기준, 폐지 24종목 포함), 2016-08-18 ~ 2026-08-04, 프로덕션 진입점 `run_candidate_backtest`. 후보는 결정적 빌더(`generate_self_improvement_candidates`) 6개 — LLM 후보 작성기는 토큰을 쓰므로 사용하지 않았고, 그래프가 평가하는 것과 같은 조합입니다(사용자 규칙 컴파일본 2개 + 비교 프로파일 4개).

## C-1. 회전율 — 상한은 4/6을 걸러내지만 픽은 바꾸지 않았습니다

```
후보                    회전율/년   10년 수익률   상한 초과
A0 compiled_conditions      14.5      -40.64%       -
A1 compiled_conditions      14.5      -40.64%       -
A2 long_regime_momentum    295.7      -84.93%      YES
A3 quality_trend_hold       96.5      -47.94%      YES
A4 volatility_breakout      69.0      -72.55%      YES
A5 rolling_sharpe_mom      312.9      -75.92%      YES

상한 24/년이 6개 중 4개를 제거
상한 없이 선택: A0 (14.5/년)   상한 적용 후 선택: A0 (14.5/년)  ← 동일
```

**픽이 안 바뀐 이유가 중요합니다.** 프로덕션은 사용자 규칙이 컴파일되면 `own_rule` 필터로 **compiled_conditions 후보만 선택 대상**으로 삼습니다(`backtest.py`). 그 두 개가 이미 저회전(14.5/년)이라 상한이 걸릴 일이 없습니다.

즉 §B-2a에서 잰 **+2.38%p는 프로파일 6개가 모두 경쟁하는 조건에서 나온 값**이고, 사용자 규칙이 컴파일되는 정상 경로에서는 상한이 발동하지 않습니다. 상한은 **규칙이 컴파일되지 않아 일반 템플릿이 대신 뛰는 경로의 안전장치**로 유효하며, 거기서는 A2·A5 같은 연 300회짜리를 확실히 배제합니다.

## C-2. 비용 vs 신호 — 합성 결과가 실데이터에서 뒤집혔습니다

```
기간 2016-08-18 .. 2026-08-04 (9.7년)
동일가중 buy & hold (같은 유니버스) : +74.60%   (+5.92%/년)
후보 평균, 프로덕션 비용            : -60.44%   (-9.13%/년)
후보 평균, 비용 0                   : -48.40%   (-6.60%/년)
비용 부담 (전 기간)                 :  12.03%p

선택된 후보 A0 단독
  비용 부담   :  1.54%p / 10년   (연 0.15%p)
  노출(exposure) : 0.64
  평균 보유일   : 72일
  매매 94회 / 9.7년 = 연 9.7회
  승률          : 27.7%
  최종자산      : 593,589원 / 1,000,000원   MDD -40.7%
```

**부록 A에서 "비용이 격차의 80%"라고 썼는데, 실데이터에서는 아닙니다.** 그건 합성 시장의 프로파일들이 연 47회를 매매했기 때문이고, 실제로 선택되는 후보는 연 9.7회입니다.

실데이터 분해(선택 후보 기준, buy&hold 대비 −115%p):

```
거래비용            :  -1.5%p    (1%)
현금으로 노는 자본  :  약 -27%p   (노출 0.64 → 미투입 36%)
신호 자체           :  약 -87%p   (76%)
```

**규칙 자체가 돈을 잃습니다.** 승률 27.7%, 평균 72일 보유. RSI≤30 진입 / RSI≥70 청산인데, 청산 조건이 거의 발동하지 않아 실질적으로 −8% 손절이 대부분의 청산을 담당합니다. 지는 쪽만 잘라내고 이기는 쪽은 오지 않는 구조입니다.

## C-3. 종목별 추천 — 실데이터에서 동작 확인

```
기준일 2026-08-04, 선택 후보 A0
  001470  HOLD    347원    청산 조건 미충족 - 보유 유지
  005110  HOLD  1,254원    청산 조건 미충족 - 보유 유지
엔진 최종 보유 포지션 2개 — 액션 2건과 일치
```

유니버스 200종목 중 마지막 봉에 판정이 나오는 건 2종목입니다. 나머지는 전략이 들어가 있지도, 들어가려 하지도 않는 종목이라 백테스트가 할 말이 없고, 스크리닝 목록에 있으면 그래프가 `WATCH`로 채웁니다.

## C-4. 그래서 결론

측정은 이제 정직합니다. `selection_adjusted_sharpe −0.344`가 이 전략을 정확히 거절하고 있고, 화면 숫자도 홀드아웃 기준입니다. **백테스트 품질 쪽에서 회수할 수 있는 것은 사실상 다 회수했습니다.**

남은 것은 백테스트 배관이 아니라 **전략입니다.** 실데이터가 가리키는 곳:

1. **청산 규칙이 작동하지 않음** — RSI≥70이 거의 안 걸려 손절이 청산을 대신합니다. 시간 기반 청산이나 트레일링을 조건에 넣어야 승률 27.7%가 움직입니다.
2. **노출 0.64** — 나머지 36%가 현금인데, 같은 기간 유니버스는 +74.6%였습니다. 슬롯이 비는 이유(진입 신호 부족 vs 자본 부족)를 `execution_audit`의 skipped 카운트로 갈라야 합니다.
3. **진입 신호의 방향** — 이 10년 KOSPI에서 RSI≤30 매수는 하락 추세 진입이었습니다. 부록 A의 E3이 말하듯 후보 간 차이를 검출하려면 연 8%p가 필요한데, 지금 필요한 건 후보 선택이 아니라 **다른 알파 계열**입니다.

---

# 부록 D — C-4의 세 항목을 실측으로 풀어봄 (2026-08-07)

선택 후보 A0(사용자 규칙 컴파일본), 시점정합 200종목, 2016-08-18~2026-08-04, 완료 거래 94건.

## D-1. "청산 규칙이 작동하지 않는다" — 맞지만, 고쳐도 소용없습니다

```
청산 사유        거래   비중    평균수익률   승률    평균보유
stop_loss          64  68.1%     -16.77%    0.0%      60일
exit signal        30  31.9%     +19.79%   86.7%      98일
```

전제는 사실입니다 — **청산의 68%를 손절이 담당하고, 사용자가 쓴 RSI≥70 청산은 32%만 발동합니다.** 그리고 청산 신호로 끝난 거래는 86.7%가 이깁니다.

그래서 "손절이 이길 거래를 자르고 있다"고 추론했고, 손절 폭만 바꿔 검증했습니다:

```
  손절   10년 수익률      MDD   거래  승률   손절청산  신호청산  평균보유
    8%      -40.64%   -40.75%    94  27.7%       64        30      72일
   15%      -45.91%   -46.01%    94  37.2%       55        39      96일
   25%      -48.59%   -54.32%    94  46.8%       40        54     125일
   없음     -37.05%   -45.53%    95  53.7%        1        94     179일
```

**추론이 틀렸습니다.** 손절을 넓히면 승률은 27.7% → 53.7%로 크게 오르는데 **수익률은 −37~−49% 사이에서 움직이지 않습니다.** 손절을 아예 없애도 10년 +3.5%p 개선에 그치고 MDD는 −40.8% → −45.5%로 나빠집니다.

86.7%라는 승률은 청산 규칙이 좋아서가 아니라 **정의상 그렇습니다** — RSI 70까지 회복한 거래만 그 그룹에 들어가니까요. 생존자를 세고 있었습니다.

**결론: 청산은 레버가 아닙니다. 현재 8% 손절을 유지하는 게 MDD 기준으로 가장 낫습니다.**

## D-2. 노출 0.64 — 자본이 아니라 신호가 부족합니다

```
9.7년간 생성된 BUY 신호      : 121건  (연 12.5건, 200종목 대상)
실제 체결된 매수             : 96건
현금 부족으로 스킵된 매수    : 25건   (신호의 20.7%)

평균 보유 포지션             : 1.86 / 10
중앙값                       : 2 / 10
슬롯이 가득 찬 날            : 0일 (0.0%)
포지션이 하나도 없는 날      : 380일 (15.6%)
```

**슬롯이 가득 찬 날이 하루도 없습니다.** 자본이 노는 주된 이유는 살 것이 없어서입니다. 신호 연 12.5건 × 평균보유 72일 ÷ 252 ≈ 정상상태 3.6 포지션인데, 실측 1.86입니다.

다만 그 차이 안에 **별개의 결함**이 있습니다: 슬롯 8개가 비어 있는데도 매수 25건이 `skipped_insufficient_cash`로 막혔습니다. 신호가 연 12.5건뿐인 전략에서 그중 20.7%를 현금 문제로 흘리는 건 큽니다. `_execute_pending_orders`의 사이징이 `equity/max_positions`를 요구하는데 가용 현금이 그에 못 미치는 상황(같은 날 신호가 몰릴 때, 드로다운 이후)으로 보이며, 확인이 필요합니다.

## D-3. 남은 것은 진입 신호뿐입니다

```
승리   26건 (27.7%)   평균 +22.91%   중앙값 +17.79%   평균보유 90일
패배   68건 (72.3%)   평균 -15.81%   중앙값  -9.87%   평균보유 65일

거래당 기대값        : -5.10%
손익비 (평균승/평균패): 1.45
손익분기 승률        : 40.8%   ← 실제 27.7%
```

손익비 1.45는 나쁘지 않습니다. 문제는 승률이 손익분기점보다 **13.1%p 낮다**는 것이고, D-1이 보여주듯 청산을 어떻게 바꿔도 이 갭은 닫히지 않습니다. 남는 건 진입입니다.

이 10년 KOSPI에서 `RSI ≤ 30 매수`는 하락 추세에 올라타는 진입이었습니다. 같은 유니버스 동일가중 buy&hold가 **+74.6%** 인 동안 이 규칙은 −40.6%였습니다.

필요한 개선폭은 둘 중 하나입니다:

- 승률 27.7% → **40.8%** (+13.1%p), 손익비 유지
- 또는 손익비 1.45 → **2.61**, 승률 유지

부록 A의 E3이 말한 검출 한계(연 8%p)는 **후보 중에서 고르는** 문제였습니다. 지금 필요한 건 고르는 게 아니라 **다른 진입 계열**입니다 — 추세 추종, 상대강도, 밸류/퀄리티처럼 RSI 역추세와 상관이 낮은 것. 그리고 그걸 판정할 측정 장치는 이제 정직하게 작동합니다.
