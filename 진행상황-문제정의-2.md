# QuantAgent 문제 정의 II — 퀀트 수익 품질 / 속도·파이프라인 안정성

- **작성일:** 2026-08-04
- **범위:** 기존 `진행상황-문제정의.md`(2026-08-01)가 다루지 않은 두 축.
  ① 백테스트가 만들어 내는 **숫자가 현실을 대표하는가**(수익 품질),
  ② **얼마나 걸리고 얼마나 안 죽는가**(속도·파이프라인 안정성).
- **검증 근거:** 로컬 브랜치 `fix/resolve-vague-strategy-requests` 소스 정독. 아래 항목은 전부 파일·라인 근거가 있습니다. 라이브 부하 측정은 미실시(측정 항목은 §C에 별도 정리).
- **읽는 법:** 각 항목은 `무엇이 / 왜 문제인가 / 근거 / 결과로 무슨 숫자가 틀어지는가` 순서. 해결책은 적지 않았습니다(문제 정의 문서).

---

## A. 퀀트 수익 품질

> 전제: 지금 사용자가 보는 "검증 누적 수익률"(`fe/src/features/app/OverviewTab.tsx:67`)은 **전략의 기대 성과가 아니라, 몇 가지 구조적 편향이 누적된 상한값**에 가깝습니다. 아래 A-1~A-3이 그 주된 이유입니다.

### A-P0 — 숫자의 방향 자체를 바꾸는 문제

#### A-1. 백테스트 유니버스가 "오늘 기준" 종목 집합 (생존편향 + 시총 룩어헤드)

- **무엇이:** 10년 백테스트가 도는 유니버스는 `core.symbol_master`에서 **현재** `market='KOSPI'`인 보통주를 **현재 시총(MKTCAP)** 내림차순 200개로 잡습니다.
- **왜 문제인가:** 10년 전에 대형주였다가 몰락·상장폐지된 종목은 유니버스에 아예 없고, 10년간 10배 성장해 지금 top 200에 든 종목은 **1일차부터** 유니버스에 있습니다. 롱온리 전략에서 이건 단방향 상승 편향입니다. 게다가 `listing_status`가 상류에서 전부 `delisted`로 깨져 있어(주석에 명시) 상장폐지 종목을 구분할 방법 자체가 현재 없습니다.
- **근거:** `ai/ai_graph/data_sources/db.py:383-398`, `ai/ai_graph/data_sources/db.py:79-82`
- **틀어지는 숫자:** total_return, CAGR, MDD, Sharpe — 전부. 편향 크기는 KOSPI 기준 통상 연 2~5%p 수준으로 알려져 있지만, 여기서는 **측정된 적이 없습니다.**
- **참고:** 팀은 "스크리닝 결과를 유니버스로 쓰면 룩어헤드"라는 더 큰 문제는 이미 인지·수정했습니다(같은 함수 docstring). 남은 건 이 잔여 편향이고, 지금은 문서화조차 안 돼 있습니다. `_benchmark_return`만 경고 문구를 답니다(`ai/ai_graph/nodes/backtest.py:1947-1950`).

#### A-2. 지표 워밍업 구간이 없음 — 백테스트 초반 신호가 전부 무의미

- **무엇이:** 롤링 지표가 창(window)보다 짧은 구간에서도 **NaN이 아니라 값을 냅니다.** 20일 이동평균이 3일차에는 "3일 평균"으로 계산되고, 조건은 그 값에 정상적으로 매칭됩니다.
  - 고속 경로: `start = max(0, local_index - window)` 후 `count`가 1이어도 평균을 채웁니다.
  - 생성 코드 경로: 진입 판정 가드가 `ready = len(closes) >= 1` 한 줄뿐입니다(주석은 "가장 긴 창만큼의 히스토리"라고 적혀 있지만 코드는 1).
- **왜 문제인가:** `breakout_high`(=252일 최고가 돌파)가 2일차에 "2일 중 최고"로 판정돼 **거의 모든 종목이 첫 며칠에 52주 신고가 돌파로 잡힙니다.** max_positions 슬롯이 백테스트 초반의 의미 없는 신호로 전부 채워지고, 그 포지션들이 이후 equity curve 전체를 지배합니다.
- **근거:** `ai/ai_graph/nodes/backtest_features.py:774-783`, `ai/ai_graph/nodes/backtest_code.py:782`, 창 정의는 `ai/ai_graph/nodes/condition_compiler.py:156-169`
- **틀어지는 숫자:** 초기 진입 시점·종목 구성 → 전체 수익률. 이건 편향이 아니라 **버그에 가깝고, 재현 테스트로 즉시 확인 가능**합니다(창 길이 미만 구간에서 지표가 NaN인지 검사).

#### A-3. 사용자에게 보이는 헤드라인 수치가 in-sample을 포함한 전체 구간

- **무엇이:** 후보 선택은 앞 70%(in-sample)만 보고 하도록 설계돼 있고(`BACKTEST_SPLIT_FRACTION = 0.7`, 목적함수도 `in_sample_*`만 사용), 이 설계 의도 자체는 옳습니다. 그런데 **리포트에 나가는 `sharpe_ratio` / `total_return` / `max_drawdown`은 engine summary의 전체 구간 값**입니다.
- **왜 문제인가:** N개 후보 중 in-sample 최고점을 고른 뒤, 그 in-sample을 포함한 전 구간 성과를 "검증 결과"로 제시합니다. 다중검정(multiple testing) 보정도, 시도한 후보 수 공개도 없습니다. 실제로 신뢰할 수 있는 숫자는 `out_sample_sharpe` 하나인데, 이건 UI 헤드라인이 아닙니다.
- **근거:** `ai/ai_graph/nodes/backtest.py:1146-1152`(argmax 선택), `:1632-1652`(metrics 조립), `backtest_module/backtest_module/backtest.py:1317-1321`(summary), `fe/src/api/quantAgentClient.ts:828`("거래비용 반영 후 검증 기간 누적 수익률")
- **덧붙여:** 엔진 스펙에는 `walk_forward`(in_sample_months=12) 설정이 존재하지만 **어디서도 사용되지 않습니다**(`backtest_module/backtest_module/models.py:155`, 실제 사용은 `strategy.py:67`에서 스냅샷에 복사만). 검증은 단일 70/30 split이고 롤링 재적합은 없습니다. FE mock은 "Walk-forward 6개 윈도우 평균"이라고 적어 둔 상태입니다(`fe/src/mocks/app.mock.ts:242`) — 구현과 표현이 다릅니다.

---

### A-P1 — 전략이 사용자 의도와 다르게 실행되는 문제

#### A-4. 평가기가 둘인데 의미론이 다르고, 같은 리그에서 경쟁함

- 구조화 경로(`_compiled_actions`)는 **exit_conditions를 실제로 평가**합니다(`ai/ai_graph/nodes/backtest_features.py:494-516`).
- 생성 파이썬 경로(`_render_condition_signal_code`)는 **exit_conditions를 아예 쓰지 않고** 고정 손절 -8% / 익절 +20%로 대체합니다(`ai/ai_graph/nodes/backtest_code.py:715-716, 789-795`).
- 두 경로의 후보가 **하나의 objective_score로 순위 경쟁**합니다(`ai/ai_graph/nodes/backtest.py:1146`). 청산 규칙이 더 느슨한 쪽이 구조적으로 유리하면, 이긴 이유가 "규칙이 좋아서"가 아니라 "평가기가 달라서"가 됩니다.
- **더 큰 문제:** 사용자 조건이 컴파일되지 않으면 **일반 템플릿 전략이 대신 백테스트되고 추천됩니다.** 현재 작업 중인 `rule_provenance`(working diff)가 이 사실을 *표시*하려는 것이지, 동작을 바꾸는 건 아닙니다. 즉 "사용자 전략이 목적함수에서 져서 일반 템플릿이 추천되는" 경로가 정상 동작으로 남아 있습니다.

#### A-5. 조건 결합이 항상 AND

- `" and ".join(parts)`(`ai/ai_graph/nodes/condition_compiler.py:327`), `all(...)`(`backtest_features.py:505, 517`).
- "RSI 70 이상 **또는** 20일선 이탈 시 매도" 같은 OR 청산은 표현 불가능하고, 조용히 AND로 바뀝니다. AND 청산은 **거의 발동하지 않으므로** 실질적으로 손절/익절만 남습니다. 엔진 스키마에는 `exit_logic = ANY` 기본값이 있는데(`models.py:184`) AI 경로는 이 필드를 쓰지 않습니다.

#### A-6. 신호측 진입가와 실제 체결가가 다름 → 손절선이 보유 손익과 어긋남

- 생성 코드는 `state["entry_price"] = close`(신호 당일 종가)로 기록하고 손익을 계산합니다(`backtest_code.py:826-827`).
- 엔진은 **다음날 시가**에 슬리피지를 얹어 체결합니다(`backtest_module/backtest_module/backtest.py:967, 1004`).
- 결과: 코드가 계산하는 -8%는 포트폴리오가 실제로 낸 -8%가 아닙니다. 갭 상·하락이 큰 종목일수록 벌어집니다.
- **더 심각한 부작용 — 상태 desync:** 코드는 `states[ticker]["in_position"] = True`로 자기 장부를 잠그는데, 엔진은 현금 부족·포지션 한도로 그 매수를 스킵할 수 있습니다(`backtest.py:940-996`, `status="skipped_*"`). 그러면 코드는 "보유 중"이라 재진입 신호를 영영 안 내고, 나중에 매도 신호는 내서 엔진이 `ignored_missing_position`으로 버립니다. 거래 수가 조용히 줄고 신호 수와 체결 수가 벌어집니다. `_execution_audit`이 이 카운트를 잡고는 있지만(`ai/ai_graph/nodes/backtest.py:1604-1609`) 사용자에게 "이 전략의 신호 중 N%는 실행되지 못했다"로 노출되지는 않습니다.

#### A-7. 손절이 이중으로 걸림

- 엔진 `RiskControls.stop_loss_pct` 기본값 0.08 (`models.py:127`) → `_risk_exit_reason`이 매일 종가로 검사(`backtest.py:1252-1259`).
- 생성 코드도 자체 손절 0.08 (`backtest_code.py:719`).
- 서로 다른 진입가 기준으로 도는 두 개의 손절이 있고, 먼저 발동하는 쪽이 이깁니다. 사용자가 손절을 설정하지 않았을 때도 **-8% 손절이 강제 적용**되는데, 이건 리포트 어디에도 명시되지 않습니다.

#### A-8. 자본이 부족할 때 종목코드 오름차순으로 배분

- 엔진: `buy_orders.sort(key=lambda order: order.ticker)` (`backtest.py:938`)
- 생성 코드: entries가 `(date, ticker)` 정렬 순서대로 슬롯을 채움 (`backtest_code.py:751, 822`)
- 신호 강도·점수가 아니라 **종목코드 숫자가 작은 순**으로 매수됩니다. 결정론적이긴 하지만 재현 가능한 편향이고, 카피 트레이딩 관점에서 설명이 불가능합니다. (`_adaptive` 템플릿만 score 정렬을 씁니다 — `backtest_code.py:1058-1059`. 여기서도 경로 간 의미론이 갈립니다.)

#### A-9. 체결 현실성: 유동성 제약 없음, 슬리피지 고정

- 매수 수량은 예산 나누기 단가일 뿐, **그날 거래량 대비 상한이 없습니다**(`backtest.py:982`). 1억을 소형주 시가에 통째로 밀어 넣어도 슬리피지는 0.1% 고정입니다.
- 시장 충격(market impact) 모델 없음. ADV 대비 주문 비율 제한 없음.
- 거래세 기본값 0.23%(`models.py:146`)는 과거 요율 기준입니다. 수수료·세금 자체는 반영되고 있다는 점은 확인했습니다(0으로 도는 게 아님).
- **틀어지는 숫자:** 회전율이 높은 전략일수록 수익률 과대. 현재 objective는 turnover penalty만 약하게(`-0.08 * min(1, 연회전/24)`) 겁니다(`ai/ai_graph/nodes/backtest.py:1856-1864`).

#### A-10. 거래정지·상장폐지 좀비 포지션

- 그날 바가 없으면 포지션은 `position.last_price`로 계속 평가됩니다(`backtest.py:1285-1290`).
- 매도 주문은 `order.ticker in today_bars`일 때만 체결되므로(`backtest.py:879`), 데이터가 끊긴 종목은 **마지막 가격 그대로 끝까지 보유**됩니다. 상장폐지 손실이 0으로 기록됩니다.
- A-1의 유니버스 편향과 겹치면 이중으로 손실이 사라집니다.

#### A-11. 벤치마크가 KOSPI200이 아니라서 알파를 측정할 수 없음

- `calculate_quantstats_metrics`는 `benchmark_returns=None`으로만 호출됩니다(`backtest_module/backtest_module/backtest.py:1296`). 따라서 `information_ratio`, `r_squared`, `greeks`(알파·베타), `compare`가 **전부 None → 기본값으로 대체**됩니다(`performance.py:208-213, 361-379`).
- AI 쪽 `benchmark_return`은 "같은 (편향된) 유니버스 동일가중 buy&hold"입니다(`ai/ai_graph/nodes/backtest.py:1913-1931`). 코드 스스로 "이건 KOSPI200 지수가 아니다"라고 경고를 답니다.
- **결론:** "KOSPI200 퀀트 에이전트"인데 **"지수를 이겼는가"에 답할 수 있는 경로가 구조적으로 없습니다.** 창고에 지수 시계열이 없다는 것도 확인됨(`ai/ai_graph/nodes/risk_manager.py:31-37` — VKOSPI도 없음).

#### A-12. 지표 계산 실패가 그럴듯한 값으로 조용히 대체됨

- `_apply_metric_defaults`가 실패한 지표를 **0.0 / [] / {}로 채우고** warning은 `metric_warnings` 배열 안쪽에 넣습니다(`performance.py:361-379`).
- `max_drawdown` 계산이 실패하면 화면에 **MDD 0%**가 뜹니다. 이건 "안전한 기본값"이 아니라 가장 위험한 방향의 기본값입니다.
- 몬테카를로는 과거 일별 수익률의 **IID 부트스트랩**입니다(`performance.py:493-556`). 변동성 군집·자기상관을 전부 파괴하므로 꼬리 위험을 체계적으로 과소평가하고, seed 고정(42)이라 매번 같은 수치가 나와 정밀해 보입니다. `loss_probability`, `risk_of_ruin` 같은 이름으로 노출됩니다.

#### A-13. 선언만 되고 집행되지 않는 리스크 컨트롤

- `max_sector_weight_pct = 0.4` — **엔진 어디에서도 참조되지 않습니다.** 섹터 쏠림 제한이 없습니다.
- `exclude_listing_risk = True` — 미사용.
- `use_adjusted_price`, `walk_forward` — 스냅샷에만 복사되고 미사용.
- 근거: `backtest_module/backtest_module/models.py:126-131, 154-155` vs 전 코드베이스 grep 결과(사용처는 `max_gross_exposure_pct` 하나뿐).
- 한편 리스크 매니저가 계산하는 상관·집중도는 **포지션 사이징이 아니라 confidence 스칼라 하나만** 깎습니다(`ai/ai_graph/nodes/risk_manager.py:102-147`). 백테스트 포트폴리오는 반도체 10종목을 그대로 삽니다.

#### A-14. 목적함수 하한이 "임시"인 채로 운영 중

```
# TEMPORARY TEST-ONLY: 샤프와 MDD 커트라인을 결과 노출 검증용으로 느슨하게 잡았다.
MIN_OBJECTIVE_SHARPE = 0.0
MAX_OBJECTIVE_DRAWDOWN = -0.50
```
- `ai/ai_graph/nodes/backtest.py:58-61`
- 이 값이 `strategy_validated`를 결정하고(`:1330`), 다운스트림 추천 게이트가 이걸 읽습니다. 즉 **"Sharpe 0 이상, 반토막 안 남"이 현재의 검증 통과 기준**입니다. 동시에 자가개선 라운드 진입 조건이기도 해서, 대부분의 런이 개선 없이 통과합니다.

#### A-15. (경미) `_profit_factor`가 승률에서 합성된 가짜 지표

- `win_rate / (1 - win_rate)`를 3.0에서 clip한 값을 profit factor라고 부릅니다(`ai/ai_graph/nodes/backtest.py:1900-1910`). 손익비와는 아무 관계가 없습니다.
- **현재 호출되지 않는 데드코드**라 지금 사용자에게 나가지는 않습니다. 삭제 대상. 남겨 두면 언젠가 누가 씁니다.

---

## B. 속도 · 파이프라인 안정성

> 기존 문서의 ①(동기 실행)은 이미 해결됐습니다 — 지금은 `BackgroundTasks`로 큐잉됩니다(`ai/ai_graph/api.py:436-450`). 아래는 그 이후에 남은 문제들입니다.

### B-P0 — 죽는 이유

#### B-1. 모든 상태가 단일 프로세스 안에 있어 수평 확장이 원천 봉쇄됨

- **job store 기본값이 in-memory** (`AI_JOB_STORE` 미설정 시 `memory`, `ai/ai_graph/jobs.py:428-429, 483`)
- **SSE 이벤트 버퍼도 프로세스 로컬** (`app.state.job_events`)
- **AOAI 동시성 게이트도 프로세스 로컬** — 코드가 직접 명시: *"deployment runs a single uvicorn process... Were the service ever to run multiple workers or hosts, this would need to move behind Redis"* (`ai/ai_graph/llm/concurrency_gate.py:241-246`)
- **백테스트 ProcessPoolExecutor도 요청 안에서 생성** (`ai/ai_graph/nodes/backtest.py:680-690`)
- **결과:** uvicorn worker를 2개로 올리는 순간 → worker A에 만든 job을 worker B가 404로 응답. SSE 끊김. AOAI 게이트는 각자 카운트해서 실제 동시성이 2배. **"부하가 오면 늘린다"는 선택지가 존재하지 않습니다.** 이게 기존 문서 ③("OCI가 몰리면 죽음")의 구조적 원인입니다.
- **부수 문제:** 프로세스가 재시작하면 in-flight job은 통째로 유실되고, `RUNNING` 상태로 영원히 남습니다. lease/heartbeat/reaper 없음(`jobs.py` 전체에 stale job 회수 코드 없음).

#### B-2. 긴 분석이 헬스체크를 포함한 모든 동기 엔드포인트를 굶김

- `create_analysis_job`은 `def`(동기) 핸들러이고, `BackgroundTasks`는 **anyio 스레드풀(기본 40)** 위에서 돕니다. `/health`, `GET /analysis-jobs/{id}`, 인증 등 다른 동기 핸들러도 **같은 풀**을 씁니다.
- 10분짜리 분석 40개가 스레드를 점유하면 **헬스체크가 응답을 못 합니다.** 그러면 오케스트레이터/모니터링 눈에는 "서버가 죽었다"로 보이고, 실제로는 계산 중입니다.
- 큐도 없고 분석 전용 동시성 상한도 없습니다. 40은 "분석 동시 실행 한도"로 설계된 숫자가 아니라 무관한 기본값입니다.
- **덧붙여:** 기존 문서 ③이 지적한 "배포가 `/health`와 FE `/`만 확인한다"는 문제와 정확히 맞물립니다. 이 조합에서는 `/health`가 green이어도, red여도 실제 상태를 뜻하지 않습니다.
- 근거: `ai/ai_graph/api.py:421-451`

#### B-3. 시간 예산이 "권고"일 뿐 강제되지 않음 — 618초의 실제 출처

- `DEFAULT_WALL_BUDGET_SECONDS = 540`, `DEFAULT_CANDIDATE_TIMEOUT_SECONDS = 180` (`ai/ai_graph/nodes/backtest.py:71-73`)
- wall budget은 **자가개선 라운드 시작 전에만** 검사됩니다(`:1254`). 이미 시작된 라운드는 끝까지 갑니다.
- 라운드 내부 데드라인은 `timeout * wave_count`, `wave_count = ceil(후보수 / 워커수)`입니다(`:697-698`). 후보 12개 / 워커 2개면 **180초 × 6 = 1080초**가 정상 예산입니다.
- 즉 **백테스트 노드 하나만으로 18분이 설계상 허용**됩니다. 618초는 이상 동작이 아니라 사양대로 동작한 결과입니다.
- 상류에도 강제 예산이 없습니다: DB statement timeout 30초(백테스트 스캔은 120초, `data_sources/db.py:55-60`) × 스크리닝 완화 최대 3라운드(각 라운드가 LLM 호출 + 전체 유니버스 재스캔, `db.py:70`). **요청 전체에 걸린 단일 deadline이 없어서, 각 단계가 자기 예산을 다 쓰면 합이 얼마가 되든 그대로 갑니다.**

#### B-4. 요청 단위 프로세스 풀 — 전역 동시성을 모름

- `_candidate_worker_count`는 `os.cpu_count()`와 후보 수만 보고 워커 수를 정합니다(`ai/ai_graph/nodes/backtest.py:783-800`). **동시에 도는 다른 분석의 존재를 모릅니다.**
- 동시 요청 5건 × 워커 2개 = 프로세스 10개가 각자 prepared market data 사본을 들고 돕니다. fork라도 CoW는 파이썬 refcount 때문에 금방 무너집니다.
- spawn 환경에서는 아예 병렬을 껐습니다("측정된 Windows 프로덕션 입력에서 RSS 3배" — 같은 함수 주석). 즉 **메모리가 이미 병목이라는 걸 코드가 알고 있고, 회피만 해 둔 상태**입니다.

---

### B-P1 — 느린 이유

#### B-5. 50만 행을 파이썬 dict로 만들어 세 번 복제

- 200종목 × 약 2,520거래일 ≈ **50만 행**, 각 행에 지표 30여 개.
- 경로: SQL → `_price_row_from_feature_frame_record`로 행마다 dict 생성(`data_sources/db.py:804-818`) → `PreparedFeatureStore(price_rows)` → `_engine_market_rows`가 OhlcvBar + metric dict로 **다시** 변환(`ai/ai_graph/nodes/backtest.py:1440-1469`) → `prepare_engine_market_data`가 인덱스 재구성.
- 워커 initargs로 `price_rows` 통째를 넘깁니다(`:684-688`).
- **이게 지연과 메모리를 동시에 만드는 핵심 지점**이고, 기존 문서 ⑤(캐싱)·⑥(백테스트 분할)이 다루려는 대상과 같은 곳입니다. 다만 진짜 문제는 조회 시간이 아니라 **표현 방식(dict-of-rows)** 입니다 — 서버측 10년 전체 count가 1초라는 건 코드 주석이 이미 확인해 둔 사실입니다(`db.py:40-42`).

#### B-6. 상관계수 계산이 순수 파이썬 O(N²·T) — 41초의 정체

- `_average_pairwise_correlation`: 후보 전체 쌍에 대해, 쌍마다 `sorted(set(a) & set(b))`로 날짜 교집합을 만들고 파이썬 루프로 공분산을 계산합니다(`ai/ai_graph/nodes/risk_manager.py:196-248`).
- 후보 20개면 190쌍 × 2,500일. 후보가 늘면 제곱으로 늘어납니다. numpy `corrcoef` 한 번이면 밀리초 단위인 계산입니다.
- **품질 측면 문제도 겹칩니다:** 상관을 **10년 전 구간**으로 계산합니다. "오늘 이 포트폴리오가 쏠려 있는가"에 10년 평균 상관은 답이 아닙니다(보통 60~120일).

#### B-7. 부분 실패가 정상 결과로 위장됨 (기존 ④의 다른 얼굴)

- 지표 패밀리 하나가 실패하면 `except Exception`으로 삼키고 그 패밀리 없이 진행합니다(`data_sources/db.py:785-802`).
- 결과: 해당 지표를 쓰는 조건은 `_ind()`가 NaN을 돌려줘 **영원히 매칭되지 않습니다**(`ai/ai_graph/nodes/backtest_code.py:739-747`). 전략은 아무것도 사지 않거나, 남은 조건만으로 다른 전략이 되어 돕니다. 그런데 **결과물은 정상적인 백테스트 리포트**입니다.
- `unavailable_indicator_families`가 metadata에 담기긴 하지만, "이 전략은 요청한 지표 없이 실행됐다"가 사용자 결론으로 올라오지는 않습니다.

#### B-8. 실패의 원자성 — 10분 계산이 마지막 노드 예외 하나로 통째 폐기

- 어떤 노드든 예외 → `instrument_node`가 그대로 re-raise → `run_analysis` 실패 → job FAILED (`ai/ai_graph/graph.py:240-258`, `jobs.py:388-409`).
- **부분 결과 저장 경로가 없습니다.** 백테스트가 9분 걸려 성공해도 Report 노드에서 LLM이 실패하면 그 9분은 사라집니다. 재시도하면 처음부터입니다(디스크 캐시가 후보 평가는 살려주지만, 노드 단위 체크포인트는 없습니다).

#### B-9. 취소가 노드 경계에서만 동작

- `raise_if_cancelled()`는 노드 진입 시점에만 호출됩니다(`graph.py:226-228`).
- 사용자가 **9분짜리 Backtest 노드 도중에** 취소하면 아무 일도 일어나지 않고, 노드가 끝난 뒤에야 반영됩니다. UI는 "중단됨"을 보여주는데 CPU는 계속 돕니다.

#### B-10. 디스크 캐시가 공유 `/tmp`이고, 매 요청마다 전체 스캔

- 기본 경로가 `gettempdir()/quantagent-backtest-v2` (`ai/ai_graph/nodes/backtest.py:365`).
- `_DiskEvaluationCache.__init__`가 매번 `_cleanup()`을 돌려 **디렉터리 전체를 glob + stat** 합니다(`:448-465`). 캐시 상한이 2GB이므로 파일이 수만 개가 될 수 있고, 그 스캔이 모든 백테스트 노드 시작 시점에 붙습니다.
- 멀티 프로세스 배포 시 동일 디렉터리를 여러 프로세스가 정리하며 경쟁합니다.

#### B-11. 계측 부재 — "어디가 느린가"를 사후에 알 방법이 없음

- `execution_stats`에 라운드별 wall_seconds, 캐시 히트, 워커 수는 남습니다(`ai/ai_graph/nodes/backtest.py:767-780`) — 백테스트 노드 **내부**만.
- **없는 것:** 노드별 소요 시간의 사용자·운영 노출, DB 쿼리별 소요, LLM 호출별 소요/토큰, 단계별 예산 대비 실측.
- 그래서 "41초짜리 상관 계산"류를 발견하려면 매번 사람이 코드를 읽어야 합니다. 기존 문서 ⑧(비용 계산)이 토큰만 다루는데, **시간도 같은 레이어에서 같이 집계되어야** 합니다.

---

## C. 측정해야 할 것 (지금 답할 수 없는 질문들)

문제 정의가 주장이 아니라 사실이 되려면 아래 숫자가 필요합니다. 전부 현재 미측정입니다.

| # | 질문 | 방법 | 대응 항목 |
|---|------|------|-----------|
| 1 | 생존편향이 수익률을 몇 %p 올리는가 | 상장폐지 포함 유니버스로 동일 전략 재실행 | A-1 |
| 2 | 워밍업 미적용이 초기 진입에 얼마나 기여하는가 | 창 길이 미만 구간 지표를 NaN 처리 후 diff | A-2 |
| 3 | in-sample과 out-sample 성과 격차 | 이미 계산됨(`degradation`) — **노출만 하면 됨** | A-3 |
| 4 | 신호 대비 미체결 비율 | 이미 계산됨(`execution_audit`) — 노출만 하면 됨 | A-6 |
| 5 | 전략이 KOSPI200을 이겼는가 | 지수 시계열 확보가 선행 조건 | A-11 |
| 6 | 노드별 소요 시간 분포 (p50/p95) | 노드 계측 + 예산 대비 기록 | B-11 |
| 7 | 동시 요청 N에서 RSS·응답시간·헬스체크 | 부하 테스트 (현재 미실시) | B-1, B-2, B-4 |
| 8 | 50만 행 파이프라인의 단계별 비용 | dict 생성 / 변환 / 워커 전달 각각 계측 | B-5 |

---

## D. 우선순위 제안

**"틀린 숫자를 빠르게 주는 것"보다 "맞는 숫자를 늦게 주는 것"이 먼저입니다.** 성능 작업(기존 문서 ⑤⑥)은 A-2, A-3, B-3을 정리하기 전에 하면, 잘못된 결과를 더 빨리 만드는 일이 됩니다.

| 순위 | 항목 | 이유 |
|------|------|------|
| 1 | A-2 (워밍업) | 명백한 버그. 재현·수정·검증이 모두 국소적 |
| 2 | A-3 + A-14 (검증 기준·노출) | 이미 계산된 `out_sample_sharpe`/`degradation`을 헤드라인으로 올리고, TEMPORARY 상수를 운영 기준으로 확정 |
| 3 | B-3 (요청 단위 deadline) | 618초의 직접 원인. 예산을 실제로 강제 |
| 4 | B-1 + B-2 (상태 외부화 + 분석 전용 동시성 상한) | 확장·안정성의 전제. 이거 없이는 부하 대응 선택지가 없음 |
| 5 | A-4 + A-6 (평가기 일원화) | 사용자 전략과 실행 전략의 불일치. 제품 신뢰의 근간 |
| 6 | A-1 (유니버스 편향) | 데이터(`listing_status`) 복구가 선행. 그전까지는 **명시적 고지**가 최선 |
| 7 | B-5 + B-6 (표현 방식·상관 계산) | 순수 성능. 위 항목들 이후 |
| 8 | A-13, A-15 | 선언만 된 설정·데드코드 정리 |

---

## E. 기존 문서와의 관계

| 기존 항목 | 이 문서에서의 갱신 |
|-----------|-------------------|
| ① 동기 실행 | **해결됨** — `BackgroundTasks` 전환 확인(`api.py:436`). 대신 B-2(스레드풀 공유)가 새로 생김 |
| ② AI 동시성 | B-1로 확장 — 문제는 토큰/rotation만이 아니라 **게이트가 프로세스 로컬**이라는 것 |
| ③ OCI 다운 | B-1·B-2·B-4가 구체적 메커니즘. 재기동 자동화는 증상 대응 |
| ④ DB 장애 위장 | B-7이 같은 병리의 다른 사례(지표 패밀리 유실). A-12도 같은 계열(지표 실패 → 0.0) |
| ⑤ 데이터 캐싱 | B-5로 정정 — 병목은 조회가 아니라 **행 표현 방식** |
| ⑥ 백테스트 병렬 | B-4 선결 필요 — 전역 동시성 인식 없이 분할하면 메모리만 늘어남 |
| ⑧ 비용 계산 | B-11 — 토큰과 **시간**을 같은 레이어에서 집계 |
