# QuantAgent 과잉설계·죽은코드 감사 — 2026-09-02

- 기준 SHA: `dbd196b` 계열(현재 작업 브랜치). 13개 서브시스템 병렬 감사, 75건 발견, 최대 **~6383줄** 절감 가능.
- 태그: `delete`(죽은 코드) · `yagni`(1회용 추상화·중복) · `stdlib`(표준 라이브러리로 대체) · `shrink`(같은 로직 더 짧게).
- **이 문서는 목록일 뿐, 아무것도 삭제하지 않았습니다.** `medium`/`low` 확신과 대형 삭제(db_split)는 실행 전 개별 확인 필요.

| # | 태그 | 확신 | 줄 | 위치 | 무엇을 자르나 → 대체 |
|---|---|---|---|---|---|
| 1 | yagni | medium | 2828 | `ai/ai_graph/data_sources/db_split.py:1` | db_split.py is a 2828-line, 84%-line-identical hand-maintained parallel copy of db.py, reachable ONLY via AI_DATA_SOURCE_VARIANT=db_split. Nothing sets that: docker-compose defaults to 'db', CI/tests set 'db', and every production import uses data_sources.db directly. Its only references are the __i… → Delete db_split.py, its __init__.py alias entry, and its 3 dedicated test files (test_db_split_source_manifest.py + the db_split cases). Re-add a variant only w… |
| 2 | delete | high | 854 | `fe/src/mocks/reportStrategies.mock.ts:1` | Three mock files that only import each other and are never consumed by any page/component/api: mocks/app.mock.ts (288 lines, terminal exports appOverview/activeStrategySpec/readyEnvelope/analysisJobStatus unused), mocks/reports.mock.ts (149, reportSummaries unused), mocks/reportStrategies.mock.ts (4… → nothing |
| 3 | delete | high | 497 | `quantagent_strategy/quantagent_strategy/__init__.py:1` | Entire quantagent_strategy/quantagent_strategy/ package (models.py 278L, strategy.py 172L, __init__.py 47L) — a stale near-duplicate of backtest_module. backtest_module is the live engine (imported by ai/ai_graph/nodes/backtest.py and backend/app/services/ai_backtest_subprocess_runner.py); quantagen… → Delete the whole package (source + its tests); no shim needed — backtest_module already supersedes it as the sole live engine. |
| 4 | delete | medium | 251 | `backend/app/services/email_provider_inspection.py:L1-L251` | Entire service module email_provider_inspection.py (BrevoEmailInspector, map_brevo_delivery_event, BrevoEmailEvent, BrevoBlockedContactStatus, BrevoTransactionalEmailReference). Only importer is tests/unit/test_email_track4.py; no route/worker wires it and no Brevo webhook route consumes it. Test-on… → nothing |
| 5 | delete | medium | 230 | `ai/ai_graph/graph.py:3799-3830` | A ~230-line tail block duplicates ai_graph/quant_performance.py: the 2nd def build_public_backtest_performance (3799-3829) plus its private helpers _pipeline_source (3788), _build_backtest_reliability (4053), _build_data_quality (4109), _build_public_benchmark (4126), _build_public_metric_details (4… → Keep only the quant_performance re-export import; delete the mirror block. Repoint the one test hook tests/test_metric_registry.py:55 (graph._metric_detail) to … |
| 6 | delete | high | 230 | `fe/src/features/reports/strategyReportFilters.ts:1` | Entire dead 'strategy reports' feature. features/reports/strategyReportFilters.ts (78 lines) has zero importers. In api/quantAgentClient.ts a whole cluster exists only to serve it and each other, never called by any page: getReportStrategies (L632), getStrategyReportById (L641), getStrategyWorkspace… → nothing |
| 7 | delete | high | 196 | `backend/app/services/fe_contract_store.py:L1467-L1664` | Top-level async function _persist_server_report() — a ~196-line server-report persistence path with zero call sites anywhere in the repo (production or tests). Superseded by _persist_ai_backtest_report() which immediately follows it. → nothing |
| 8 | delete | high | 145 | `backend/app/db/email_strategy_subscriptions.py:L1-L145` | Entire db module email_strategy_subscriptions.py (list_subscriptions/save_subscription/delete_subscription + _transaction/_list_rows/_response helpers). Nothing imports it — the routes it backed (/me/email-strategy-subscriptions) are retired stubs that call _retired_subscription_route(). The backing… → nothing |
| 9 | delete | medium | 120 | `ai/ai_graph/nodes/signal.py:78-129` | The entire single-ticker rule engine in signal.py: generate_signal() plus its input models SignalStrategy, SignalCondition, MarketSnapshot, ConditionOperator and the matcher helpers _matching_rules/_matches/_describe. It is reached only via the `if "market_snapshot" in state` branch of signal_node, … → Delete the models, generate_signal, and the matcher helpers; collapse signal_node to the else-branch WATCH stub (keep SignalResult + _result, which the stub sti… |
| 10 | delete | high | 115 | `ai/ai_graph/jobs.py:L395-L507` | ReadOnlyAnalysisJobStore wrapper class (a full pass-through delegate over AnalysisJobStore that refuses create_job) plus its companion AnalysisHistoryReadOnlyError. Grepped the whole repo: the class is never instantiated, imported, or referenced anywhere outside its own definition and the exception … → nothing — delete the class (jobs.py:395-507) and the exception (jobs.py:280-282) |
| 11 | delete | medium | 77 | `ai/ai_graph/llm/factory.py:83-161` | Public readiness API `live_provider_configuration_ready()` plus its only consumers `_live_provider_configuration_ready_for_role()` and `_configured_aoai_roles()` — a whole role-scoped config-validation cluster. Whole-repo grep confirms zero production callers; the only importer is ai/tests/test_llm_… → Delete all three functions (their test would go with them). If a health endpoint ever needs a preflight check, it can be reintroduced when a real caller exists. |
| 12 | delete | high | 56 | `ai/ai_graph/nodes/backtest_code.py:L1719-1774` | _self_improvement_grid_codes() — a ~56-line helper that builds a profile x lookback x threshold grid of rendered signal-code strings. Grepped whole repo (worktree): zero callers in production, tests, or scripts. Purely dead. → nothing |
| 13 | delete | medium | 54 | `ai/ai_graph/nodes/backtest_code.py:L565-612` | _strategy_template_candidates() — ~48-line strategy_id-prefix -> mock-candidate-list dispatcher. Its only callers are _non_legacy_llm_candidates (L1114) and _deterministic_fallback_candidates (L1124), both dead. Removing it also strips the five now-unused mock imports (BOLLINGER_/BREAKOUT_VOLUME_/RE… → nothing |
| 14 | delete | high | 43 | `ai/ai_graph/graph.py:1397-1439` | _record_analysis_memory is defined twice with byte-identical bodies (1397-1439 and 4008-4050). The single call site (line 1337 in envelope_node) binds to the last definition, so the first copy is pure shadowed dead code. No test imports it. → nothing — delete lines 1397-1440, keep the copy at 4008 |
| 15 | yagni | medium | 40 | `fe/src/components/ui/button.tsx:1` | Two parallel Button systems. components/ui/button.tsx is a full shadcn button (cva variants, class-variance-authority, @radix-ui/react-slot asChild) but has exactly one consumer: features/app/StrategyInputPanel.tsx (2 call sites using size=sm variant=onDark). The simpler components/common/Button.tsx… → Use the existing common/Button (add an onDark variant if needed) in StrategyInputPanel and delete ui/button.tsx |
| 16 | yagni | medium | 39 | `ai/ai_graph/data_sources/profile_aware.py:1` | profile_aware.py adds zero behavior: it re-exports db_test's public API via 'from .db_test import *', then re-declares PostgresPipelineDataSource as an empty 'pass' subclass and load_pipeline_data_from_env as a byte-for-byte copy of db_test's — both shadowing identical originals. Only reachable via … → Delete profile_aware.py; if the benchmark-facing name must stay, point the two aliases straight at db_test in __init__.py instead of a wrapper module. |
| 17 | delete | high | 34 | `fe/src/styles/tokens.ts:1` | styles/tokens.ts exports a `tokens` design-token object (colors/radii/shadow) that nothing imports. Confirmed zero importers across fe/src; the only textual hits are two comments. DailyDigestEmail.tsx's own comment says it hardcodes the token values inline instead of using this file. → nothing |
| 18 | delete | medium | 33 | `ai/ai_graph/nodes/backtest_code.py:L1288-1320` | _generated_strategy_candidates() — renders the compiled-condition + profile signal-code strings. Its only two callers are _candidate_code_pool (L1101) and _deterministic_fallback_candidates (L1127), both themselves dead; no test references it. It is the sole *production* caller of the ~360-line _ren… → nothing |
| 19 | delete | medium | 29 | `ai/ai_graph/research_contract.py:119-149` | `ExplorationCandidateRefV2` and `ExplorationExecutionSpecV2` are redefined here byte-for-byte identical to the same-named classes in ai/ai_graph/schemas.py (same fields, same `candidate_ids_are_unique` validator, same `is_executable`). research_contract already imports `ResearchCandidateExecutionSpe… → Delete the local class bodies and add them to the existing `from ai_graph.schemas import ...` — `ExplorationCandidateRefV2, ExplorationExecutionSpecV2`. isinsta… |
| 20 | stdlib | high | 26 | `ai/ai_graph/nodes/backtest.py:2907-2936` | _normal_quantile() hand-rolls Acklam's rational approximation of the inverse standard normal CDF (~30 lines of magic a/b/c/d coefficient tuples). Its only caller is _expected_max_sharpe (2 calls). → statistics.NormalDist().inv_cdf(p) (stdlib, 3.8+, more accurate). Keep only the p<=0/p>=1 -> 0.0 guard; the whole approximation body becomes one line. |
| 21 | delete | high | 21 | `ai/ai_graph/jobs.py:L1205-L1226` | _execution_events_from_backtest: a ~21-line ExecutionEvents builder that maps audit_events + equity_curve into signals/orders/fills/positions/trades/equity. Zero callers repo-wide. The live completion path builds events via _events_from_storage_ledger instead; this is a stranded alternate constructo… → nothing — delete the function |
| 22 | delete | medium | 21 | `backend/app/api/contract_policy.py:L315-L387` | Dead accessors in contract_policy.py: iter_contracts() (0 refs anywhere) and raise_feature_not_implemented() (a 501-raiser referenced only by a test, never by any route). → nothing |
| 23 | delete | high | 20 | `ai/ai_graph/api.py:2513-2532` | Module-private helpers `_find_job_by_trace_or_debug_ref` (api.py:2513-2522) and `_find_job_by_report_id` (api.py:2525-2532) have zero callers anywhere in the repo (grep-confirmed across ai/ and backend/; not referenced by any endpoint, __init__ re-export, or dynamic dispatch). Their lookup endpoints… → nothing (delete both functions) |
| 24 | shrink | medium | 20 | `ai/ai_graph/token_auth.py:353-360` | The lazy 'read app.state attr, build-if-None, store, return' cache block is copy-pasted 5 times: auth.py RequireAuthenticatedUser._resolve_resolver (97-104), token_auth.py RequireUserIdentity._resolve_resolver (353-360), RequireUserIdentityWithinQuota._resolve_quota (388-395), RequirePreflightIdenti… → One module helper `def _cached_on_app_state(request, attr, build): v = getattr(request.app.state, attr, None); if v is None: v = build(); setattr(request.app.st… |
| 25 | delete | high | 20 | `backend/app/services/fe_contract_store.py:L658-L677` | Function _analysis_run_trace_uuid() — derives a trace UUID from run payload; zero call sites repo-wide. → nothing |
| 26 | delete | high | 18 | `ai/ai_graph/nodes/backtest_code.py:L1091-1108` | _candidate_code_pool() — orchestrates de-duped LLM/generated/fallback candidate pooling. Grep of the entire worktree shows zero references anywhere (no prod, no test). It is the dead root of a subtree: it is the only thing that ties _generated_strategy_candidates + _deterministic_fallback_candidates… → nothing |
| 27 | delete | high | 17 | `ai/ai_graph/graph.py:3593-3609` | First build_public_backtest_performance def (1-arg, 3593-3609) is dead: overwritten first by the 2nd def at 3799 and finally by the quant_performance import at 4253. test_quant_performance.py:595 asserts graph.build_public_backtest_performance IS quant_performance's, confirming the local defs are ne… → nothing — delete; the re-export at line 4253 is the live binding |
| 28 | delete | high | 17 | `ai/ai_graph/graph.py:3715-3731` | _recommendation_gate is defined twice (3715 single-arg using state['strategy_validated']; 3863 with keyword-only performance param). The 3863 def shadows 3715, and all callers (line 1239 and the tests) resolve to 3863 since performance is optional. The 3715 body never executes. → nothing — delete lines 3715-3732, keep the def at 3863 |
| 29 | shrink | low | 15 | `ai/ai_graph/exploration_policy.py:223-271` | `load_active_exploration_policy_from_env` and `load_exploration_policy_from_env` are ~90% identical — same DSN resolution, same `psycopg.connect`/`dict_row` connector setup, same try/except mapping to `exploration_policy_database_unavailable`. They differ only in which inner loader they call (active… → Extract one private `_with_policy_connection(env, connector, loader)` helper holding the shared dsn/connect/except boilerplate; the two public functions become … |
| 30 | delete | high | 13 | `ai/ai_graph/data_sources/db.py:2275-2283` | indicator_families_for_metrics() plus its exclusive helper ALWAYS_LOADED_FAMILIES: a public-looking function with a docstring but zero callers anywhere in the repo (db_split.py has its own copy; db.py's is never imported or called). ALWAYS_LOADED_FAMILIES is referenced only inside this dead function… → Delete both. INDICATOR_FAMILY_BY_METRIC stays (db_test.py imports it independently). |
| 31 | delete | high | 12 | `ai/ai_graph/data_sources/db.py:2246-2256` | _PROFILE_INDICATOR_METRICS dict exists only to feed a discarded lookup: indicator_families_for_query does `_ = _PROFILE_INDICATOR_METRICS.get(_screening_profile(query), ())` (result thrown away) then unconditionally `return tuple(INDICATOR_TABLES)`. The per-profile metric table never affects the ret… → Delete the _PROFILE_INDICATOR_METRICS dict and the `_ = ...` line; indicator_families_for_query becomes `return tuple(INDICATOR_TABLES)`. |
| 32 | delete | high | 12 | `DE/quant_agent/data/models.py:85-96` | IngestionRun frozen dataclass — a fully-defined 10-field model with zero references anywhere in the repo (grep across DE/ and tests found only the definition). Ingestion runs are tracked via UUIDs and repository.start_ingestion_run/finish_ingestion_run, not this type. → nothing (delete the class) |
| 33 | yagni | medium | 12 | `DE/scripts/compute_technical_indicators_pipeline.py:139-150` | TaDbClient Protocol (4-method interface) is referenced exactly once — as the declared type of the `client` local at line 595. Every helper function annotates the concrete `client: DockerPsqlClient` instead, so the Protocol buys no polymorphism. → type the client local as `DockerPsqlClient \| PsycopgClient`; delete the Protocol |
| 34 | delete | high | 11 | `ai/ai_graph/nodes/condition_compiler.py:494-504` | compile_entry_expression() — a 'back-compat' wrapper that just calls compile_conditions() and returns .per_stock. Grep shows zero references anywhere in the repo, including tests, and it is not re-exported. → nothing |
| 35 | delete | low | 11 | `ai/ai_graph/nodes/risk_manager.py:73-83` | The VKOSPI risk rule in apply_risk_rules(). MacroSnapshot.vkospi is documented as always None (the warehouse carries no volatility index), so `macro.vkospi is not None and macro.vkospi > 30` can never fire — a dead branch plus its model field, retained as a speculative knob for a feed that does not … → Drop the vkospi rule block and the vkospi field, or leave a one-line note; re-add when a VKOSPI series is actually wired. |
| 36 | delete | high | 11 | `fe/src/utils/searchCandidateMeta.ts:1` | utils/searchCandidateMeta.ts exports formatTradingCandidateMeta, which has no callers anywhere in the repo (only its own definition). The file also carries a stray UTF-8 BOM. Its intended search-result consumer no longer exists. → nothing |
| 37 | delete | high | 10 | `ai/ai_graph/nodes/backtest_code.py:L1119-1128` | _deterministic_fallback_candidates() — builds a fallback candidate list; only caller is the dead _candidate_code_pool (L1105). No test or prod reference. Also the sole user of MOCK_BACKTEST_CODE_LLM. → nothing |
| 38 | shrink | low | 10 | `ai/ai_graph/token_auth.py:500-513` | `build_read_only_account_token_resolver_from_env` (500-513) is a trimmed copy of `build_account_token_resolver_from_env` (475-497): identical auth_enabled guard and dsn resolution, differing only by passing record_usage=False and omitting the Redis cache wrapper. Two functions maintain the same setu… → Add keyword params to the existing builder: `build_account_token_resolver_from_env(env=None, *, record_usage=True, cached=True)`; the preflight caller passes re… |
| 39 | delete | high | 10 | `ai/ai_graph/nodes/condition_compiler.py:631-640` | conditions_from_metadata() — validates raw dicts into Conditions. Grep shows zero references anywhere in the repo, including tests, and it is not re-exported. → nothing |
| 40 | delete | high | 10 | `DE/scripts/ingest_kis_adjusted_ohlcv.py:1099-1110` | Local sql_literal + jsonb_literal re-defined at the bottom of the script, byte-identical to the canonical helpers in quant_agent.data.db. The script already imports from quant_agent.data.db (line 41). → add sql_literal, jsonb_literal to the existing `from quant_agent.data.db import ...` line; delete the two local defs |
| 41 | delete | low | 10 | `DE/quant_agent/data/config.py:389-391` | KindConfig.is_configured and WicsConfig.is_configured return bool(self.corp_list_url)/bool(self.company_info_url), but those fields always fall back to a non-empty DEFAULT_* url, so both properties are structurally always True — and the `if not self.config.is_configured: raise SourceConfigurationErr… → drop the always-true properties and their unreachable guard branches (unlike Bok/Dart/Krx/Kis whose keys can genuinely be absent) |
| 42 | delete | medium | 9 | `ai/ai_graph/nodes/backtest_code.py:L10-18` | Import cleanup left after the above cuts: MOCK_BACKTEST_CODE_LLM (L60) + MockBacktestCodeLLM import become dead once _deterministic_fallback_candidates is removed; the mock candidate-list imports (L10-18) become dead once _strategy_template_candidates and the RSI constants are removed. → nothing |
| 43 | delete | medium | 9 | `DE/quant_agent/data/external.py:468-470` | _stable_hash (json.dumps sort_keys + sha256 hexdigest) is copy-pasted in 4 modules; external.py:468 and repository.py:1769 are byte-identical, seibro.py:447 is functionally identical (just a redundant local `import json`). One shared helper suffices. → define once (e.g. in quant_agent.data.db) and import; delete the duplicate copies in external.py, seibro.py (keep freshness_lineage_audit's variant if its separ… |
| 44 | shrink | low | 8 | `ai/ai_graph/nodes/backtest.py:692-699` | _DigestWriter is an 8-line file-like class that exists only to stream pickle bytes into a sha256 via pickle.Pickler(_DigestWriter(digest)).dump(rows). → digest.update(pickle.dumps(rows, protocol=pickle.HIGHEST_PROTOCOL)) and delete the class. Trades the streaming write for a transient full-pickle buffer; keep th… |
| 45 | delete | high | 8 | `ai/ai_graph/progress.py:L75-L82` | activity_listener_installed(): a public helper reporting whether an activity reporter is installed. No caller anywhere in the repo (the docstring claims the provider client uses it to decide on streaming, but nothing does). → nothing — delete the function |
| 46 | delete | medium | 8 | `ai/ai_graph/llm/concurrency_gate.py:L145-L151` | AOAIConcurrencyGate.current_capacity() and in_flight() accessors. observe_rate_limited_response() is used by production aoai.py, but these two methods are referenced only by test_llm_concurrency_gate.py — no production caller reads capacity/in-flight. → nothing — delete both methods (tests can assert via the private fields or be trimmed) |
| 47 | delete | high | 8 | `ai/ai_graph/nodes/signal.py:201-207` | build_investment_signal()'s `debate` keyword param and the `if debate:` block that appends bull/bear summaries. The only production caller (signal_node) and the test never pass debate; grep finds no `debate=` for this function. Dead since the debate was removed (per the node's own docstring). → Drop the `debate` parameter and lines 201-207. |
| 48 | delete | high | 8 | `ai/ai_graph/research_contract.py:112-116` | Two back-compat aliases that nobody imports: `StrategyExecutionSpecV1 = CanonicalRuleV1` (with a 4-line migration comment) and `ParseOutcomeV1 = ParseReviewV1`. Whole-repo grep shows every `StrategyExecutionSpecV1` reference resolves to the real class in schemas.py, and `ParseOutcomeV1` appears lite… → Delete both alias lines and their comments. |
| 49 | delete | high | 8 | `backend/app/services/fe_contract_store.py:L858-L865` | NoReturn helper _analysis_run_already_completed() — raises a 409 AppError but is never called (sibling _analysis_completion_payload_conflict handles the used path). → nothing |
| 50 | delete | medium | 8 | `backend/app/db/user_queries.py:L100-L107` | clear_user_schema_cache() — schema-cache invalidation utility 'for tests and post-migration reloads' that has zero call sites, including in tests. → nothing |
| 51 | yagni | medium | 8 | `DE/quant_agent/data/lineage_quality.py:59-66` | guard_downstream_result — a wrapper that just calls require_lineage_quality(report) then result_factory(). No production caller (grep finds only test_lineage_quality.py); callers use require_lineage_quality directly. → nothing (delete the helper; drop its dedicated test) |
| 52 | delete | medium | 8 | `DE/scripts/ingest_dart_bok_history.py:1161-1168` | Local chunk_date_range re-defined in the script (param renamed window_days), logic identical to quant_agent.data.ingestion.chunk_date_range. Script does not import ingestion but easily could. → import chunk_date_range from quant_agent.data.ingestion; delete the local copy |
| 53 | delete | high | 7 | `ai/ai_graph/data_sources/db.py:3118-3124` | _query_requires_rsi_oversold(query): no callers in db.py and not imported anywhere in the repo (only db_split.py references its own separate copy). Dead in this module. → Delete the function. |
| 54 | delete | high | 7 | `ai/ai_graph/data_sources/db.py:3127-3133` | _has_rsi_oversold_entry(price_rows): no callers in db.py and not imported anywhere (only db_split.py uses its own copy). Dead in this module. The RSI_OVERSOLD_THRESHOLD constant it reads stays, since a test imports that constant directly. → Delete the function. |
| 55 | yagni | low | 7 | `ai/ai_graph/jobs.py:L899-L906` | InMemoryAnalysisJobStore.create(), get(), and run_sync() — thin aliases that just call create_job()/get_job()/run_job_sync(). They exist only for test ergonomics; every production call site uses the canonical create_job/get_job names. → nothing — drop the aliases and have the tests call create_job/get_job/run_job_sync directly |
| 56 | delete | high | 7 | `backend/app/db/existing_report_queries.py:L455-L461` | Helper _format_ratio_value() — formats a numeric ratio to 2dp; zero call sites repo-wide. → nothing |
| 57 | delete | high | 7 | `DE/scripts/compute_technical_indicators_pipeline.py:1396-1403` | Local sql_literal re-defined, identical to quant_agent.data.db.sql_literal. Script already imports from quant_agent.data.db (line 50). → add sql_literal to the existing db import; delete the local def |
| 58 | delete | high | 6 | `ai/ai_graph/nodes/backtest_code.py:L1111-1116` | _non_legacy_llm_candidates() — filters legacy templates out of an LLM candidate list. Grep of whole worktree: zero references anywhere (no prod, no test). → nothing |
| 59 | delete | high | 6 | `ai/ai_graph/nodes/backtest_code.py:L557-562` | _candidate_violation_summaries() — formats candidate violation strings. Grep of whole worktree: zero references anywhere. → nothing |
| 60 | delete | high | 6 | `fe/src/api/quantAgentClient.ts:414` | Two more unused exports in api/quantAgentClient.ts: getLatestAnalysisJob (L414) is a thin wrapper over readLatestAnalysisJob() with no callers, and confidenceToPercent (L1351) has no callers. Both verified unused internally and externally (getWorkspaceTemplate/latestAnalysisReportId etc. are used, s… → nothing |
| 61 | yagni | medium | 5 | `ai/ai_graph/llm/mock.py:730-752` | `MockBacktestCodeLLM(MockLLMClient)` is a one-subclass override whose only job is a bigger `generate_backtest_candidates`. The base `MockLLMClient.generate_backtest_candidates` (lines 730-733) is dead: the sole call site is `MOCK_BACKTEST_CODE_LLM.generate_backtest_candidates(...)` where `MOCK_BACKT… → Move the full method onto `MockLLMClient`, delete the dead 4-line base stub and the `MockBacktestCodeLLM` subclass, and change backtest_code.py to `MOCK_BACKTES… |
| 62 | stdlib | high | 4 | `ai/ai_graph/nodes/backtest.py:3108-3113` | _compound_returns() manually folds `equity *= 1.0 + r` over a sequence to get prod(1+r)-1. → math.prod(1.0 + r for r in daily_returns) - 1.0 (empty seq -> math.prod=1 -> 0.0, same as the loop). |
| 63 | delete | high | 4 | `ai/ai_graph/data_sources/db.py:2032-2041` | _dart_amount(field, *, prior=False): the `prior` parameter and its `->'raw'->>'frmtrm_amount'` branch are never exercised — every one of the ~11 call sites uses the default (prior=False). Speculative flexibility. → Drop the `prior` param and the conditional; inline the single path `accounts_jsonb->'{account}'->>'amount'` into the CASE expression. |
| 64 | delete | high | 4 | `ai/ai_graph/data_sources/db_split.py:667` | _fetch_screening_candidates is a 2-line delegating wrapper ('return self._screen_with_relaxation(conn, query)[0]') with zero callers anywhere in the repo (grep for the method name finds only the two definitions). Dead in db_split.py and identically dead in db.py:735. → Delete the method from both db_split.py:667 and db.py:735. |
| 65 | delete | high | 3 | `ai/ai_graph/nodes/backtest_code.py:L61-63` | Module constants SAFE_RSI_CODE / CONSERVATIVE_RSI_CODE / SMOOTHED_RSI_CODE = MOCK_BACKTEST_CODE_CANDIDATES[0..2]. Defined at import time and never read anywhere in the repo (prod or tests). → nothing |
| 66 | delete | high | 3 | `ai/ai_graph/data_sources/db.py:735-736` | _fetch_screening_candidates(self, conn, query): thin one-line wrapper returning self._screen_with_relaxation(conn, query)[0], with no callers anywhere in the repo. → Delete the method; callers already use _screen_with_relaxation directly. |
| 67 | delete | medium | 3 | `ai/ai_graph/data_sources/__init__.py:13` | _DATA_SOURCE_VARIANT_ALIASES carries three entries that resolve to the two dead modules above — 'db_split' (L13), 'db_test_profile_aware' (L15), 'profile_aware' (L16). No deployment, CI job, or test ever sets AI_DATA_SOURCE_VARIANT to any of them; the only live values are 'db' (default) and db_test … → Drop those 3 alias entries when the db_split and profile_aware modules are removed, leaving db/db_test. |
| 68 | shrink | high | 3 | `ai/ai_graph/nodes/report.py:457-460` | _report_safe_performance(): after the None guard, an `if isinstance(performance, PerformanceAvailable)` branch and the fallthrough both `return performance.model_dump(mode="json")` — the isinstance check selects between two identical statements. → Replace lines 457-460 with a single `return performance.model_dump(mode="json")`. |
| 69 | delete | medium | 3 | `DE/quant_agent/data/catalogs.py:35-37` | BOK_MONTHLY_OIL_SERIES_IDS — a frozenset comprehension with zero references in the repo (not even tests). → nothing (delete the constant) |
| 70 | delete | low | 2 | `ai/ai_graph/data_sources/db.py:44-49` | TRADING_DAYS_PER_YEAR and DEFAULT_BACKTEST_LOOKBACK_DAYS: the backtest window is now a calendar window (BACKTEST_EVALUATION_YEARS, resolved from the trading calendar), per the adjacent comment. These bar-count constants are unused by production code; DEFAULT_BACKTEST_LOOKBACK_DAYS survives only beca… → Delete both once the test's assertion is dropped/rewritten; low confidence because a test currently references DEFAULT_BACKTEST_LOOKBACK_DAYS. |
| 71 | delete | high | 2 | `backend/app/dependencies.py:L36-L37` | FastAPI dependency get_stock_universe_engine() — returns request.app.state.trading_data_db_engine; never referenced by any route or Depends (get_trading_data_engine covers the used path). → nothing |
| 72 | delete | high | 2 | `backend/app/db/session.py:L129-L130` | Helper rows_from_mappings() — one-liner list-comprehension wrapper ([dict(row) for row in rows]) with zero call sites repo-wide. → nothing |
| 73 | delete | high | 2 | `DE/quant_agent/data/repository.py:131-132` | store_api_request_log (singular) — a one-line convenience wrapper around store_api_request_logs([event], run_id) with zero callers anywhere. → nothing (delete; callers already use the plural batch method) |
| 74 | delete | high | 1 | `ai/ai_graph/graph.py:154` | Module constant _PUBLIC_METRIC_UNAVAILABLE_REASON = 'metric unavailable in this analysis window' is defined at line 154 and referenced nowhere in the repo. → nothing — delete the line |
| 75 | delete | high | 1 | `ai/ai_graph/token_auth.py:350` | `request.state.resolved_account_token = resolved` is assigned in RequireUserIdentity.resolve_token but never read anywhere (grep across the repo returns only this one line). It is a leftover; the preflight flow that actually reads request-local token state uses a different key, `preflight_account_to… → nothing (delete the assignment line) |
