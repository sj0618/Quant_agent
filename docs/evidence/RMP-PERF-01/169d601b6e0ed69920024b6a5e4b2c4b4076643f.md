# RMP-PERF-01 evidence bundle

- `wbs_id`: `RMP-PERF-01`
- `subject_implementation_sha`: `169d601b6e0ed69920024b6a5e4b2c4b4076643f`
- `tested_sha`: `169d601b6e0ed69920024b6a5e4b2c4b4076643f`
- `tested_tree`: `992d1da2d4b83f24163798f40102dc114d375999`
- `bundle_revision`: the Git revision that contains this file; its immutable URI is
  recorded in the WBS evidence cell.
- `output_uri`: this file's Git blob at that same immutable bundle revision.

## Acceptance evidence

Commands, run without database or provider secrets:

```sh
env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL \
  -u AI_AOAI_API_KEY -u AI_AOAI_ENDPOINT -u AI_AOAI_DEPLOYMENT \
  -u AI_LLM_API_KEY -u OPENAI_API_KEY \
  ai/.venv/bin/python -m pytest -q \
  ai/tests/test_quant_performance.py ai/tests/test_jobs.py ai/tests/test_report.py \
  ai/tests/contracts/test_api_envelope_contract.py ai/tests/test_api.py

ai/.venv/bin/python -m pytest -q \
  backtest_module/tests/test_backtest.py \
  backtest_module/tests/test_models.py \
  backtest_module/tests/test_strategy.py

cd backend/fe-api-preview && npm run typecheck
```

Results: each command exited `0`; independent QA observed `56` Python contract tests,
`37` backtest-module tests, and TypeScript typecheck passing.  The post-fix consolidated
Python run also passed `152` tests with `3` server-only skips.  `available` requires a
complete engine-generated methodology manifest; `unavailable` is a separate structural
variant with no metrics or chart fields.  API, jobs, reports, production smoke, and the
preview client were tested against that discriminated contract.

## Review record

- Reviewer kind: `independent_codex_owner_delegated` (non-human)
- Reviewer: Codex independent QA; product changes authored by reviewer: `false`
- Owner delegation: the product owner explicitly delegated QA closure while the assigned
  QA reviewer was unavailable.
- Assigned WBS reviewer: `조은채` (preserved in the WBS; not impersonated)
- Verdict: PASS for the stated release contract
- Reviewed at: `2026-08-24 00:32:34 KST`

This closes a public-contract implementation gate. It does not claim a live return or
production market-data result.
