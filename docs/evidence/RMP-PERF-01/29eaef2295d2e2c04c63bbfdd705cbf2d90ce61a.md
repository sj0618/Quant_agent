# RMP-PERF-01 evidence bundle (re-run at 29eaef2)

- `wbs_id`: `RMP-PERF-01`
- `tested_sha`: `29eaef2295d2e2c04c63bbfdd705cbf2d90ce61a`
- `tested_tree`: `6fc637dab917ab70f924b80c2e19121c39f2638b`
- `executed_at`: `2026-08-24 KST`
- `executed_by`: implementation lane (윤서준). Execution record only; no review verdict.
- `supersedes`: nothing. The earlier bundle at
  `169d601b6e0ed69920024b6a5e4b2c4b4076643f` stays as the record for its own SHA; this
  file re-runs the same fixed commands at current `HEAD`.

## Acceptance wording under test

> metadata 누락이나 신뢰도 부족이면 성과 숫자와 chart가 전부 unavailable

## Observed commands and results

```sh
env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL \
  -u AI_AOAI_API_KEY -u AI_AOAI_ENDPOINT -u AI_AOAI_DEPLOYMENT \
  -u AI_LLM_API_KEY -u OPENAI_API_KEY \
  ai/.venv/bin/python -m pytest -q \
  ai/tests/test_quant_performance.py ai/tests/test_jobs.py ai/tests/test_report.py \
  ai/tests/contracts/test_api_envelope_contract.py ai/tests/test_api.py
```

`exit_code=0` — `62 passed, 1 warning in 2.99s`.

```sh
ai/.venv/bin/python -m pytest -q \
  backtest_module/tests/test_backtest.py \
  backtest_module/tests/test_models.py \
  backtest_module/tests/test_strategy.py
```

`exit_code=0` — `42 passed in 2.53s`.

```sh
cd backend/fe-api-preview && npm ci && npm run typecheck
```

`npm ci` `exit_code=0`; `npm run typecheck` (`tsc -b --pretty false`) `exit_code=0`, no
diagnostics emitted. `npm ci` was required because this worktree had no
`backend/fe-api-preview/node_modules`; it is a setup step, not part of the acceptance
command.

## Acceptance-axis coverage

| Axis | Covered | Evidence |
|---|---|---|
| 신뢰도 부족 → 숫자·chart 전부 unavailable | yes | `ai/tests/test_quant_performance.py::test_public_projection_removes_every_metric_and_chart_for_insufficient_data` |
| metadata 누락 → complete value 미노출 | yes | `ai/tests/test_quant_performance.py::test_public_projection_requires_engine_manifest_before_exposing_complete_values` |
| unavailable state가 클라이언트 타입과 일치 | yes | `backend/fe-api-preview` typecheck passes against the discriminated envelope. |

Supporting rows in the same file pin the shape of the `unavailable` variant rather than
its trigger: `test_public_metrics_missing_engine_scalars_become_none`,
`test_public_profit_factor_rejects_an_engine_default_even_without_a_saved_availability_map`,
`test_public_profit_factor_warning_cannot_be_overridden_by_stale_availability`, and
`test_public_metrics_consume_fail_closed_walk_forward_availability`.

## Count drift against the earlier bundle

The `169d601b` bundle recorded `56` Python contract tests and `37` backtest-module tests.
The same command selections now report `62` and `42`. The difference is tests added
between the two SHAs, not a change in selection. Recorded so the two bundles are not read
as disagreeing.

## Coverage limits

- Every assertion here is against fixtures and an in-memory job store. No production
  database, no live provider, no deployed environment.
- This is a public-contract implementation gate. It is not a live return, a market-data
  result, or a release judgement.
- Checkout is the linked worktree `.claude/worktrees/yunseojune-task-distribution-93cea6`.
  From the repository root both `ai_graph`'s test paths and `backtest_module` resolve to
  this worktree. The `backend` package's editable install resolves to the primary
  checkout and is not exercised by these commands.
- Interpreter: `ai/.venv/bin/python`, Python `3.11.15`, pandas `3.0.3`, pyarrow absent.

## Review record

- Reviewer: **not assigned in this bundle.** Self-approval is prohibited for this row.
- Verdict: **none recorded.**
