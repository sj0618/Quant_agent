# FT-DB-02 증적 번들

- `wbs_id`: `FT-DB-02`
- `subject_implementation_sha`: `c4e8a89a907aa0aa736bd043686712132ef07455`
- `tested_sha`: `29eaef2295d2e2c04c63bbfdd705cbf2d90ce61a`
- 실행 환경: [`docs/evidence/_RUNNER.md`](../_RUNNER.md)
- 수용 기준(WBS): 정확한 status·reason·retry policy와 UI contract PASS

## 고정 command

```sh
cd ai && pytest -q
```

```
751 passed, 9 skipped, 1 warning in 27.41s
```

`exit=0`

## 이 행을 실제로 덮는 테스트

전체 suite PASS는 이 행의 수용 기준을 직접 읽어주지 않는다. 해당 축을 검사하는
테스트를 지목해 따로 실행했다.

```sh
cd ai && python -m pytest -q \
  tests/test_api.py::test_database_connection_failure_has_a_safe_retryable_envelope \
  tests/test_screening_pipeline_failure_classifier.py::test_connection_timeout_maps_to_safe_failure_bucket
```

```
2 passed, 1 warning in 0.42s
```

`exit=0`

`ai/tests/test_api.py:950`이 단언하는 내용(원문 대조):

| 수용 기준 축 | 단언 |
|---|---|
| status | `result["status"] == "failed"` |
| reason | `failure_cause.category == "infrastructure_failure"`, `subcause == "db_connection_unavailable"`, `failure_stage == "interpreting"`, `owner == "data_source_config"` |
| retry policy | `result["retryable"] is True`, `failure_cause.retryable is True` |
| 오류 상세 누출(WBS 위험 항목) | `"must-not-leak" not in repr(result)` — DSN의 `password=` 문자열이 봉투에 남지 않음 |
| 부분 결과 미발행 | `strategy_spec is None`, `candidate_cards == []`, `performance is None`, `report is None` |

사용자에게 보이는 문구는 `"운영 데이터 소스에 연결할 수 없습니다. 잠시 후 다시 시도해 주세요."`
하나로 고정되어 있다.

## 커버한 축 / 미커버 축

| 수용 기준 축 | 상태 | 근거 |
|---|---|---|
| 정확한 status | 커버 | `status == "failed"` |
| 정확한 reason | 커버 | `failure_cause` 6필드 완전 일치 단언 |
| retry policy | 커버 | `retryable is True` 두 층 |
| 오류 상세 비노출 | 커버 | 비밀 문자열 부재 단언 |
| **UI contract PASS** | **미커버** | 이 증적에는 프런트엔드 실행이 없다. `cd fe && npm test`도, Playwright 렌더도 돌리지 않았다. UI가 이 봉투를 어떻게 표시하는지는 확인되지 않았다. |
| 실제 PostgreSQL 연결 실패 | **미커버** | `OperationalError`를 주입한 fake runner로 검증했다. 운영 DB의 실제 장애 분류는 서버 증적으로만 닫힌다. |

## 승인

reviewer 미지정. 이 번들을 만든 주체가 승인하지 않는다.
