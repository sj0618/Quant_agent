# RMP-QA-01 Execution Evidence

## 검증 대상

- WBS ID: `RMP-QA-01`
- 단계: 8/31 리서치 MVP 복구
- 작업 원문: `공개 contract·fail-closed data·preflight·performance·ownership·prohibited copy·browser E2E를 release candidate에서 독립 검증한다.`
- Done · QA 계약: `offline unit/integration/browser suite PASS, fixture가 production evidence로 사용된 0건, P0/P1 blocker와 artifact URI가 모두 기록된다.`
- WBS 지정 실행검증(원문): `cd fe && npm test && cd ../ai && python3.11 -m pytest tests -q`
- 이 문서의 성격: **소유자 실행(owner execution) evidence다.** WBS 소유자가 검증 업무를 실제 수행한 결과이며, 그 결과가 `NOT PASS`라는 사실 자체를 소유 업무의 정당한 산출물로 기록한다. **최종 release approval/sign-off는 별도 승인자의 몫이며, 이 문서가 그것을 대신하지 않는다.**
- repo: `Quant_agent`(canonical, public)
- application tested SHA: `785642dbdc071de143b66e996628df3ea2699a0c`(`origin/main` 최신, 별도 read-only worktree에서 검증 — 현재 evidence branch를 전환/병합하지 않음)
- Workspace lock: `LOCK-QA-EVIDENCE-01 · contract/integration/e2e release evidence · application source 수정 금지`
- **WBS 상태 후보**: `증적대기`(현재 시트상 `대기` → 이번 owner execution 완료에 따라 `증적대기`로 전이 후보. 실제 시트 반영은 이 문서와 별개로 결정)

## 검증 환경 및 범위

- OS: Windows
- 실행 방식: `origin/main`(tested SHA)을 가리키는 detached read-only git worktree를 별도로 생성해 검증. 현재 작업 branch의 HEAD·working tree는 변경하지 않음.
- FE: `npm ci` 후 `npm test` 실행(로컬 실행환경 의존성 설치, repo 파일 변경 아님).
- AI: `ai/`와 `backtest_module/`을 각각 editable install(`pip install -e .`) 후 실행. `quantstats`는 `backtest_module/pyproject.toml`에 선언돼 있어 `ai/`만 설치하면 누락됨을 확인하고 함께 설치함(로컬 실행환경 보완, 소스 변경 아님).
- AI 실행 시 `AI_DATABASE_DSN`, `QUANT_DB_DSN`, `DATABASE_URL`, `AI_AOAI_API_KEY`, `AI_AOAI_ENDPOINT`, `AI_AOAI_DEPLOYMENT`, `AI_LLM_API_KEY`, `OPENAI_API_KEY`를 `env -u`로 제거(값 기록 없음, 변수명만).
- application source·test 코드는 이 검증 과정에서 전혀 수정하지 않았다.

## WBS 지정 verifier

```text
cd fe && npm test && cd ../ai && python3.11 -m pytest tests -q
```

- **literal `python3.11` verifier는 이 환경에 존재하지 않아 그대로 실행할 수 없었다.** (`python3.11` 바이너리 부재, 이 환경엔 Python 3.12.5만 설치됨)
- `ai/pyproject.toml`의 `requires-python = ">=3.11"` 기준으로 Python 3.12가 호환됨을 확인한 뒤, **동등 대체 실행**으로 진행했다. WBS 원문 실행과 대체 실행을 아래에서 명확히 구분해 기록한다.

## 실제 실행 결과

### FE (`cd fe && npm test`, WBS 원문 그대로 실행)

- exit code: `1`
- **68 total / 62 pass / 6 fail / 0 skip**
- 실패 6건(`landing-archive-cta.test.mts` 1건, `report-archive-safety.test.mts` 4건, `research-result-renderer.test.mts` 1건)은 **기존에 이미 기록된 Windows `file://` pathname 호환성 문제와 동일**(`new URL(...).pathname` 이중 드라이브 경로 결합으로 `vite.config.ts` 로드 실패). 새로운 failure 없음.

### AI — literal WBS verifier

- `python3.11 -m pytest tests -q`: **실행 불가**(interpreter 부재).

### AI — Python 3.12 equivalent execution(대체 실행, WBS 원문과 동일시하지 않음)

```text
env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL -u AI_AOAI_API_KEY -u AI_AOAI_ENDPOINT -u AI_AOAI_DEPLOYMENT -u AI_LLM_API_KEY -u OPENAI_API_KEY python -m pytest tests -q
```

- exit code: `1`
- **830 total / 800 pass / 30 fail / 12 skip**
- **30건 중 29건이 동일한 failure signature인 `KeyError: 'investment_signal'`을 공유한다**(`ai_graph/nodes/risk_manager.py`). 근본 원인이 동일하다고 단정하지 않으며, 동일한 오류 시그니처(예외 종류·파일·메시지)가 반복 관측됐다는 사실만 기록한다. 정확한 원인은 이번 검증에서 코드를 수정하지 않았으므로 확정 진단이 아니며, 후속 조사가 필요한 후보로 남긴다.
- 나머지 1건은 다른 signature(`TypeError`).
- skip 12건은 실 서버 DSN 미설정 시 자동 skip되도록 설계된 integration 테스트 계열로 추정된다(요약 로그 기준, 개별 skip 사유 문자열은 이번 실행에서 캡처하지 않음).

## Done 항목별 결과

| Done 항목 | 판정 | 근거 |
|---|---|---|
| offline unit/integration suite PASS(FE) | `FAIL` | 62/68, 6건 실패(기존 known Windows path issue) |
| offline unit/integration suite PASS(AI) | `FAIL` | 800/830, 30건 실패(29건이 동일 failure signature 공유) |
| offline browser suite PASS | `PENDING` | WBS 지정 verifier(`npm test`)가 `fe/e2e/**`(Playwright)를 아예 실행하지 않음을 확인. 별도 명령(`npm run test:production-readiness`)이 repo에 존재하나 실배포 URL이 필요해 이번 턴에서 실행하지 않았다 — 따라서 이 조건은 PASS도 FAIL도 아닌 `PENDING`으로 보수적으로 판정한다. |
| fixture가 production evidence로 사용된 0건 | 위반 증거 없음 | fixture 안전성을 직접 검사하는 FE 테스트들은 전부 PASS. AI 쪽 해당 테스트 1건은 위 29건과 동일한 failure signature로 미완주했을 뿐, fixture가 실제로 production evidence에 섞였다는 증거는 발견되지 않았다. |
| blocker 기록 | `완료` | 아래 참조 |
| artifact URI | `PENDING` | evidence 파일 생성·커밋 후 해당 commit/repo URI로 확정 예정 |

## 반증 및 미검증 범위

- FE 6건 실패는 기존 기록과 동일한 failure signature로 재현됐으며, 이번 실행에서 새로운 failure signature는 관찰되지 않았다.
- AI 실패 중 29건이 동일한 failure signature(`KeyError: 'investment_signal'`, 동일 파일·동일 라인)를 공유한다는 사실은 직접 대조해 확인했다(근본 원인 동일성은 단정하지 않는다).
- `PYTHONUTF8=1`/`PYTHONIOENCODING=utf-8` 환경변수로 재현을 시도했으나 동일하게 실패해, 화면에 보인 한글 mojibake는 실행 결과의 원인이 아니라 터미널 출력 표시 문제로 판단했다(응용 코드 결함과는 별개).
- browser E2E는 전혀 실행되지 않아 `browser suite PASS`는 완전히 미검증 상태로 남는다.
- 실 서버/운영 DB 접근 없이 오프라인으로만 실행했다.
- application source·test·fixture는 이 검증 과정에서 전혀 수정하지 않았다.

## 소유자 실행 결과

- application tested SHA: `785642dbdc071de143b66e996628df3ea2699a0c`
- FE: `68 total / 62 pass / 6 fail`(기존 기록과 동일한 Windows path compatibility failure signature)
- AI literal verifier(`python3.11`): 환경에 없어 실행 불가
- AI Python 3.12 equivalent: `800 pass / 30 fail / 12 skip`(30건 중 29건 동일 failure signature `KeyError: 'investment_signal'` 공유)
- browser E2E: WBS verifier에 포함되지 않아 `PENDING`
- fixture → production evidence 유입: 직접 증거 없음
- blocker 기록: `완료` / artifact URI: `PENDING`

### 상태 구분

| 구분 | 값 |
|---|---|
| WBS 상태 후보 | `증적대기` |
| verification result | `NOT PASS` |
| owner execution | `완료` |
| reviewer | `pending` |

**상태 표현: `실행완료 — verification NOT PASS, blocker recorded, reviewer pending`**

## 검토 기록

- 이 문서는 **WBS 소유자(RMP-QA-01) 실행 evidence**다. 소유자가 검증 업무를 실제로 수행했고, 그 결과인 `NOT PASS`를 있는 그대로 기록한다.
- **`APPROVE`, `reviewer approved`, `QA approved`, 최종 승인을 의미하는 어떤 표현도 이 문서에 기록하지 않는다.** 최종 release approval/sign-off는 WBS가 지정한 별도 승인자의 판단에 맡긴다.
- 실행/증적 작성: WBS 소유자 실행 지원 (AI-assisted, Claude Code)
- 검토일: `2026-08-27 (KST)`
