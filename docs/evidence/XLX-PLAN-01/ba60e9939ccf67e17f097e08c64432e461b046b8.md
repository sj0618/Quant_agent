# XLX-PLAN-01 증적 묶음

> **Migration note**: 이 문서는 public-safe 이관본이다. pre-migration(비공개) 저장소에서 작성된 원본 QA 증적을 public `Quant_agent` 저장소로 옮긴 사본이며, 새로 QA를 수행한 문서가 아니다. 기존 QA verdict와 tested SHA는 그대로 유지했고, 개인 식별정보와 private repository 참조만 일반화했다.
>
> **Migration check**: 이 evidence가 검증한 대상 파일(`outputs/quantagent-production-wbs-20260813/QuantAgent_프로덕션_신뢰성_WBS_계획본.xlsx`)은 2026-08-26 기준 `Quant_agent/main`(`4bc31482f91ec7e75b446dd3464519c95d96439a`)과 blob hash가 완전히 동일하다(`4dbccb90ecca57e0fc3537eed047e65899f3bdac`). 아래 결론은 여전히 유효하다.

## 검증 대상

- wbs_id: `XLX-PLAN-01`
- subject_implementation_sha: `dba9ea14171456ea1e01a4f61a06d288cd582f4e` (WBS `증적 묶음 URI`에 기록된 "git:dba9ea1", `origin/main`의 ancestor로 확인됨)
- tested_sha: `ba60e9939ccf67e17f097e08c64432e461b046b8` (fetch 시점의 `origin/main` HEAD)
- tested_tree: `b9fd41ffd9f5671032b1e67d00037a0fd6a914fe`
- bundle_revision: 이 파일이 포함된 Git revision. 불변 URI는 WBS 증적 칸에 기록됨.
- output_uri: 동일한 불변 bundle revision에서의 이 파일의 Git blob.

## 최신 Done

2_WBS(gid `2030261433`) `XLX-PLAN-01` 행 기준:

- 작업: 원본 template hash, sheet order, used range, formulas, styles를 검사해 계획본 디자인 기준을 고정한다.
- Done · QA 계약: `source hash match, 1_RnR/2_WBS/3_Members order match, 원본 read-only`
- 실행·검증(WBS 지정): `node .omx/scripts/verify-production-readiness-plan.mjs`
- Owner/승인자: WBS 원문 참조 / 상태·계약: 증적대기

## 기존 증적

WBS `증적 묶음 URI` 칸: `git:dba9ea1 · 커밋만 · 증적 묶음·검토 대기`. GitHub Issue 검색(`XLX-PLAN-01`) 결과 이 ID를 다루는 기존 Issue/PR은 없다.

## 실제 추가 검증

대상 파일: `outputs/quantagent-production-wbs-20260813/QuantAgent_프로덕션_신뢰성_WBS_계획본.xlsx` (commit `dba9ea1`에서 추가된 "계획본").

### source hash match / 원본 read-only

```
git rev-parse dba9ea1:"outputs/.../QuantAgent_프로덕션_신뢰성_WBS_계획본.xlsx"
→ 4dbccb90ecca57e0fc3537eed047e65899f3bdac
git rev-parse origin/main:"outputs/.../QuantAgent_프로덕션_신뢰성_WBS_계획본.xlsx"
→ 4dbccb90ecca57e0fc3537eed047e65899f3bdac
git log --all --oneline -- "outputs/.../QuantAgent_프로덕션_신뢰성_WBS_계획본.xlsx"
→ dba9ea1 한 커밋만 존재 (추가 이후 수정 이력 없음)
```

`dba9ea1`이 이 파일을 추가한 이후 `tested_sha`까지 Git blob 해시가 완전히 동일하고, 이 경로를 건드린 커밋이 추가 커밋 하나뿐임을 확인했다. 즉 파일이 바이트 단위로 원본과 일치하며(source hash match) 이후 수정되지 않았다(원본 read-only).

### 1_RnR/2_WBS/3_Members order match

동일 커밋에 포함된 구조 검사 기록(`....xlsx.inspect.ndjson`)의 `"kind":"sheet"` 항목을 대조했다.

```
{"kind":"sheet","name":"1_RnR","index":0,...}
{"kind":"sheet","name":"2_WBS","index":1,...}
{"kind":"sheet","name":"3_Members","index":2,...}
```

시트 순서가 `1_RnR`(0) → `2_WBS`(1) → `3_Members`(2)로 Done이 요구하는 순서와 정확히 일치한다.

### WBS 지정 명령 실행

```
node .omx/scripts/verify-production-readiness-plan.mjs
```

결과: `Error: Cannot find module '...\.omx\scripts\verify-production-readiness-plan.mjs'`, `code: MODULE_NOT_FOUND`, exit `1`. 이 저장소에 `.omx/` 디렉터리가 존재한 적이 없어(RS-LED-01 bundle과 동일 확인) 지정 명령은 실행할 수 없다. 위 두 항목은 Git 객체 해시와 커밋에 포함된 구조 검사 기록으로 직접 대체 검증했다.

## Done별 결과

| Done 하위 항목 | 결과 | 근거 |
|---|---|---|
| source hash match | **PASS** | `dba9ea1`과 `tested_sha`의 Git blob 해시 동일(`4dbccb9...`) |
| 1_RnR/2_WBS/3_Members order match | **PASS** | 커밋에 포함된 inspect.ndjson의 시트 index 0/1/2 순서 확인 |
| 원본 read-only | **PASS** | 해당 경로를 건드린 커밋이 추가 커밋 1건뿐 |

## 제한사항 및 non-blocking observation

- WBS `실행·검증`에 지정된 `node .omx/scripts/verify-production-readiness-plan.mjs`는 이 저장소에 한 번도 존재한 적이 없어 실행 불가(exit 1, MODULE_NOT_FOUND)이다. RS-LED-01 evidence bundle에서도 동일하게 확인된 공통 gap이며, Done 자체를 직접 위반하는 반증은 아니다.
- `formulas, styles` 검사는 WBS `작업` 서술에는 언급되나 Done 문구 자체(`source hash match, 1_RnR/2_WBS/3_Members order match, 원본 read-only`)에는 별도 항목으로 명시되지 않았다. source hash가 완전히 동일하므로 formula·style을 포함한 파일 전체가 원본과 바이트 단위로 일치함은 이미 포함되어 있다고 판단했다.
- application source·product test·WBS는 이 QA 과정에서 전혀 수정하지 않았다.

## 검토 기록

- QA 실행/증적 수집: AI-assisted QA (Claude Code)
- WBS 지정 승인자: (WBS 원문 참조)
- 최종 판정: `APPROVE WITH NON-BLOCKING OBSERVATIONS`
- 검토일: `2026-08-24 (KST)`
