# `QV-PTI-01` independent QA evidence

- `wbs_id`: `QV-PTI-01`
- `role`: independent QA reviewer — WBS-designated approver이며 owner와 다른 사람. self-QA 아님.
- `tested_baseline_sha`: `2783022c59711353dabe54da0403ae287e8dd4cd` (`origin/main`)
- `execution_method`: 현재 evidence branch·working tree를 건드리지 않고, tested baseline SHA를 대상으로 한 격리된 detached worktree에서 검증을 수행한 뒤 worktree를 제거했다. 이후 evidence branch/HEAD/working tree가 변경되지 않았음을 재확인했다.

## WBS contract

- 작업: `point-in-time 유니버스와 상장폐지 처리의 데이터 계약을 정한다.`
- Done·QA 계약: `as-of universe·delisting·source version·sample fixture가 정의됨`
- 실행·검증(literal verifier): `cd ai && pytest -q`

## historical reference 상태

**Historical reference: none — the WBS evidence cell contains a non-resolvable template placeholder rather than a concrete canonical SHA/PR/URI.**

WBS evidence cell(증적URI)에는 resolvable canonical SHA/PR/URI가 없었다 — 실제 커밋을 가리키는 참조가 아니라 generic template placeholder("commit only · QV-PTI-01 · local verification")만 존재했다. 이는 실제 SHA/PR이 존재하는 stale reference가 아니라, **애초에 canonical reference 자체가 채워지지 않은 상태**다. 따라서 lineage 재검증 대상으로 삼지 않았고, 대신 canonical tested baseline에서 Done 계약과 일치하는 구현을 직접 탐색했다.

## implementation/artifact mapping

WBS Work/Done 계약 원문을 기준으로 canonical tested baseline에서 확인한 실제 대상:

- `DE/docs/point-in-time-universe-evidence-contract.md` — as-of 기준 유니버스·상장폐지 편입 규칙과 immutable evidence 필드(`source`, `as_of`, `source_version`, `lineage_hash`, `universe_snapshot_hash`, `indicator_input_hash`, `formula_version`, `seed_hash`)를 정의하는 문서.
- `DE/migrations/013_point_in_time_universe_membership.sql` — `source_version` 컬럼을 포함한 실제 DB 스키마.
- `DE/fixtures/release-strategy-validation-manifest.json` — 위 8개 필드를 전부 채운 sample fixture.
- `ai/ai_graph/immutable_snapshot.py` — `pit_universe`/`delisting`/`indicator_input` 세 종류의 snapshot과 이를 묶는 `ImmutableSnapshotBundle` 계약 모델. literal verifier 범위(`ai/`) 안에 위치.
- `ai/tests/test_immutable_snapshot.py` — 위 모델에 대응하는 sample fixture와 테스트.

## Done·QA 항목별 결과

| # | Done 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | as-of universe 정의됨 | **PASS** | 계약 문서의 as-of 기준 멤버십 규칙 + `ImmutableSnapshotBundle`의 `as_of` 필드/검증 로직을 직접 확인. |
| 2 | delisting 정의됨 | **PASS** | 계약 문서의 상장폐지 편입 규칙 + `delisting` snapshot kind와 정책 버전이 fixture에 구체화됨을 확인. |
| 3 | source version 정의됨 | **PASS** | 계약 문서가 `source_version` 필드를 명시적으로 정의하고, 실제 DB 컬럼과 sample fixture 값이 일치함을 확인. |
| 4 | sample fixture 정의됨 | **PASS** | DE 쪽 fixture와 ai 쪽 Python fixture(`_bundle()`) 모두 구체적으로 존재하며 대응 테스트가 PASS함을 확인. |

## literal verifier 결과 — canonical tested baseline(격리 worktree)

```
$ cd ai && pytest -q
831 passed, 12 skipped, 1 warning
exit code: 0
```

## direct-corresponding test 결과

```
$ pytest -v tests/test_immutable_snapshot.py
test_pit_delisting_and_indicator_inputs_are_one_valid_immutable_bundle PASSED
test_snapshot_hash_changes_when_any_input_changes PASSED
test_bundle_rejects_mixed_as_of_dates PASSED
3 passed
exit code: 0
```

## runtime/operational scope

이 WBS 항목의 Done 계약은 data/evidence contract의 "정의"만 요구하며, live/runtime integration 동작을 완료조건으로 요구하지 않는다(계약 문서 자체가 "This contract defines the minimum reproducible input... not proof of a live release or a substitute for a PostgreSQL evidence run"이라고 명시). 이에 따라 runtime/operational 검증은 수행하지 않았다.

## server/database deployment scope

이번 QA에서 실제로 확인한 것은 **repository-level PIT data contract**다 — canonical GitHub repository에 PIT 관련 문서, migration, fixture, model이 존재함을 검증했다. 그러나 다음은 검증하지 않았다:

- shared/server PostgreSQL에 해당 migration(`013_point_in_time_universe_membership.sql`)이 실제 적용됐는지
- 서버 DB에 PIT membership 데이터가 실제 적재돼 있는지
- 서버 DB에서 delisting/as-of query가 실제로 동작하는지

**Server/database deployment state was not verified. This QA establishes the repository-level PIT data/evidence contract only; it does not establish that the corresponding migration or PIT data is deployed or populated in the shared/server database.**

범위를 명시적으로 분리하면:

- repository-level contract 정의 → 이번 QA 범위
- server DB migration 적용 → 미검증
- 실제 PIT 데이터 적재 → 미검증
- live pipeline integration → 미검증

현재 WBS Done 계약("point-in-time 유니버스와 상장폐지 처리의 데이터 계약을 정한다" / "as-of universe·delisting·source version·sample fixture가 정의됨")은 repository-level 정의만 요구하므로, 위 미검증 사항은 blocker로 승격하지 않는다.

## S/R/O/C 참고 상태

- S = **PASS**
- R = **N/A** — pytest 실행 여부와 관계없이, 현재 WBS Done 계약이 live/runtime integration 동작을 완료조건으로 요구하지 않기 때문.
- O = **N/A** — 현재 WBS Done 계약이 shared/server DB deployment 또는 운영 DB 상태를 완료조건으로 요구하지 않기 때문.
- C = **N/A**

**주의**: R/O의 `N/A`는 "서버를 확인해서 정상임이 확인됐다"는 뜻이 아니다 — 현재 Done 계약이 애초에 그 축을 요구하지 않는다는 뜻이며, server DB deployment state 자체는 이번 QA에서 실제로 확인하지 않았다(위 "server/database deployment scope" 절 참조).

`QA-WBS-01`은 DRAFT/PENDING이므로 이 분류는 참고용이며 최종 판정의 필수조건으로 사용하지 않았다.

## Observations

1. WBS evidence cell에는 resolvable canonical SHA/PR/URI가 없고 template placeholder만 존재했다.
2. 핵심 DE 문서·schema·fixture는 literal verifier `cd ai && pytest -q`의 직접 테스트 범위 밖이다. 다만 canonical tested baseline에서 해당 artifact들을 직접 읽어 Done 계약과의 일치를 확인했다.
3. `ai/ai_graph/immutable_snapshot.py`의 계약 모델은 현재 직접 테스트는 존재하지만 broader ai_graph pipeline에서 아직 소비되지 않는다. 현재 Done 계약은 "defined"만 요구하므로 non-blocking이다.
4. shared/server DB에서 migration 적용, PIT 데이터 적재, 실제 as-of/delisting 동작은 검증하지 않았다. 현재 WBS의 repository-level definition 계약 범위 밖이므로 non-blocking이다.

## Blocker

없음.

## Reviewer

- 이 문서는 independent QA reviewer evidence다.
- owner ≠ approver, self-QA 아님.

## Final verdict

**`APPROVE WITH OBSERVATIONS`**

## verdict rationale

1. Done·QA 계약 4개 세부 항목 전부 canonical tested baseline에서 문서·스키마·코드·fixture를 직접 읽고 대응 테스트로 PASS 확인했다.
2. literal verifier가 exit 0, 831 passed / 12 skipped / 실패 0건으로 완전히 통과했다.
3. 직접 대응 테스트(3/3 PASS)로 재확인했다.
4. historical reference는 실제 SHA가 존재하는 stale reference가 아니라 애초에 채워지지 않은 placeholder였을 뿐이며, 동일 계약이 canonical tested baseline에서 문서·스키마·코드 3중으로 독립 검증 가능했으므로 FAIL/BLOCKED 사유로 사용하지 않았다.
5. Done 계약 정의 대상이 literal verifier 범위 밖(DE/)에 걸쳐 있다는 점과, 정의된 모듈이 아직 파이프라인에 통합되지 않았다는 점은 non-blocking observation으로만 남긴다.
6. 현재 WBS Done 계약은 repository-level 정의만 요구하므로, server/database deployment state(migration 적용·데이터 적재·live query 동작)를 검증하지 않았다는 사실도 non-blocking observation으로 분리했다 — 이는 별도의 O-level/운영 검증 대상이며 이번 verdict를 좌우하지 않는다.
