# `XLX-PLAN-02` independent QA evidence

- `wbs_id`: `XLX-PLAN-02`
- `role`: independent QA reviewer — WBS-designated approver이며 owner와 다른 사람. self-QA 아님.
- `tested_baseline_sha`: `cd8815a4c215174d7526a5fdbfb36063d01a07ad` (`origin/main`)
- `execution_method`: 현재 evidence branch·working tree를 건드리지 않고, tested baseline SHA를 대상으로 한 격리된 detached worktree에서 검증을 수행한 뒤 worktree를 제거했다. application source·대상 workbook은 수정하지 않았다. 이후 evidence branch/HEAD/working tree가 변경되지 않았음을 재확인했다.

## Governance observation (approver)

reviewed WBS export의 approver cell은 비어 있었다. 실제 designated approver는 별도로 확인됐으며, export의 공백은 기록 공백으로 취급한다. `owner != designated approver`이며 self-QA가 아니다. 이 governance observation은 blocker가 아니다.

## WBS contract

- 작업(Work): `template 복제본에 현재 계획의 RACI·원자 WBS·용량 결정을 한국어로 채운다.`
- Done·QA 계약: `1행=1 intended commit, 모든 필수 열, 디자인·시트 순서 보존`
- 실행·검증(literal verifier): `node .omx/scripts/verify-production-readiness-plan.mjs`

`QA-WBS-01`(S/R/O/C)은 이 WBS 항목의 completion gate로 추가하지 않았다 — WBS 원문이 명시적으로 요구하지 않는다.

## 대상 artifact

`outputs/quantagent-production-wbs-20260813/QuantAgent_프로덕션_신뢰성_WBS_계획본.xlsx`

## Literal verifier

```
$ node .omx/scripts/verify-production-readiness-plan.mjs
Error: Cannot find module '...\.omx\scripts\verify-production-readiness-plan.mjs'
code: 'MODULE_NOT_FOUND'
exit code: 1
```

- result: **FAIL**
- reason: canonical tested baseline에 `.omx/scripts/verify-production-readiness-plan.mjs` 경로 자체가 존재하지 않는다.
- 대체 명령으로 literal verifier PASS를 가장하지 않았다.
- 이 verifier 실패와 아래 Done A/B/C 직접 audit 결과는 별도로 판정했다 — verifier 부재만으로 Done 조건을 자동 FAIL 처리하지 않았다.

## Work contract verification

| 항목 | 결과 | 근거 |
|---|---|---|
| template 복제본 기반 | **SATISFIED** | 대상 파일은 canonical main에서 이미 잠긴 시트 구조(`1_RnR/2_WBS/3_Members`)를 사용하는 복제본 계열 파일이다. |
| RACI | **PARTIAL** | owner/backup/선행/lock 정보는 행마다 존재하나, 명시적인 R/A/C/I 4역할 구조는 확인되지 않았다. |
| 원자 WBS | **SATISFIED** | 90개 행이 각각 고유 ID·작업·intended commit을 가진다. |
| 용량 결정 | **SATISFIED** | 90/90행에 `추정: Nh` 형태의 용량 정보가 존재한다. |
| 한국어 작성 | **SATISFIED** | 작업 열 90/90행 전부 한국어, 비한국어 행 0건. |

**Work contract overall: `PARTIAL`** — RACI observation은 blocker가 아니다.

## Done A/B/C

### Done A — `1행=1 intended commit`: **PASS**

- 검사 대상: 90개 atomic row(요약행 QAG-001 제외)
- intended commit 공백: 0건
- 중복 intended commit(여러 행이 동일 값 공유): 0건
- 문제 있는 row: 없음

### Done B — `모든 필수 열`: **PASS WITH OBSERVATION**

- header 17개 열 전부 존재, 누락 없음
- 핵심 열(우선/ID/소유/작업/의도된 커밋) 공백: 0건
- "상태" 열 공백 2건: `MT-DATA-03`, `UX-ARCH-02`
- merged cell로 인한 오판이 아님을 직접 확인했다(해당 두 행 주변에 merge 없음, 시트 전체 merge는 헤더 행과 무관한 2건뿐).

### Done C — `디자인·시트 순서 보존`: **PASS WITH OBSERVATION**

- worksheet order: `1_RnR`, `2_WBS`, `3_Members` — 전부 visible
- structural design(시트명·순서·가시성)은 baseline과 일치
- repo 내 과거 prototype 산출물은 시트 구조가 근본적으로 달라 비교 baseline에서 제외했다
- pixel-perfect/style-level(column width, row height, 세부 style)까지 비교 가능한 독립 baseline이 repo 안에 없어 그 범위의 검증은 수행하지 않았다 — WBS에 없는 기준으로 검증 범위를 확장하지 않았다.

## Quantitative audit (canonical tested baseline에서 직접 재계산)

| 항목 | 결과 |
|---|---|
| SHA-256 | `92311cc787929b94963f77a180343185f859bfe7e27e0af048de77398ec2fa6b` |
| size | 2,059,497 bytes |
| atomic Korean rows(QAG-001 제외) | 90 |
| worksheet formulas(raw XML `<f>` 전체) | 331 |
| formula error tokens | 0 |

## Historical evidence comparison

| historical claim | current canonical 결과 | 판정 |
|---|---|---|
| SHA `92311cc…` | `92311cc787929b94963f77a180343185f859bfe7e27e0af048de77398ec2fa6b` | **MATCH** |
| 90 atomic Korean rows | 90 | **MATCH** |
| worksheet formulas 331 | 331 | **MATCH** |
| formula errors 0 | 0 | **MATCH** |

historical PR #190/#199 및 관련 SHA(`5de61fd`, `250cf98`, `405b248`)는 canonical `origin/main`과 공통 조상이 없는 disconnected/orphan lineage이며, canonical repo에 해당 PR 번호 자체가 존재하지 않는다. 위 수치는 이 historical 참조를 신뢰하지 않고 canonical tested baseline에서 독립적으로 재계산한 결과다.

## Observations

1. "상태" 열 2/90행 공백(`MT-DATA-03`, `UX-ARCH-02`).
2. RACI는 명시적 4-role(R/A/C/I) 구조가 아니라 owner+backup 수준의 기능적 대응만 존재한다.
3. WBS literal verifier(`.omx/scripts/verify-production-readiness-plan.mjs`)가 canonical repo에 부재한다 — 이번 QA 대상 항목 고유의 결함이 아니라 repo 전반에 걸친 기존 verifier-path 공백으로 보인다.
4. reviewed WBS export의 approver cell이 비어 있었다(기록 공백, governance observation, blocker 아님).
5. (참고) 대상 workbook 내부 각 행이 자체적으로 인용하는 실행검증(`node scripts/check-production-plan.mjs`)은 canonical repo에 실제로 존재한다 — meta-tracking WBS 행 자체가 인용하는 verifier와는 대조적이다.

## Blocker

`none`

## Reviewer

- 이 문서는 independent QA reviewer evidence다.
- owner ≠ designated approver, self-QA 아님.

## Final verdict

**`APPROVE WITH OBSERVATIONS`**

근거:
- Done A/B/C 모두 canonical artifact 직접 audit으로 PASS했다(B, C는 non-blocking observation 동반).
- historical quantitative claims(SHA·행수·수식수·오류수) 전부 canonical tested baseline에서 독립 재계산해 MATCH를 확인했다.
- literal verifier FAIL은 이 항목 고유의 결함이 아니라 repo 전반의 verifier infrastructure observation으로 별도 분리했다.
- RACI 표현 방식 및 상태 2행 공백은 재현 가능한 실제 관찰 사항이지만 계약의 실질을 무효화하지 않는 non-blocking observation이다.
