# FT-EMPTY-05 증적 번들

- `wbs_id`: `FT-EMPTY-05`
- `subject_implementation_sha`: `01ed9ddb539668df9a0024020ff9a9c41e89b1a0`
- `tested_sha`: `29eaef2295d2e2c04c63bbfdd705cbf2d90ce61a`
- 실행 환경: [`docs/evidence/_RUNNER.md`](../_RUNNER.md)
- 수용 기준(WBS): empty error code, no cache reuse, no recommendation PASS

## 고정 command

```sh
cd ai && pytest -q
```

```
751 passed, 9 skipped, 1 warning in 27.41s
```

`exit=0`

## 이 행을 실제로 덮는 테스트

```sh
cd ai && python -m pytest -q tests/test_screening_pipeline_failure_classifier.py
```

```
10 passed in 0.25s
```

`exit=0`

수용 기준 축과 테스트의 대응:

| 수용 기준 축 | 테스트 |
|---|---|
| empty error code (schema 오류와 구분) | `test_empty_screen_is_a_data_gap_answer_not_an_unknown_crash`, `test_schema_validation_failure_does_not_zero_fill_a_successful_analysis` |
| no cache reuse | `test_empty_runner_result_is_not_completed_or_reused_as_an_analysis` |
| 빈 결과가 성공으로 끝나지 않음 | `test_run_job_sync_reports_an_empty_screen_as_a_finished_failed_job` |
| 인접 실패와의 분리 | `test_missing_price_history_is_reported_separately_from_an_empty_screen`, `test_connection_timeout_maps_to_safe_failure_bucket`, `test_lock_exhaustion_is_named_not_swallowed_as_unknown` |
| 원문 예외 미노출 | `test_run_job_sync_uses_safe_failed_envelope_instead_of_raw_exception` |

## 커버한 축 / 미커버 축

| 수용 기준 축 | 상태 | 근거 |
|---|---|---|
| empty error code | 커버 | 위 두 테스트 |
| no cache reuse | 커버 | `test_empty_runner_result_is_not_completed_or_reused_as_an_analysis` |
| **no recommendation** | **부분** | 이 파일의 테스트들은 job이 `failed`로 끝나 사용자 payload가 비는 것까지 단언한다. 그러나 "추천 0건"을 이름으로 단언하는 테스트는 이 선택 안에 없다. `recommendation_gate`가 빈 결과에서 어떤 값을 갖는지는 별도 단언이 필요하다. |
| UI 표시 | **미커버** | 프런트엔드 실행 없음. |
| 운영 빈 응답 | **미커버** | 합성 runner 결과로 검증했다. 운영 스크리닝의 실제 빈 응답은 서버 증적 필요. |

## 승인

reviewer 미지정. 이 번들을 만든 주체가 승인하지 않는다.
