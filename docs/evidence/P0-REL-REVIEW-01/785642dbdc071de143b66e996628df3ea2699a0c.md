# P0-REL-REVIEW-01 Owner Review Evidence

## 검토 대상

- WBS ID: `P0-REL-REVIEW-01`
- 단계: 8/31 release QA
- 작업 원문: `release artifact·QA gate·rollback 증적을 독립 검토하고 QA-REL sign-off를 남긴다.`
- Done · QA 계약: `QA-AUTH/DATA/AI/UX/JOB/REPORT gate와 artifact URI를 교차 검토`
- WBS 지정 실행검증(원문): `node .omx/scripts/verify-production-readiness-plan.mjs`
- 이 문서의 성격: **소유자 리뷰(owner review) evidence다.** WBS 소유자가 release artifact·QA gate·rollback 증적을 실제로 교차 검토한 결과이며, 최종 결론이 `NOT READY`라는 사실 자체를 리뷰 업무의 정당한 산출물로 기록한다. **최종 QA-REL 승인/release approval/sign-off는 별도 승인자의 몫이며, 이 문서가 그것을 대신하지 않는다.**
- repo: `Quant_agent`(canonical, public)
- Workspace lock: `LOCK-QA-EVIDENCE-01 · release artifact · QA evidence · read-only review (application source 수정 금지)`
- reviewed baseline: `origin/main = 785642dbdc071de143b66e996628df3ea2699a0c`(fetch 시점 최신)

## 검토 환경 및 범위

- 이 문서는 **read-only evidence aggregation**이다 — 새로운 테스트를 실행하지 않고, 최신 `origin/main` tree, 현재 evidence branch, 최신 WBS export를 직접 대조해 작성했다.
- application source·test 코드는 이 검토 과정에서 전혀 수정하지 않았다.
- **아래 표에서 세 가지를 명확히 구분한다**: (1) `WBS 시트 상태` = Google Sheets export에 적힌 값 그대로(수정하지 않음, stale일 수 있음), (2) `repo evidence` = `docs/evidence/`에 실제로 존재하는 파일(main 반영 여부 별도 표기), (3) `직접 검증` = 이번 검토 세션에서 origin/main tree 또는 실제 서비스를 직접 확인해 얻은 사실.

## WBS 지정 verifier

```text
node .omx/scripts/verify-production-readiness-plan.mjs
```

- `git ls-tree -r origin/main`으로 전체 tree를 직접 검색한 결과, 이 파일은 **최신 origin/main 어디에도 존재하지 않는다**(`.omx/`는 `fe/.omx/`에 상태 JSON만 있음, 별도 스크립트 디렉터리 없음).
- **literal verifier = `UNAVAILABLE`**. 대체 스크립트를 생성하거나 이를 우회하는 코드를 작성하지 않았다. 이하 gate 검토는 verifier 실행 없이 evidence 직접 대조로만 수행했다.

## Gate Inventory

각 표의 열: **WBS ID | WBS 시트 상태 | repo evidence / 비고 (⚡=이번 세션 직접 검증)**

### QA-AUTH (WBS 원문 지정, 실명 생략 — gate-level 요약: `대기`)

| WBS ID | WBS 시트 상태 | repo evidence / 비고 |
|---|---|---|
| P0-BE-01 | 완료(라벨) | evidence 존재(main). *migration 커밋 메모: target files have since changed on main — stale 가능성 명시됨* |
| UX-AUTH-02 | 증적대기 | #118 · commit · npm test PASS, reviewer 검토 대기 |
| UX-AUTH-03 | 대기(시트, 미갱신) | **evidence는 evidence branch(`evidence/qa-batch-20260827`)에 존재, main에는 아직 없음.** ⚡ 이번 세션 owner execution: login/refresh/logout **functional PASS**, formal Playwright trace **PENDING**(환경 제약, durable screenshot으로 보완) |
| UX-AUTH-04 | 대기(시트) | repo에 evidence 없음. **`BLOCKED — strategy generation unavailable · private-data fixture not established`는 사용자가 제공한 상태이며, 이번 검토에서 WBS 시트/repo 자체 근거로는 독립 확인되지 않았다** — 사용자 제공 정보로만 인용한다. |
| UX-AUTH-05 | 대기(시트) | repo에 evidence 없음. ⚡ **BLOCKED, 최신 origin/main에서 직접 재확인**: rate-limit UX가 backend/FE 어디에도 구현돼 있지 않음. `token_auth.py` 주석에 "세션쿠키 인터랙티브 사용자는 의도적으로 quota 대상에서 제외"라고 명시돼 있어, **현재 origin/main 구현 기준으로 session-cookie interactive 사용자에게 해당 rate-limit scenario가 발생하지 않도록 되어 있음.** |
| P0-REL-REVIEW-03 | 진행 | PR merged·deploy SUCCESS·public Playwright PASS, 독립 human review/authenticated artifact pending |

### QA-DATA (WBS 원문 지정 — gate-level: `대기`)

| WBS ID | WBS 시트 상태 | repo evidence / 비고 |
|---|---|---|
| FT-RLS-01, FT-DB-02 | 증적대기 | 옛 SHA 번들만 존재(사전 lane 조사에서 stale로 확인) |
| FT-FIX-08 | 증적대기 | acceptance 문구 대조 미수행 |
| P0-DE-01, P0-DE-02 | 증적대기 | commit-only placeholder |
| QA-DATA-AUDIT-01 | **완료** | evidence 존재(main) |
| QA-DATA-AUDIT-02 | **완료** | evidence 존재(main) |
| QA-DATA-AUDIT-03 | 증적대기 | **known blocker** — 동일 input hash에서 universe/delisting 재현성을 직접 검증하는 테스트 부재, GitHub Issue #16으로 추적 |
| QA-DATA-AUDIT-04 | **완료** | evidence 존재(main) |
| QA-DATA-AUDIT-05 | 증적대기 | commit-only placeholder |
| P0-REL-REVIEW-04 | 대기 | 빈 템플릿 |

### QA-AI (WBS 원문 지정 — gate-level: `대기`)

| WBS ID | WBS 시트 상태 | repo evidence / 비고 |
|---|---|---|
| FT-LLM-03 | 완료(라벨) | reviewer APPROVE 기록 — *참조 PR 번호가 canonical repo 범위 밖으로 추정, 직접 대조 불가* |
| FT-SCH-04, FT-EMPTY-05, FT-L4-06 | 증적대기 | commit-only placeholder |
| EV-FT-03/04/05 | 증적대기 | commit-only, 독립검토 대기 |

### QA-UX (WBS 원문 지정 — gate-level: `대기`)

| WBS ID | WBS 시트 상태 | repo evidence / 비고 |
|---|---|---|
| OD-E2E-01 | **차단** | D-CORE-001 superseded — historical evidence로만 보존, product gate 아님 |
| UX-MOB-01, UX-MOB-02 | 증적대기 | npm test PASS, reviewer 검토 대기 |
| UX-ROUTE-01 | 완료(라벨) | evidence 존재(main). *migration 메모: target files changed since verdict — stale 가능성* |
| UX-ROUTE-02, UX-BUILD-01, UX-CTA-01, MR-UI-01, MR-CLAIM-01 | 증적대기 | 대부분 human approval/reviewer pending 상태로 남음 |
| P0-REL-REVIEW-03 | 진행 | 위와 동일 |

### QA-JOB (WBS 원문 지정 — gate-level: `대기`)

| WBS ID | WBS 시트 상태 | repo evidence / 비고 |
|---|---|---|
| FT-JOB-07 | 진행 | commit-only |
| P0-DB-01, EV-FT-06 | 증적대기 | commit-only, 독립검토 대기 |
| OD-JOB-01 | **차단** | D-CORE-001 superseded |

### QA-REPORT (WBS 원문 지정 — gate-level: `대기`)

| WBS ID | WBS 시트 상태 | repo evidence / 비고 |
|---|---|---|
| MR-REG-01, MR-PF-01, MR-ALL-01, MR-API-01 | 증적대기 | commit-only placeholder |
| MR-ENG-01 | 증적대기 | R/O + reviewer approval pending |
| MR-UI-01, MR-CLAIM-01 | 증적대기 | 위 QA-UX와 동일 |
| QV-PTI-01, QV-OOS-01, QV-BIAS-01, QV-EXE-01 | 증적대기 | commit-only |
| P0-REPLAY-01 | 증적대기 | commit-only. ⚡ 스크립트(`DE/scripts/replay-strategy-validation-report.mjs`) 존재는 이번 세션 이전에 직접 확인됨 |
| QA-DATA-AUDIT-05 | 증적대기 | 위와 동일 |
| P0-REL-REVIEW-02 | 대기 | 빈 템플릿 |

### QA-REL (WBS 원문 지정 — gate-level: `대기`)

| WBS ID | WBS 시트 상태 | repo evidence / 비고 |
|---|---|---|
| EV-GATE-01 | **완료** | evidence 존재(main), 별도 세션에서 재검증 완료 이력 있음 |
| EV-QA-01 | 증적대기 | evaluator 365 tests PASS, 독립검토 대기 |
| P0-BE-02 | 대기(시트, 미갱신) | 시트는 구 저장소 링크 placeholder. **evidence는 evidence branch에 존재, main에는 아직 없음** — **독립 검증 evidence에서 durable job store / migration revision / AI contract version 3조건 PASS**, 실서버 probe·최종 승인은 별도 대기 |
| **P0-REL-01** | 대기 | **빈 템플릿**(`실행 후 URI · exit=0 · reviewer 기록`) — release ID, deploy log, authenticated smoke, rollback readiness, reviewer 2명 중 **어느 것도 아직 기록되지 않았다** |
| **P0-REL-REVIEW-01**(이 문서) | 대기 | 이번 리뷰로 `증적대기` 전이 후보 |
| P0-REL-REVIEW-02, 04 | 대기 | 빈 템플릿 |
| P0-REL-REVIEW-03 | 진행 | 위와 동일 |

## Release artifact / Rollback evidence

- **P0-REL-01**(release ID·deploy log·authenticated smoke·rollback readiness·reviewer 2명) — **빈 템플릿, 실행되지 않음.**
- 이번 검토 범위(WBS export 및 canonical repo)에서는 실제 server snapshot/restore 및 DB migration rollback drill evidence를 확인하지 못했다.
- `1_RnR` 시트의 `P0-SUP-DEPLOY-SNAPSHOT-READINESS-01`(rollback snapshot 워크플로 계약): "검증 완료 · 실제 server snapshot/restore 및 DB migration rollback drill 증적 대기"
- `P0-SUP-DEPLOY-RELEASE-TRUST-01`: "검증 완료 · GitHub CI/배포 증적 대기"
- `P0-SUP-PUBLIC-READINESS-QA-01`의 시트 기록(`BLOCKED · 공개 endpoint timeout`, 2026-08-14 시점)은 **stale로 보인다** — ⚡ 이번 세션 중 `https://qt-agent.kro.kr/ai-api/api-status`를 직접 호출해 200 OK·`job_store.active_mode=persistent` 응답을 확인했다. 다만 이 사실이 시트에 반영돼 있지는 않다.

## 반증 및 미검증 범위

- **WBS gate-level summary 기준 PASS 확정: 0 / 7 (7개 모두 상태 '대기')** — QA-AUTH, QA-DATA, QA-AI, QA-UX, QA-JOB, QA-REPORT, QA-REL.
- "완료" 라벨이 붙은 몇 건(P0-BE-01, UX-ROUTE-01)은 migration 시점 메모에 "target files have since changed on main"이라고 명시돼 있어, 현재 코드 기준 재검증 없이는 신뢰할 수 없다.
- FT-LLM-03, MR-ENG-01 등 일부 evidence가 참조하는 PR 번호는 canonical repo(`Quant_agent`, public)의 실제 PR 범위를 벗어나 있어 이 저장소만으로는 대조되지 않는다.
- literal verifier가 `UNAVAILABLE`이라 자동화된 gate 재검증은 수행하지 못했다 — 이 문서의 판정은 evidence 파일·WBS 시트 직접 대조에 근거한다.
- 이번 검토에서 application source·test·WBS는 전혀 수정하지 않았다.

## Owner Review 결과

- literal verifier: `UNAVAILABLE`
- WBS gate-level summary 기준 PASS 확정: `0 / 7 (7개 모두 상태 '대기')`
- RMP-QA-01: `NOT PASS`(evidence branch에 존재, main 미반영)
- UX-AUTH-03: functional `PASS` / formal trace `PENDING`(evidence branch에 존재, main 미반영)
- UX-AUTH-04: `BLOCKED`(사용자 제공 상태, repo/WBS 자체 근거로는 독립 확인 불가)
- UX-AUTH-05: `BLOCKED`(현재 origin/main 구현 기준으로 session-cookie interactive 사용자에게 해당 rate-limit scenario가 발생하지 않도록 되어 있음을 직접 재확인)
- QA-DATA-AUDIT-03: `증적대기` — known blocker, Issue #16
- P0-REL-01(release/rollback evidence): 빈 템플릿. 이번 검토 범위에서는 실제 server snapshot/restore 및 DB migration rollback drill evidence를 확인하지 못했다.
- **owner review**: `완료`
- **owner review result**: `NOT READY`

**상태 표현: `검토완료 — release package NOT READY, missing/blocked evidence recorded, reviewer pending`**

## 검토 기록

- 이 문서는 **WBS 소유자(P0-REL-REVIEW-01) 리뷰 evidence**다. 소유자가 release artifact·QA gate·rollback 증적을 실제로 교차 검토했고, 그 결론인 `NOT READY`를 있는 그대로 기록한다.
- **`APPROVE`, `release approved`, `final sign-off complete`, 최종 승인을 의미하는 어떤 표현도 이 문서에 기록하지 않는다.** owner가 남길 수 있는 것은 `owner review result = NOT READY`까지이며, QA-REL 최종 sign-off는 WBS가 지정한 별도 승인자의 판단에 맡긴다.
- 실행/증적 작성: WBS 소유자 리뷰 지원 (AI-assisted, Claude Code)
- 검토일: `2026-08-27 (KST)`
