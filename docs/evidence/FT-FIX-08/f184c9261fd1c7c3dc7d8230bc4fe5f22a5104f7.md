# FT-FIX-08 independent QA evidence

## 1. 검증 대상

- **WBS ID**: `FT-FIX-08`
- **Work (원문)**: fixture mode를 명시 배지·추천 차단이 있는 development profile로만 제한한다.
- **Done · QA 계약 (원문)**: release에서 fixture config 불가, dev에서 badge와 limit 표시
  - 이 원문을 세 개의 관찰 가능한 atomic 조건으로 분해한다 (새 요구사항을 추가하지 않음):
    - **Done A** — release profile에서 fixture 사용/설정이 불가능해야 한다.
    - **Done B** — dev profile에서 fixture 데이터에 badge(출처 라벨)가 표시돼야 한다.
    - **Done C** — dev profile에서 fixture 사용에 limit(제한/추천 차단)이 표시돼야 한다.
- **Workspace lock (WBS 원문 그대로)**: `LOCK-AI-CORE-01 · ai/ai_graph/**(llm 제외), backtest_module/**, ai/tests/core/**`
- **WBS 지정 검증 명령**: `cd ai && pytest -q`
- **역할 분리**: Owner와 WBS 지정 독립 승인자가 서로 다른 역할임을 WBS에서 확인. self-APPROVE 아님.
- **canonical subject SHA**: `470d33e496401e1a9ad0f9d1b5470bbe8f049e49` — migration-root(부모 커밋 없음). `is_release_profile`/`FixtureModeForbiddenError`/`fixture_mode_forbidden_in_release`/`BacktestReliability`/`project_public_performance` 모두 이 시점부터 canonical 이력에 존재하며, 이보다 앞선 추적 가능한 도입 커밋은 없다.
- **tested SHA**: `f184c9261fd1c7c3dc7d8230bc4fe5f22a5104f7`
- **tested tree**: `4cf1587ba69c7b35049152675011f133d507510d`
- **branch**: `evidence/qa-batch-20260831`
- **검토일**: 2026-09-01

### WBS as-built 계보와의 관계

WBS as-built workbook은 이 행의 증적 URI를 `5bc339b01a46e6a97bc11c30be5f2e5304d792cf..f61de31cd57b2032279fc410dc3ca73c5230bb71`로 기록한다. `git merge-base --is-ancestor`로 확인한 결과 두 SHA 모두 canonical `origin/main`의 조상이 아니며, `git branch -r --contains`로 확인한 결과 canonical `main` 계보와 공통 조상이 없는 별도 orphan 브랜치 집합에만 존재한다. 이 범위는 canonical subject로 사용하지 않았으며, 6번 항목에 WBS 계보 불일치 observation으로만 기록한다. 이 evidence의 모든 판정은 canonical subject SHA(`470d33e...`)와 tested SHA(`f184c9261...`)에서 독립적으로 재실행·재확인한 결과에만 근거한다.

기존 `docs/evidence/FT-FIX-08/`는 canonical `main`에도 확인 가능한 어떤 브랜치에도 존재하지 않았다 — 이 문서가 최초 evidence다.

## 2. 검증 범위와 방법

- Disposable detached `git worktree` + 전용 `venv`에서 tested SHA를 checkout하여 실행. 원본 checkout(`<repository-root>`)의 working tree, `evidence/qa-batch-20260831` 브랜치, source, test, WBS, 기존 evidence 파일은 모두 무수정 상태로 유지했다.
- 모든 로컬 절대경로는 `<repository-root>`(원본 checkout) 또는 `<temporary-worktree>`(disposable worktree)로 일반화해 기록한다.
- 검증 범위: WBS literal verifier 단독 전체 실행, Done A/B/C 관련 targeted unit test 3개 배치(총 25개 test), 동일 tested SHA의 canonical GitHub Actions 로그 교차 확인, 관련 소스(`ai/ai_graph/data_sources/db.py`, `db_split.py`, `ai/ai_graph/quant_performance.py`, `ai/ai_graph/schemas.py`, `ai/ai_graph/research_eligibility.py`, `ai/ai_graph/research_contract.py`, `fe/src/features/app/PerformanceTab.tsx`, `fe/src/features/app/strategyWorkspaceMapper.ts`) 직접 읽기.
- 범위 제외: 실제 배포된 production 서버, 실제 PostgreSQL, 실제 AOAI/LLM provider는 어느 시점에도 호출하지 않았다. FE 렌더링은 소스 코드 읽기로만 확인했고 브라우저에서 실행하지 않았다. commit/push/PR/Issue/WBS 수정은 수행하지 않았다.

## 3. WBS 지정 검증 명령과 결과

- 명령: `pytest -q`
- working directory: `<temporary-worktree>/ai`
- 실행 조건: dependency 설치가 완전히 끝난 뒤, 다른 검증/설치 프로세스가 전혀 실행되지 않는 상태에서 단독으로 실행했다.
- exit code: `0`
- 결과: `847 passed, 12 skipped, 1 warning in 156.65s (0:02:36)`
- 실패 없음.

## 4. Done 조건별 검증

### 4-1. 최초 targeted 검증 (Done A/B/C 핵심 unit test)

명령 (`<temporary-worktree>` 루트에서 실행):
```
pytest -v \
  ai/tests/test_db_data_source.py::test_release_profile_refuses_to_serve_the_local_fixture_bundle \
  ai/tests/test_db_data_source.py::test_development_still_gets_the_fixture_bundle_with_its_badge \
  ai/tests/test_db_data_source.py::test_the_split_variant_carries_the_same_guard_and_badge \
  ai/tests/test_db_data_source.py::test_the_api_and_the_data_layer_share_one_release_profile_definition \
  ai/tests/test_research_contract.py::test_dev_preview_is_explicitly_limited_to_fixture_rendering
```
- exit code: `0`
- 결과: `5 passed in 1.68s`

이 중 `test_dev_preview_is_explicitly_limited_to_fixture_rendering`은 `ResearchDevPreviewV1` 스키마의 unit test다. 이 스키마는 현재 어떤 live endpoint에도 배선되지 않은 schema-only 후보이므로, 이 test의 PASS만을 Done B/C의 최종 근거로 사용하지 않았다 — 최종 근거는 4-3의 배치다.

### 4-2. 추가 targeted 검증 (배제 대상 후보 확인)

명령:
```
pytest -v \
  ai/tests/test_research_eligibility.py::test_ineligible_reasons_have_fixed_precedence \
  ai/tests/test_research_contract_api.py::test_data_evidence_probe_is_hidden_token_gated_and_has_no_job_side_effects
```
- exit code: `0`
- 결과: `15 passed, 1 warning in 5.73s`

`test_data_evidence_probe_is_hidden_token_gated_and_has_no_job_side_effects`가 검증하는 `not_production_eligible` 경로는 `/_operator/`로 시작하는 hidden operator-only 진단 endpoint이며, 자신의 테스트 이름 자체가 "no job side effects"임을 명시한다. 이 경로는 실제 사용자 추천 흐름의 canonical Done C 메커니즘이 아니므로, 보조 조사 및 후보 배제 근거로만 기록한다(6번, 5-3번 참조).

### 4-3. 최종 Done B/C 계약 확인 targeted 검증

저장소에서 전체 경로와 test ID를 다시 확인한 명령:
```
pytest -v \
  ai/tests/test_quant_performance.py::test_public_projection_removes_every_metric_and_chart_for_insufficient_data \
  ai/tests/test_quant_performance.py::test_public_performance_reliability_boundary_cases \
  ai/tests/test_quant_performance.py::test_public_projection_requires_engine_manifest_before_exposing_complete_values \
  ai/tests/test_api.py::test_backtest_api_redacts_legacy_insufficient_performance_before_serialization \
  ai/tests/test_api.py::test_job_api_redacts_legacy_available_performance_when_source_is_stale
```
- exit code: `0`
- 결과: `5 passed, 1 warning in 8.91s`

## 5. Done별 판정

| Done | 판정 | canonical 근거 |
|---|---|---|
| Done A | `PASS` | `ai/ai_graph/data_sources/db.py`의 `is_release_profile()`/`FixtureModeForbiddenError`. release profile + DSN 없음 조건에서 fixture 사용을 시도하면 `FixtureModeForbiddenError`가 발생하고 `reason == "fixture_mode_forbidden_in_release"`. `ai/ai_graph/data_sources/db_split.py`는 이 정의를 복제하지 않고 `db.py`에서 직접 import해 동일한 release-profile 정의와 guard를 공유한다(`test_the_api_and_the_data_layer_share_one_release_profile_definition`으로 확인). |
| Done B | `PASS` | canonical badge 메커니즘은 `BacktestReliability.source`다. fixture 데이터의 공개 `source` 값은 `"fixture"`이며, `ai/ai_graph/quant_performance.py::project_public_performance()`가 이를 공개 `PerformanceUnavailable.safe_facts.source`로 보존한다. 이 값은 `GET /analysis-jobs/{job_id}` 응답의 `result.user_payload.performance`에 실제로 직렬화된다(`test_backtest_api_redacts_legacy_insufficient_performance_before_serialization`으로 확인). FE `fe/src/features/app/PerformanceTab.tsx`가 reliability badge와 `sourceLabel()`의 `"예시 데이터"` 라벨을 렌더링하도록 구현되어 있으며, `fe/src/features/app/StrategyWorkspace.tsx`의 `/app` 워크스페이스 렌더 트리에 연결되어 있음을 소스 추적으로 확인했다(브라우저 렌더링은 실행하지 않았다). 내부 `PipelineDataBundle.metadata`(`ai/ai_graph/envelope.py`의 `InMemoryDebugStore`에만 저장, 공개 직렬화에서 제외)는 debug-only이므로 badge의 canonical 근거가 아니다. |
| Done C | `PASS` | canonical limit/추천 차단 메커니즘은 `BacktestReliability.status`다. fixture source는 reliability 계산 로직에서 항상 reason이 추가되어 `status == "insufficient"`가 되고, 이 경우 `project_public_performance()`는 `PerformanceAvailable` 대신 `PerformanceUnavailable`을 반환한다 — metrics, equity curve, benchmark 및 개별 성과 수치가 공개 응답 어디에도 나타나지 않는다(`test_public_projection_removes_every_metric_and_chart_for_insufficient_data`가 직렬화된 JSON 전체를 재귀 검사해 확인). FE `PerformanceTab.tsx`가 `"표시할 성과 수치가 없습니다"`와, `reliability.status === "insufficient"`일 때 정확히 "짧거나 좁은 표본의 숫자를 실제 성과처럼 보이지 않도록 숨겼습니다"라는 제한 문구를 렌더링하도록 분기되어 있음을 소스에서 확인했다. `not_production_eligible`은 hidden operator-only 진단 endpoint에서만 사용되며 메인 사용자 추천 흐름의 추천 차단 근거가 아니다. |

## 6. 반증 및 예외 확인

1. WBS as-built에 기재된 `5bc339b01a46e6a97bc11c30be5f2e5304d792cf..f61de31cd57b2032279fc410dc3ca73c5230bb71` 범위는 canonical `main`의 조상이 아닌 orphan lineage다(1번 참조).
2. canonical repo에서는 관련 메커니즘(Done A/B/C 모두)이 migration-root `470d33e496401e1a9ad0f9d1b5470bbe8f049e49`부터 이미 존재한다 — 이보다 앞선 추적 가능한 도입 커밋은 canonical 이력에 없다.
3. Workspace lock이 지정하는 `ai/tests/core/**` 디렉터리는 현재 canonical repo에 존재하지 않는다(`ai/tests/`에는 `contracts/`, `prompt_semantics_output/`만 존재). literal verifier(`cd ai && pytest -q`)는 이 경로 제한과 무관하게 `ai/tests/` 전체를 실행하므로 이번 검증 자체는 이 drift의 영향을 받지 않았다.
4. `ResearchDevPreviewV1`(`ai/ai_graph/research_contract.py`)은 schema-only이며 `ai/ai_graph/api.py`의 어떤 live endpoint 핸들러에서도 생성 지점이 없다.
5. `not_production_eligible` 경로(`ai/ai_graph/research_eligibility.py::evaluate_research_eligibility()`)는 `api.py`에서 오직 hidden, token-gated, no-job-side-effects 진단 endpoint(`/_operator/research-data-evidence`)에서만 호출된다.
6. FE의 과거 `development_fixture_only`/dev-preview 배지 렌더링 코드는 이후 commit에서 제거된 이력이 있지만, 현재 canonical badge/limit 계약은 `BacktestReliability`와 `PerformanceTab.tsx` 경로로 독립적으로 존재하며 공개 API 직렬화 테스트와 FE 소비·렌더링 경로의 소스 추적으로 재확인했다.
7. 동일 tested SHA의 canonical CI("Python checks"/"Offline release-trust gate")는 일부 관련 계약(`test_research_contract.py` 전체, `test_db_data_source.py`의 일부 test ID)을 corroborate하지만, FT-FIX-08 핵심 guard test 전부를 실행하지는 않는다 — 이번 독립 로컬 검증(3, 4번)이 핵심 근거다.
8. 이후 commit에서 추가된 인증 job admission 경로(`request.is_parse_bound`)는 job store readiness 판정에 Done A의 fixture-forbidden gate와 동일한 DSN 환경변수 조건을 요구하므로, Done A를 약화시키지 않는다.
9. FE 소비·렌더링 경로는 소스 추적으로 확인했으며, 이번 QA에서는 브라우저 기반 UI 렌더링을 별도로 실행하지 않았다.

## 7. 재현 절차

1. `<repository-root>`에서 canonical repository를 clone/fetch한다.
2. tested SHA를 detached 상태로 checkout한다: `git worktree add --detach <temporary-worktree> f184c9261fd1c7c3dc7d8230bc4fe5f22a5104f7`.
3. `<temporary-worktree>/ai`에 전용 venv를 만들고 그 안에서 `pip install -e ../backtest_module -e . pytest`를 실행한다.
4. WBS literal verifier: `<temporary-worktree>/ai`에서 `pytest -q` 실행 (DSN/AOAI/LLM key 관련 환경변수는 이름만 명시적으로 unset, 값 기록 없음). 다른 프로세스 없이 단독 실행.
5. Done A/B/C targeted test: `<temporary-worktree>` 루트에서 4번 섹션의 세 명령을 순서대로 실행한다.
6. 검증 완료 후 `<temporary-worktree>`를 제거한다: `git worktree remove <temporary-worktree> --force`.

## 8. 최종 판정

- **technical result**: `PASS`
- **review verdict**: `APPROVE WITH NON-BLOCKING OBSERVATIONS`
- **blocking issue**: 없음
- **non-blocking observations**: 6번 참조
- **approval status**: `승인 완료`
- **승인일**: 2026-09-01
- **WBS 완료 전이 가능 여부**: evidence push 및 immutable permalink 확보 후 가능

## 9. 승인 정보

- QA 실행 및 이 evidence 작성: `WBS 지정 독립 승인자 수행 read-only QA`
- 승인 권한: `WBS 지정 독립 승인자`
- 실제 승인 상태: `승인 완료`
- 승인 기록: `WBS 지정 독립 승인자가 기술 검증 결과와 non-blocking observations를 검토하고 승인 · 2026-09-01`
- Owner와 WBS 지정 독립 승인자가 서로 다른 역할임이 WBS에서 확인되어 self-approval 충돌이 없음.
- 이 문서는 owner·승인자의 실명이나 개인 이메일을 기록하지 않는다.
