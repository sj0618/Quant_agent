# PM-GOAL-00 independent QA evidence (reviewer draft)

- `wbs_id`: `PM-GOAL-00`
- `role`: independent reviewer QA — owner ≠ WBS-designated approver, self-QA 아님
- `qa_tested_sha`: `3cda2774767b95b9e264252d783e34dfa052dae3` (이 QA 세션이 실제로 실행을 재현한 `origin/main` 기준선)
- `implementation/evidence commit under test`: `72da24067c54067989f58d0947271281c41d46c1` — `qa_tested_sha`의 조상임을 `git merge-base --is-ancestor`로 확인
- `windows_cli_guard_fix_commit`: `c8fb6265e6ff9d2c2fb22e2fda6a49cdc7c47245` — 위 두 커밋 모두의 조상, `72da2406`보다 2분 먼저 커밋됨
- `execution_method`: detached git worktree를 `origin/main`에 생성해 실행. 현재 작업 브랜치는 switch/merge/rebase/reset 없이 그대로 유지.
- `application source 수정`: 없음 (read-only 검증만 수행, `LOCK-QA-EVIDENCE-01` 준수)

## WBS 원문

- 작업: 현재 문서 구조를 검사하는 Goal preflight를 만든다.
- Done·QA 계약: 고정 command가 현재 문서에서 기대 FAIL을 내고, 필요한 필드를 채운 revision에서는 exit 0.
- 실행·검증 (WBS 셀 원문): `node .omx/scripts/verify-production-readiness-plan.mjs`
- Workspace lock: `LOCK-QA-EVIDENCE-01` · WBS planning evidence · read-only review (application source 수정 금지)
- 의도된 커밋: `[TEST] add planning-goal preflight`

## WBS literal verifier 부재 확인

`qa_tested_sha`에서 WBS 셀에 적힌 명령을 문자 그대로 실행했다:

```
$ node .omx/scripts/verify-production-readiness-plan.mjs
Error: Cannot find module '.../.omx/scripts/verify-production-readiness-plan.mjs'
exit=1 (MODULE_NOT_FOUND)
```

canonical repo 최상위에 `.omx/` 디렉터리가 존재하지 않는다(`fe/.omx/`는 프론트엔드 빌드 산출물로 무관). 이는 WBS 셀이 현재 저장소 레이아웃과 맞지 않는 경로를 그대로 들고 있다는 사실의 기록이며, 이 실행 실패 자체를 Done 계약의 FAIL 판정으로 사용하지 않는다. Done 계약이 요구하는 실제 preflight는 아래에서 독립적으로 확인했다.

## 실제 살아있는 preflight — 독립 재현 (`qa_tested_sha` 기준)

```
$ node scripts/check-production-plan.mjs --board docs/plans/quantagent-production-control-board.md
exit=0
stdout: {"blockerCount":2,"gitSha":"aa318206aa684389a4c5a11255cb0658f70cbcc7","result":"PASS","transitionCount":2}
```

동일 커맨드 재실행 시 결과 동일(결정적). board의 `snapshot.gitSha`(`aa318206...`)가 `qa_tested_sha`의 조상임을 별도로 확인.

**FAIL 경로 — `<repo 외 임시 경로>`에 board 사본을 만들어 mutate 후 실제 CLI 프로세스로 검증** (원본 `docs/plans/quantagent-production-control-board.md`는 수정하지 않음):

```
$ node scripts/check-production-plan.mjs --board <repo 외 임시 경로>/board_zero_sha.md
stderr: [production-plan] snapshot.gitSha must be a non-zero 40-character lowercase Git SHA
exit=1

$ node scripts/check-production-plan.mjs --board <repo 외 임시 경로>/board_no_recurrence.md
stderr: [production-plan] BL-PLAN-001.recurrenceCount must be a non-negative integer
exit=1
```

두 경우 모두 board mutation 이후 원본 파일에 대한 sanity 재실행에서 다시 exit 0/PASS 확인.

## 계약 회귀 스위트

```
$ node --test scripts/check-production-plan.test.mjs
tests 5, pass 5, fail 0
exit=0
```

## Windows URL guard 수정 확인 (소스 직접 확인, `qa_tested_sha` 기준)

`scripts/check-production-plan.mjs:159`:

```js
if (process.argv[1] && pathToFileURL(process.argv[1]).href === import.meta.url) {
```

`pathToFileURL(...)` 방식으로 되어 있음을 현재 main에서 직접 확인. (구 형태 `new URL(process.argv[1], "file:")`는 Windows 드라이브 경로의 `C:`를 URL scheme으로 오인해 CLI 진입 자체가 조용히 exit 0이 되는 결함이 있었다.) 이 수정이 정상 동작함은 위 "실제 살아있는 preflight" 절의 PASS/FAIL 재현 결과 자체가 실증한다 — 만약 가드가 여전히 깨져 있었다면 mutation을 넣은 board에서도 exit 0/무출력이 나왔어야 한다.

## 과거 `NEEDS_FIX` 판정과의 관계

WBS 셀의 `증적URI`에 남아있는 "issue · NEEDS_FIX · Windows CLI silent exit 0"는 이전 저장소에서 이 저장소로의 migration 직후 생성된 orphan root 커밋(그 커밋 메시지가 "Initial snapshot migrated from ..."으로 시작함) 기준의 판정이다. 그 root와 implementation/evidence commit(`72da2406`) 사이에 Windows guard 수정 커밋(`c8fb626`)을 포함해 4개 커밋이 존재함을 `git log`로 확인했다. 즉 `NEEDS_FIX`는 수정 이전 상태에 대한 기록이며, 현재 `qa_tested_sha`에는 적용되지 않는다. 이 verdict는 과거 기록을 그대로 재사용하지 않고 `qa_tested_sha`에서의 직접 재실행 결과만을 근거로 한다.

## 반증/미검증 범위

- 이 preflight는 문서 구조만 검사한다. 운영 배포, 실데이터, 사람 승인 여부는 이 명령으로 증명되지 않으며, Done 계약 자체가 이를 요구하지 않는다.
- POSIX 환경에서는 재실행하지 않았다(결함이 Windows 전용이었으므로 우선순위가 낮다고 판단).

## Reviewer verdict

- **verdict: `APPROVE`**
- 근거: owner ≠ WBS-designated approver, self-QA 아님. WBS 원문 verifier의 경로 부재를 숨기지 않고 명시했다. 실제 살아있는 preflight를 `qa_tested_sha`에서 PASS/FAIL 양방향으로 독립 재현했고, 회귀 스위트 5/5 PASS를 재확인했으며, Windows guard 수정을 소스 레벨과 동작 레벨 양쪽에서 확인했다. 기존 developer evidence(`72da2406`)와 모순되는 지점을 발견하지 못했다.
- Reviewer: WBS-designated approver (independent reviewer, self-approval 아님)
- Reviewed at: 2026-08-28
