# P0-BE-02 QA Evidence

## 검증 대상

- WBS ID: `P0-BE-02`
- 단계: release gate
- 작업 원문: `readiness가 durable store, migration revision, AI contract version을 검사하게 한다.`
- Done · QA 계약: `의존성 하나라도 누락되면 readiness non-200`
- Owner: WBS 원문 지정(개인 식별정보는 이 문서에 남기지 않음)
- 승인자: WBS 원문 지정(개인 식별정보는 이 문서에 남기지 않음)
- repo: `Quant_agent`(canonical, public)
- tested_sha: `9de51544d377cb3f94372a43d5e9763be21342f7`(`origin/main` HEAD, 검증 시점)
- WBS 지정 실행·검증(원문): `cd backend && pytest -q tests -k readiness`

## 검증 환경 및 범위

- OS: Windows
- interpreter: 시스템 Python 3.12.5 (`ai/` 하위에 별도 repo-local venv가 없어 시스템 인터프리터로 실행)
- 실행 기준: 로컬 clone의 working tree, `tested_sha`와 일치(clean, uncommitted change 없음)
- `AI_DATABASE_DSN`, `QUANT_DB_DSN`, `DATABASE_URL`, `AI_AOAI_API_KEY`, `AI_AOAI_ENDPOINT`, `AI_AOAI_DEPLOYMENT`, `AI_LLM_API_KEY`, `OPENAI_API_KEY` 환경변수를 `env -u`로 제거한 상태에서 실행(값은 기록하지 않음, 변수명만)
- 실 DB·Redis·AOAI·배포 서버 접근 없음(오프라인 pytest, `TestClient` in-process 호출)
- 사전 준비: 로컬 실행 환경에 `psycopg[binary]`가 없어 `ai/tests/test_api.py` import 자체가 실패했다. 이 의존성은 이미 `ai/pyproject.toml`에 선언돼 있었으며, 로컬 환경에 설치한 뒤 재실행했다. 이는 저장소 파일 변경이 아니라 로컬 실행환경 보완이다.

## WBS 지정 verifier 불일치

WBS가 지정한 `cd backend && pytest -q tests -k readiness`는 문법적으로는 실행 가능하지만, 실제로는 `backend/app/api/routes/readiness.py`의 readiness 라우트(`auth_runtime`/`main_db`/`trading_data_db`/`redis`)만 검사한다. 이 라우트는 P0-BE-02의 Done · QA 계약이 요구하는 durable store / migration revision / AI contract version 중 어느 것도 검사하지 않는다.

P0-BE-02가 실제로 의도하는 구현·테스트 대상은 다음이다:

- 구현: `ai/ai_graph/api.py`(AI 서비스 readiness 게이트)
- 관련 migration: `service_db/migrations/021_ai_analysis_jobs.sql`
- 테스트: `ai/tests/test_api.py`

따라서 이번 QA는 WBS 지정 명령을 그대로 실행하지 않았고, 위 실제 구현·테스트 대상을 직접 실행했다. 이 차이는 구현 결함이 아니라 **WBS 실행검증 필드(및 필요 시 Workspace lock 범위)가 실제 구현 위치를 반영하지 못한 non-blocking documentation discrepancy**로 기록한다. 향후 WBS의 실행검증 필드는 `ai/tests/test_api.py`의 관련 테스트를 가리키도록, Workspace lock은 `backend/**`뿐 아니라 `ai/**` 관련 경로를 포함하도록 보완이 필요하다.

## 실제 QA 실행

```text
cd ai && env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL -u AI_AOAI_API_KEY -u AI_AOAI_ENDPOINT -u AI_AOAI_DEPLOYMENT -u AI_LLM_API_KEY -u OPENAI_API_KEY python -m pytest -v \
  "tests/test_api.py::test_release_readiness_requires_durable_job_store_before_other_dependencies" \
  "tests/test_api.py::test_release_readiness_rejects_missing_migration_and_contract_drift" \
  "tests/test_api.py::test_release_readiness_requires_a_rule_draft_signer"
```

- exit code: `0`
- 결과: `3 passed, 0 failed`

세 테스트만 최소 범위로 직접 선택 실행했다(`test_api.py` 전체나 무관한 테스트는 실행하지 않음). 앞의 두 테스트는 Done · QA 계약이 명시한 3개 핵심 하위 조건(durable job store, migration revision, AI contract version)을 실제로 단언(assert)하는 테스트로 코드에서 직접 식별했다. 세 번째 테스트(`rule-draft signer`)는 Done 원문에 명시된 항목은 아니지만, 같은 readiness 게이트 안에서 함께 확인되는 인접 의존성이라 함께 실행했다.

## Done 항목별 결과

WBS Done · QA 계약이 명시하는 핵심 하위 조건 3개:

| Done 핵심 조건 | 검증 방법 | 실제 근거 | 판정 |
|---|---|---|---|
| durable job store | `test_release_readiness_requires_durable_job_store_before_other_dependencies` | durable store 미충족 시 503 + `durable_job_store_required`, 그리고 migration 프로브가 호출조차 되지 않음(단락 회로)을 직접 단언 | `PASS` |
| migration revision | `test_release_readiness_rejects_missing_migration_and_contract_drift`(전반부) | migration probe가 `False`를 반환하면 503 + `migration_revision_required`를 직접 단언 | `PASS` |
| AI contract version | `test_release_readiness_rejects_missing_migration_and_contract_drift`(후반부) | 계약 버전을 drift시키면(`ai-mvp.v1` → 불일치 값) 503 + `ai_contract_version_mismatch`를 직접 단언 | `PASS` |

같은 readiness 게이트에서 함께 확인한 추가/인접 dependency(Done 원문의 하위 조건은 아님):

| 추가 확인 항목 | 검증 방법 | 실제 근거 | 판정 |
|---|---|---|---|
| rule-draft signer | `test_release_readiness_requires_a_rule_draft_signer` | signer 미설정 시 503 + `rule_draft_signer_required`를 직접 단언 | `PASS` |

## 반증 및 예외 확인

- 3개 테스트 모두 실패·skip 없음, 완화(relax)나 우회 없음.
- 이 3개가 durable store / migration revision / AI contract version(핵심 3조건) 및 rule-draft signer(인접 항목)를 각각 직접 단언하는 테스트임을 코드 레벨에서 확인했다.
- 같은 readiness 게이트의 또 다른 인접 축인 `live_provider_configuration`(`test_release_readiness_requires_live_aoai_configuration`)은 Done · QA 계약 원문에도, 이번 검증 범위에도 포함되지 않아 실행에서 제외했다.
- 관련 migration 파일 `service_db/migrations/021_ai_analysis_jobs.sql`이 tested_sha 기준 저장소에 존재함을 확인했다(파일 존재 확인만, 실제 DB 적용·probe는 수행하지 않음).
- application source·test·fixture는 이 QA 과정에서 전혀 수정하지 않았다.
- 실제 배포·운영 환경에서의 probe(O-grade evidence)와 CI(GitHub Actions) 상 재현(R-grade evidence)은 이번 evidence에서는 수행하지 않았으며, 해당 범위를 증명하지 않는다.

## 최종 판정

- QA verdict: `PASS`(코드/로컬 독립 QA 기준, source-level)
- Done 충족: `PASS` — 핵심 하위 조건 3개(durable job store / migration revision / AI contract version) 전부 대응 테스트로 커버, 전부 통과. 인접 항목(rule-draft signer)도 함께 통과.
- blocking issue: 없음
- non-blocking observation:
  1. WBS 지정 실행검증(`cd backend && pytest -q tests -k readiness`)이 P0-BE-02의 실제 구현 위치(`ai/ai_graph/api.py`, `ai/tests/test_api.py`)와 일치하지 않음 — non-blocking documentation discrepancy이며 이번 QA 결과의 유효성에는 영향 없음. WBS 실행검증 필드 및 Workspace lock 범위 보완 권장.
  2. 운영환경 probe(O-grade)와 CI 재현(R-grade)은 이번 evidence에서 수행하지 않았으며 해당 범위를 증명하지 않는다.
- 최종 승인(APPROVE) 여부: 이 문서는 수행한 QA 실행 결과만 기록한다. 승인자의 최종 승인 여부는 이 evidence와 별개로 결정한다.

## 검토 기록

- QA 실행/증적 작성: AI-assisted QA (Claude Code)
- WBS 소유자: WBS 원문 지정(실명 생략)
- WBS 승인자: WBS 원문 지정(실명 생략)
- self-QA 아님(WBS 소유자와 독립 검토자/승인자 역할이 분리됨), 기존 독립 QA/APPROVE 증적 없음(신규 QA)
- 검토일: `2026-08-27 (KST)`
