# `P1-CI-01` independent QA evidence

- `wbs_id`: `P1-CI-01`
- `role`: independent QA reviewer — WBS-designated approver이며 owner와 다른 사람. self-QA 아님.
- `tested_sha`: `e69f307d9d9c095e22bbc697c1317e171fdd4c9b` (PR #9 head)
- `latest_canonical_main_at_qa_time`: `acf0e54571224fb80d302ad7243afbfd043881ab` (`origin/main`)
- `execution_method`: 현재 evidence branch·working tree를 건드리지 않고, tested SHA를 대상으로 한 격리된 detached worktree에서 검증을 수행한 뒤 worktree를 제거했다. 이후 evidence branch/HEAD/working tree가 변경되지 않았음을 재확인했다.

## 관련 PR

- PR #9, `state: OPEN`, `head SHA`: tested SHA와 동일
- `mergeable: CONFLICTING`, `mergeStateStatus: DIRTY`
- tested SHA는 `origin/main`의 조상(ancestor)이 아니다 — 아직 병합되지 않았다.

## WBS 원문

- 작업: `deploy가 release smoke·rollback drill·artifact provenance를 통과할 때만 진행하게 한다.`
- Done·QA 계약: `gate failure가 deploy를 차단`
- 실행·검증(literal verifier): `node scripts/evaluate-release-trust.mjs`
- 기존 WBS evidence note: `same-SHA deploy/rollback R/O pending` — 이는 observation으로만 기록한다. `QA-WBS-01`의 S/R/O/C 체계는 아직 DRAFT/PENDING이므로 이를 새로운 필수 완료조건으로 승격하지 않는다.

## literal verifier 실행 결과 — 로컬(Windows, 격리 worktree)

```
$ node scripts/evaluate-release-trust.mjs
[release-trust] ai-api-and-research-contracts   → 131 passed
[release-trust] backtest-metric-contracts       → 51 passed
[release-trust] release-failure-mode-contracts  → 26 passed
[release-trust] backend-auth-report-and-deploy-contracts → 162 passed
[release-trust] frontend-production-build-and-contracts
[release-trust] failed: frontend-production-build-and-contracts (exit 127)
```

- Python 4개 게이트(131/51/26/162)는 전부 PASS했다.
- FE 단계에서 evaluator 자체가 **exit 127**로 실패했다. 원인은 `spawnSync("npm", ["test"], {shell:false})`가 Windows에서 확장자 없는 `npm`을 직접 실행하지 못하는 **evaluator 스크립트의 Windows 실행 한계**이며, PR #9의 코드 결함이 아니다.
- 이 단계만 evaluator를 우회해 `cd fe && npm test`를 직접 실행해 재확인: **66 tests / 60 pass / 6 fail**. 실패 6개는 이전 조사에서 이미 특정한 기존 Windows path-handling harness issue(`new URL(...).pathname`)와 동일 원인이며, PR #9는 `fe/` 파일을 전혀 수정하지 않는다.

### 동일 tested SHA — canonical Linux CI

- Python 게이트: **131/51/26/162 전부 PASS**(로컬과 동일 숫자)
- FE: **66/66 PASS**
- typecheck: PASS
- build: PASS
- bundle-check: PASS
- evaluator 전체: `[release-trust] all offline gates passed`(exit 0)

로컬 Windows 실패(evaluator ENOENT, FE 6개)는 실제 PR 기능 결함이 아니라 Windows 환경에 한정된 harness observation으로 기록한다.

## Done 계약 항목별 판정

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | gate failure blocks deploy | **PASS** | tested SHA `deploy.yml`을 직접 확인 — `deploy:` job에 `needs: release-trust`가 명시돼 있어, release-trust job이 실패하면 GitHub Actions 기본 동작상 deploy job이 실행되지 않는다. `origin/main`도 동일 구조를 유지한다. |
| 2 | release smoke | **PARTIAL** | **tested SHA 자체 기준**으로 판정한다: tested SHA `deploy.yml`에는 post-deploy URL/상태 확인(`wait_for_url` combined backend, `curl .../ai-api/api-status` 응답 확인)이 **존재**한다. 그러나 backend/AI API/DB 등 서비스 구성요소별 readiness를 구조적으로 검증하는 semantic assertion(각 구성요소가 개별적으로 ready 상태인지 판정하는 로직)은 tested SHA에 **부재**하다. 존재와 부재가 함께 확인되므로 WBS의 `release smoke`는 **부분 충족(PARTIAL)**으로 판정한다. `origin/main`이 이후 추가한 general backend health/AI API health/상세 readiness assertion(`auth_runtime`/`main_db`/`trading_data_db`/`redis` 개별 체크)은 이 판정을 뒷받침하는 비교·observation 근거로만 남긴다 — main에 있다는 사실 자체가 tested SHA의 판정을 바꾸지 않는다. |
| 3 | rollback drill | **FAIL** | tested SHA `deploy.yml`(459줄) 전체에서 `rollback`/`snapshot` 문자열이 **0건** — tested SHA deploy.yml에 rollback/snapshot 메커니즘이 부재하다. canonical main에는 이후 `rollback_deploy_release`/`restore_deploy_snapshot` 함수와 "Restore deploy snapshot on failure" job step, `scripts/rollback-snapshot.mjs`(+test)가 이미 구현돼 존재한다. |
| 4 | artifact provenance | **PASS** | PR #9의 실제 고유 기여 — `ai/ai_graph/api.py`에 정규식 검증(`^[0-9a-f]{40,64}$`)된 공개용 `deployment_revision` 필드를 `APIStatusResponse`(공개 `/api-status`)에 신규 추가했고, `deploy.yml`에 배포 직후 실제 배포된 revision이 기대값과 일치하는지 assert하는 단계를 추가했으며, `ai/tests/test_api.py`의 형식 검증 테스트가 위 131 PASS에 포함되어 실행·통과했다. `origin/main`에는 이 공개 provenance 필드가 아직 없다(main은 별도 operator-secret 게이트 경로로만 deployment_revision을 노출). |

## main 대비 PR #9 drift

- PR #9 자체 diff(base `470d33e` → tip)는 **6개 파일**(`.github/workflows/deploy.yml` +5/-0, `ai/ai_graph/api.py` +12/-0, `ai/tests/test_api.py` +12/-0, `backend/tests/unit/test_deploy_workflow_contract.py` +2/-0, `scripts/evaluate-release-trust.mjs` +1/-0, `scripts/evaluate-release-trust.test.mjs` +5/-0)이며, 전부 artifact provenance 기능 하나를 구현하는 좁고 유효한 변경이다.
- 그러나 최신 `origin/main`에는 PR #9 branch에 없는 다음 안전장치가 이미 존재한다: same-SHA S/R/O/C evidence gate(`workflow_dispatch` 필수 입력 8종 + "Verify same-SHA S/R/O/C release evidence" 단계), pre-deploy readiness gate(`readiness-semantic-gate.mjs`), snapshot creation("Create deploy snapshot archive" 단계), automatic rollback/restore(`rollback_deploy_release`/`restore_deploy_snapshot` + "Restore deploy snapshot on failure" job step), `workflow_dispatch` evidence inputs.
- **PR #9의 base가 오래되어 최신 main의 안전장치를 포함하지 않는다** — PR #9가 이 안전장치를 직접 삭제한 것이 아니다. PR #9 branch는 migration root(`470d33e`)를 base로 생성된 이후 갱신되지 않았고, 이 안전장치들은 그 이후 main에서 별도 작업으로 구축됐다.

## merge simulation

`git merge-tree --write-tree`(read-only) 결과, 실제 conflict가 **4건** 확인됐다:

- `.github/workflows/deploy.yml`
- `ai/ai_graph/api.py`
- `backend/tests/unit/test_deploy_workflow_contract.py`
- `scripts/evaluate-release-trust.test.mjs`

(`ai/tests/test_api.py`, `scripts/evaluate-release-trust.mjs`는 충돌 없이 auto-merge됨.)

현재 PR은 텍스트 충돌 4건으로 인해 **그대로 자동 병합이 불가능**하다. **충돌을 PR #9의 오래된 workflow context 쪽으로 잘못 해소할 경우, 최신 main의 수동·증적 기반 배포 트리거와 rollback 보호가 후퇴할 수 있다.** PR #9을 병합하면 자동으로 후퇴한다고 단정하지 않는다 — 이는 conflict 해소 방식에 달린 조건부 위험이며, observation이 아니라 verdict 근거로 기록한다.

## 유효한 PR 고유 기여

다음은 PR #9가 만든, 폐기 대상이 아닌 유효한 개선이다:

- deployment revision provenance(공개 `/api-status`에 검증된 revision 노출)
- API status provenance validation(`ai/tests/test_api.py` 신규 테스트)
- `AI_DATA_SOURCE_VARIANT="db"` gate 강화(offline release-trust gate가 DB 경로도 커버)

권고 조치: **현재 `origin/main` 위에 이 좁은 diff만 재적용/재작성한 뒤 재QA**해야 한다.

## S/R/O/C 참고 상태 (별도 축, 최종 verdict의 필수 규칙 아님)

- **S**: PASS 근거 충분 — Python 4게이트(131+51+26+162) + FE 66/66(Linux CI 기준) + typecheck/build/bundle 전부 PASS.
- **R/O**: 기존 WBS note 기준 `pending`.
- **C**: 미평가/해당 없음.

이 상태는 `QuantQA:v1`의 DRAFT classification이며, 최종 verdict의 필수 규칙으로 사용하지 않았다.

## Blocker

- QA 실행 자체를 막는 blocker는 없음.
- **승인 blocker**: rollback drill FAIL + stale/conflicting PR integration state.

## Reviewer

- 이 문서는 independent QA reviewer evidence다.
- owner ≠ approver, self-QA 아님.

## 최종 판정

**`NOT APPROVE`**

판정 핵심:
1. WBS 하위 계약 중 rollback drill이 tested SHA에서 FAIL이다.
2. PR #9는 최신 main과 4개 파일에서 실제 텍스트 충돌이 있다.
3. 최신 main에는 PR #9 branch보다 강한 release safeguards(same-SHA S/R/O/C evidence gate, pre-deploy readiness gate, snapshot creation, automatic rollback/restore)가 이미 존재한다.
4. 충돌을 PR #9의 오래된 workflow context 쪽으로 잘못 해소할 경우 최신 main의 수동·증적 기반 배포 트리거와 rollback 보호가 후퇴할 수 있다 — PR #9을 병합하면 자동으로 후퇴한다고 단정하지 않는다.
5. provenance 관련 PR #9 고유 개선은 유효하므로, 최신 main 위에 재적용한 뒤 재QA가 필요하다.
