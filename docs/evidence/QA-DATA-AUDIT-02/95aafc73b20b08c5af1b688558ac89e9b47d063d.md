# QA-DATA-AUDIT-02 증적 묶음

## 검증 대상

- WBS ID: `QA-DATA-AUDIT-02`
- 단계: QA gate
- 작업 원문: `freshness 한계와 stale 표시가 API·리포트에 일관되게 전파되는지 감사한다.`
- Done · QA 계약: `stale/as-of/reason 및 no-recommendation 증적`
- Owner: (WBS 원문 참조)
- 승인자: 조은채
- WBS 지정 실행·검증: `env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL ./ai/.venv/bin/python -m pytest -q ai/tests/test_research_eligibility.py ai/tests/test_graph_risk_report.py`
- tested_sha: `95aafc73b20b08c5af1b688558ac89e9b47d063d`(fetch 시점의 `Quant_agent/main` HEAD)

## 검증 환경 및 범위

- OS: Windows
- interpreter: repository-local venv Python 3.12.5
- 실행 기준: disposable git worktree(`--detach`)에서 tested_sha checkout
- `AI_DATABASE_DSN`, `QUANT_DB_DSN`, `DATABASE_URL` 환경변수를 `env -u`로 제거한 상태에서 실행(WBS 지정 그대로)
- 실 DB·외부 provider 접근 없음(오프라인 pytest)

## WBS 지정 명령 실행

```text
env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL <venv-python> -m pytest -q ai/tests/test_research_eligibility.py ai/tests/test_graph_risk_report.py
```

- exit code: `0`
- 결과: `41 passed, 0 failed`

Done과 직접 관련된 대표 테스트(파라미터화 포함, 전체 41건 중 발췌):

```
ai/tests/test_research_eligibility.py::test_ineligible_reasons_have_fixed_precedence[overrides4-missing_as_of]
ai/tests/test_research_eligibility.py::test_ineligible_reasons_have_fixed_precedence[overrides6-freshness_not_current]
ai/tests/test_research_eligibility.py::test_ineligible_reasons_have_fixed_precedence[overrides7-freshness_not_current]
ai/tests/test_graph_risk_report.py::test_recommendation_gate_reasons_follow_objective_floor
ai/tests/test_graph_risk_report.py::test_recommendation_gate_accepts_valid_objective
ai/tests/test_graph_risk_report.py::test_recommendation_gate_withholds_when_inputs_miss_the_minimum_data_rule
```

## Done 항목별 결과

| Done 항목 | 검증 방법 | 실제 근거 | 판정 |
|---|---|---|---|
| stale 표시 | `test_ineligible_reasons_have_fixed_precedence[...freshness_not_current]`(2개 파라미터 케이스) | freshness가 최신이 아닐 때 고정된 reason code로 ineligible 처리됨을 직접 단언 | `PASS` |
| as-of 전파 | `test_ineligible_reasons_have_fixed_precedence[...missing_as_of]` | `as_of` 누락 시 별도 reason code로 구분되어 ineligible 처리됨을 직접 단언 | `PASS` |
| reason 일관성 | `test_ineligible_reasons_have_fixed_precedence` 전체 14개 파라미터 케이스 | 사유별 고정 우선순위·reason code가 일관되게 반환됨을 파라미터화 테스트로 확인 | `PASS` |
| no-recommendation | `test_recommendation_gate_reasons_follow_objective_floor`, `test_recommendation_gate_withholds_when_inputs_miss_the_minimum_data_rule` | 최소 데이터 기준 미달 시 추천이 보류(withhold)됨을 직접 단언 | `PASS` |

## 반증 및 예외 확인

- 실패 없음, 우회·완화 없음.
- 41건 전부 이 두 파일에서 수집·실행됐으며, freshness/stale/as-of/no-recommendation 각 축에 대응하는 테스트가 실제로 존재하고 통과함을 개별 확인했다.
- 동일 SHA의 CI(GitHub Actions) 실행 로그는 이번 검증에서 조회하지 않았다.
- application source·test·fixture는 이 QA 과정에서 전혀 수정하지 않았다.

## 최종 판정

- QA verdict: `APPROVE`
- Done 충족: `PASS`
- blocking issue: 없음
- non-blocking observation: 없음
- 새 QA 필요 여부(당시 tested_sha 기준): 아니오
- 코드 수정 필요 여부: 아니오

## 검토 기록

- QA 실행/증적 수집: AI-assisted QA (Claude Code)
- 승인자: 조은채
- 최종 판정: `APPROVE`
- 검토일: `2026-08-27 (KST)`
