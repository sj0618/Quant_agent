# PM-GOAL-01 independent QA evidence (reviewer draft)

- `wbs_id`: `PM-GOAL-01`
- `role`: independent reviewer QA — owner ≠ WBS-designated approver, self-QA 아님
- `qa_tested_sha`: `3cda2774767b95b9e264252d783e34dfa052dae3` (이 QA 세션이 실제로 실행을 재현한 `origin/main` 기준선)
- `implementation/evidence commit under test`: `72da24067c54067989f58d0947271281c41d46c1` — `qa_tested_sha`의 조상임을 `git merge-base --is-ancestor`로 확인
- `windows_cli_guard_fix_commit`: `c8fb6265e6ff9d2c2fb22e2fda6a49cdc7c47245` — 위 두 커밋 모두의 조상
- `execution_method`: detached git worktree를 `origin/main`에 생성해 실행(2회 세션, 동일 `qa_tested_sha`). 현재 작업 브랜치는 switch/merge/rebase/reset 없이 그대로 유지.
- `application/docs source 수정`: 없음 (read-only 검증만 수행, `LOCK-QA-EVIDENCE-01` 준수)

## WBS 원문

- 작업: Goal preflight의 입력·기대 PASS/FAIL을 고정한다.
- Done·QA 계약: `scripts/check-production-plan.*`의 exact command와 exit 0/비0 예시를 문서화.
- 실행·검증 (WBS 셀 원문): `node .omx/scripts/verify-production-readiness-plan.mjs`
- Workspace lock: `LOCK-QA-EVIDENCE-01` · WBS planning evidence · read-only review (application source 수정 금지)
- 의도된 커밋: `[DOCS] freeze planning-goal preflight contract`

## WBS literal verifier 부재 확인

PM-GOAL-00과 동일한 `qa_tested_sha`에서 동일하게 확인: WBS 셀의 `node .omx/scripts/verify-production-readiness-plan.mjs`는 canonical repo에 존재하지 않는 경로다(`.omx/`가 최상위에 없음). 이 사실을 숨기지 않고 기록하되, 이 부재 자체를 Done 미충족으로 판정하지 않는다. Done 계약이 요구하는 대상은 "문서화"이며, 그 문서(`docs/plans/quantagent-preflight-command-contract-20260825.md`)가 명시하는 살아있는 명령을 기준으로 검증했다.

## 검증 대상 계약 문서

`docs/plans/quantagent-preflight-command-contract-20260825.md` (`qa_tested_sha`에서 존재 확인, 105줄) — `node scripts/check-production-plan.mjs [--board <path>]`와 `node --test scripts/check-production-plan.test.mjs`의 exact command, exit 0/비0 7개 예시, 이식성 조건 3가지를 기록.

## 문서화된 7개 예시 — 현재 main에서 독립 재현

| # | 조건 | 검증 방식 | 실행 결과 | 문서 기재값 | 일치 |
|---|---|---|---|---|---|
| 1 | 정상 board | 실제 CLI 재실행 | exit 0, PASS JSON | exit 0 | 예 |
| 2 | `recurrenceCount` 필드 누락 | 실제 CLI 재실행 (`<repo 외 임시 경로>` mutation) | exit 1, `BL-PLAN-001.recurrenceCount must be a non-negative integer` | exit 1 | 예 |
| 3 | `snapshot.gitSha` = 0×40 | 실제 CLI 재실행 (`<repo 외 임시 경로>` mutation) | exit 1, `snapshot.gitSha must be a non-zero 40-character lowercase Git SHA` | exit 1 | 예 |
| 4 | `control-board:v1` 마커 누락 | 회귀 스위트가 실제 CLI를 spawn(자식 프로세스)해서 검증, 재실행하여 통과 확인 | exit 1, `control-board:v1 JSON marker is missing` | exit 1 | 예 |
| 5 | `transitions[].to`==`complete` (localOnly 위반) | 실제 CLI 재실행 (`<repo 외 임시 경로>` mutation) — 이전 세션까지 미검증이었던 항목, 이번에 실제 프로세스로 재현 | exit 1, `... cannot transition to complete in a local-only snapshot` | exit 1 | 예 |
| 6 | `transitions[].reviewer`==`owner` | 실제 CLI 재실행 (`<repo 외 임시 경로>` mutation) — 이번에 처음 재현 | exit 1, `....reviewer must be independent from owner` | exit 1 | 예 |
| 7 | 잘못된 CLI 인자(usage) | 실제 CLI 재실행 3가지 변형(`--board` 값 누락 / stray arg 1개 / 인자 3개) — 이번에 처음 재현 | 3건 모두 exit 1, `usage: node scripts/check-production-plan.mjs [--board <path>]` | exit 1 | 예 |

**mutation 방식(#2, #3, #5, #6)**: 원본 board를 `<repo 외 임시 경로>`에 복사한 뒤 그 사본만 수정, `--board <사본 경로>`로 실제 CLI 프로세스를 spawn해 검증했다. 원본 `docs/plans/quantagent-production-control-board.md`는 어떤 시점에도 수정하지 않았고, mutation 이후 원본에 대한 sanity 재실행에서 다시 exit 0 / 동일 PASS JSON을 확인했다(`git status`로 원본 파일 무변경 확인).

## 계약 회귀 스위트

```
$ node --test scripts/check-production-plan.test.mjs
tests 5, pass 5, fail 0
exit=0
```

## 이식성 조건 3가지 (소스 직접 확인)

1. CLI 진입 가드가 `pathToFileURL(process.argv[1]).href === import.meta.url` 형태임을 `scripts/check-production-plan.mjs`에서 확인.
2. 계약 테스트의 fixture SHA가 상수 고정이 아니라 `git rev-parse HEAD`로 실행 시점에 해석됨을 `scripts/check-production-plan.test.mjs`에서 확인.
3. 정상 board의 `snapshot.gitSha`가 `qa_tested_sha`의 조상임을 별도로 확인했다. (zero SHA 케이스(#3)는 `snapshot.gitSha`의 형식 validation — 비-zero 40자 SHA 여부 — 을 검증한 것이며, 이 케이스 자체가 reachability 제약의 실증은 아니다.)

## 반증/미검증 범위

- 이 명령들은 문서 구조의 PASS/FAIL만 검사한다. 운영 배포·실데이터·사람 승인 여부는 증명하지 않으며, PM-GOAL-01의 Done 계약(문서화) 자체가 이를 요구하지 않는다.
- POSIX 환경 재실행은 하지 않았다(결함이 Windows 전용이었음).
- 문서의 `usage:` 행 문구는 소스의 `parseArgs` 구현과 이번 실제 실행 결과가 문자 그대로 일치함을 확인했다.

## Reviewer verdict

- **verdict: `APPROVE`**
- 근거: owner ≠ WBS-designated approver, self-QA 아님. 계약 문서가 기재한 7개 exit 0/non-zero 예시 전부를 `qa_tested_sha`에서 실제 CLI 프로세스 실행으로 독립 재현했고, 전부 문서와 일치했다. 잔여 3개(항목 5·6·7)는 이번 세션에서 `<repo 외 임시 경로>`의 mutation과 실제 프로세스 spawn으로 새로 확인했으며 원본 파일은 손상되지 않았다. 미스매치 없음.
- Reviewer: WBS-designated approver (independent reviewer, self-approval 아님)
- Reviewed at: 2026-08-28
