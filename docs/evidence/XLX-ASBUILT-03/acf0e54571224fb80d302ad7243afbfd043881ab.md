# XLX-ASBUILT-03 owner evidence (draft)

- `wbs_id`: `XLX-ASBUILT-03`
- `role`: owner execution — owner ≠ WBS-designated approver, self-APPROVE 아님. 이 문서는 reviewer가 나중에 검토할 owner evidence이며, 이 문서 자체가 최종 승인을 의미하지 않는다.
- `tested_baseline_sha`: `acf0e54571224fb80d302ad7243afbfd043881ab` (`origin/main`, 이 owner 작업이 literal verifier 부재를 재확인한 기준선)
- `WBS export snapshot`: WBS의 source of truth는 온라인 스프레드시트이며, 이번 작업의 기준은 그 온라인 스프레드시트를 2026-08-27 23:55에 다운로드한 export snapshot이다. 온라인 스프레드시트 자체의 실시간 최신 상태까지 검증했다고 주장하지 않는다.
- `execution_method`: 현재 작업 브랜치·기존 산출물 수정 없이, 신규 감사 산출물 2개만 생성. `outputs/**` 원본 파일(계획본·실적본)은 read-only로만 다뤘다.

## WBS 원문

- 작업: as-built workbook의 문체, 수식, 렌더, 계획 대비 이탈을 재검사한다.
- Done·QA 계약: 한국어 humanizer audit, all sheets visible, formula error 0, deviation note 있음
- 실행·검증(WBS 원문, literal verifier): `node .omx/scripts/verify-production-readiness-plan.mjs`
- Workspace lock: `LOCK-DE-DB-01` · `service_db/**, ai/tests/contract/**, ai/tests/release_evaluator/**`
- 의도된 커밋: `[DOCS] verify Korean as-built WBS workbook`
- owner ≠ WBS-designated approver 확인, self-APPROVE 아님

## 검토 대상 workbook

- `outputs/quantagent-production-wbs-asbuilt-20260829/QuantAgent_프로덕션_신뢰성_WBS_실적본.xlsx`

## 실제 생성한 산출물

- `outputs/quantagent-production-wbs-asbuilt-20260829/asbuilt-xlsx-audit.json` — 시트 가시성·수식/오류 스캔·humanizer 감사 결과를 구조화한 감사 데이터.
- `outputs/quantagent-production-wbs-asbuilt-20260829/deviation-note.md` — Done 계약이 요구하는 deviation note. 계획본 대비 5개 항목(행 범위/컬럼 구조/수식/한국어 humanizer/원본 상태 보존)을 대조.

두 파일 모두 신규 생성이며, 기존 `XLX-ASBUILT-01`/`02` 산출물(evidence 문서, 실적본 workbook)은 수정하지 않았다.

## literal verifier 실제 실행 결과

```
$ node .omx/scripts/verify-production-readiness-plan.mjs
exit code: 1
stderr:
Error: Cannot find module '.../.omx/scripts/verify-production-readiness-plan.mjs'
code: 'MODULE_NOT_FOUND'
```

canonical `origin/main`(`tested_baseline_sha`)에도 최상위 `.omx/`가 존재하지 않음을 `git ls-tree -r origin/main --name-only`로 별도 재확인했다(`fe/.omx`는 무관한 프런트엔드 빌드 상태). WBS가 지정한 literal verifier로는 검증 절차를 끝까지 실행할 수 없었다. 이 사실을 숨기지 않고, 임의의 대체 명령이 이 literal verifier를 "통과했다"고 표현하지 않는다.

## Done 계약 4요소 — 직접 수행한 감사 결과

| # | 요소 | 결과 | 근거 |
|---|---|---|---|
| 1 | 한국어 humanizer audit | **수행됨** | `As-built note`/`Reviewer decision` 두 자유서술 컬럼을 정규식으로 스캔. 결과는 아래 별도 절 참고. |
| 2 | all sheets visible | **PASS** (3/3) | `1_RnR`, `2_WBS`, `3_Members` 전 시트 `sheet_state == visible` 직접 확인. |
| 3 | formula error 0 | **PASS** | 워크북 전체 셀 순회 — 수식(`=`로 시작하는 셀) 0건, 7종 오류 토큰(`#REF!` 등) 0건. |
| 4 | deviation note 있음 | **PASS** | `deviation-note.md` 작성 완료(아래 참고). |

**렌더 검증 범위**: WBS Done·QA 계약이 렌더 관련 완료 기준을 `all sheets visible`로 명시하고 있어 이번 audit에서는 각 sheet의 `sheet_state == visible` 여부를 기준으로 검증했다. 별도의 GUI/픽셀 단위 visual rendering 또는 screenshot 비교를 수행했다고 주장하지 않는다.

## 한국어 humanizer audit — 발견 사항 (observation/deviation, FAIL 아님)

- `As-built note` 컬럼 17행, `Reviewer decision` 컬럼 16행이 한글 없이 영어 전용 자유서술로 채워져 있었다(중복 제외 합계 **29/132행, 22.0%**).
- 이는 humanizer audit이 **수행됐다는 사실 자체에는 영향을 주지 않는다** — WBS Done 계약은 "한국어 humanizer audit"의 **수행**을 요구하며, 워크북 모든 셀의 완전한 한국어화를 명시적으로 요구하지 않는다.
- 따라서 이 발견은 **observation/deviation으로 기록**하며 자동으로 FAIL 처리하지 않는다. `deviation-note.md`에 상세 내역을 남겼다.

## 계획본 대비 deviation 요약 (`deviation-note.md` 발췌)

| # | 항목 | 성격 |
|---|---|---|
| 1 | 행 범위(계획본 91 → 실적본 132) | 의도된 확장(`XLX-ASBUILT-02` 실행 확정 지시에 따른 설계) |
| 2 | 컬럼 구조(일정관리형 17열 → QA-계약형 17+as-built 10=27열) | 의도된 설계 변경 |
| 3 | 수식(계획본 331 → 실적본 0) | 스냅샷 설계상 당연한 차이 |
| 4 | 한국어 humanizer(29/132행 영어 전용) | observation — 위 절 참고 |
| 5 | 원본 `현재 WBS 상태` 컬럼 보존 | 이탈 없음(export snapshot과 132행 전부 일치, 재확인 완료) |

## Workspace lock drift

이 행의 Workspace lock(`LOCK-DE-DB-01` · `service_db/**, ai/tests/contract/**, ai/tests/release_evaluator/**`)은 DB 관련 소스 경로를 지정하는 lock이며, 실제 작업(workbook 감사, `outputs/**` 경로)과는 일치하지 않는다. `XLX-ASBUILT-01`/`02`와 동일한 drift다. WBS export snapshot을 수정하지 않고 기록만 한다.

## 원본 파일 무결성 (작업 전후 무변경 확인)

| 파일 | SHA-256 |
|---|---|
| `outputs/quantagent-production-wbs-asbuilt-20260829/QuantAgent_프로덕션_신뢰성_WBS_실적본.xlsx` | `505a16f354d42cfc1430c74c9343b2535bc839750e93f7e92eb69ba8a7aa50ea` |
| `outputs/quantagent-production-wbs-20260813/QuantAgent_프로덕션_신뢰성_WBS_계획본.xlsx` | `92311cc787929b94963f77a180343185f859bfe7e27e0af048de77398ec2fa6b` |

두 파일 모두 이번 owner 작업 전후로 위 SHA-256이 **동일함을 재계산해 확인**했다 — 감사는 read-only로만 수행됐다.

## 산출물 무결성 검증

- `asbuilt-xlsx-audit.json`: 재파싱(re-parse) 성공, 구조 정상.
- `deviation-note.md`: 59줄, 정상 텍스트.
- `git status --short`: 신규 산출물 2개만 untracked, 기존 tracked 파일 변경 0건.
- 실적본 workbook: 3개 시트 전부 열람 가능, OOXML 구조상 이상 없음(openpyxl 재오픈 성공).

## Context — 선행 산출물 상태 (참고용 observation, 이번 판정의 직접 논거 아님)

`XLX-ASBUILT-01`, `XLX-ASBUILT-02` 모두 owner execution result가 `PARTIAL`이고 final reviewer decision이 pending 상태임을 확인했다. WBS 원문에는 이 두 항목의 reviewer APPROVE가 `XLX-ASBUILT-03`의 선행조건으로 명시돼 있지 않으므로, 이 사실은 **참고용 context로만 기록**하며 "입력이 PARTIAL이므로 03도 PASS 불가"라는 직접적 논리로 사용하지 않았다.

## Reviewer

- 이 문서는 owner evidence다.
- self-APPROVE 아님(WBS 소유자와 승인자 역할 분리 유지).
- final reviewer decision: pending.

## Owner execution result

**`PARTIAL`**

근거:
1. Done 계약 4요소에 대해 실제 감사 업무를 수행했다 — 전 시트 가시성 확인(PASS), 수식/오류 스캔(PASS), 한국어 humanizer audit 수행(observation 기록 포함), deviation note 작성(PASS)까지 전부 완료했다.
2. 그러나 WBS가 명시한 literal verifier(`node .omx/scripts/verify-production-readiness-plan.mjs`)가 canonical repo(`tested_baseline_sha` 기준 `origin/main`)에 존재하지 않아 `MODULE_NOT_FOUND`, exit 1로 실행이 실패했다 — WBS가 지정한 방식으로 검증 절차를 성공적으로 완료할 수 없었다.

`PARTIAL`의 핵심 사유는 이 literal verifier 부재이며, humanizer audit에서 발견된 29/132행 관찰사항이나 `XLX-ASBUILT-01`/`02`의 선행 상태는 이 판정의 직접적 근거로 사용하지 않았다.
