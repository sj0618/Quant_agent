# UX-BUILD-01 independent QA evidence

## 1. 검증 대상

- **WBS ID**: `UX-BUILD-01`
- **작업 원문**: 배포 화면에 개발 서버 주소와 소스 파일이 섞이면 배포가 실패하게 한다.
- **Done · QA 계약 원문**: deployed HTML에 Vite client·source module·localhost HMR 0건
  - 이 원문을 새 요구사항 추가 없이 세 개의 관찰 가능한 조건으로 분해한다(WBS 원문을 검증하기 위한 분해이며, 별도 요구사항이 아니다):
    - **Done A** — Vite client 0건
    - **Done B** — source module 0건
    - **Done C** — localhost HMR 0건
- **실행·검증 원문**: `cd fe && npm test`
- **의도된 커밋 원문**: `[FE] block Vite development assets in production`
- **Workspace lock 원문**: `LOCK-FE-LLM-01 · fe/**, ai/ai_graph/llm/**, docs/public-copy/**`
- **역할 분리**: Owner와 WBS 지정 독립 승인자가 서로 다른 역할임을 WBS에서 확인. self-APPROVE 아님.
- **subject SHA**: `470d33e496401e1a9ad0f9d1b5470bbe8f049e49` — migration-root(부모 커밋 없음). `.github/workflows/deploy.yml`/`server-health.yml`의 Vite dev-asset 검사 정규식이 이 시점부터 byte 단위로 변경 없이 canonical 이력에 존재한다.
- **tested SHA**: `6eb756c56c4d3b0bdd60b4109b602cd16960e8b3`
- **tested tree**: `910f6fc33e40ce557120a1ad1730d1b2567df3b6`
- **branch**: `evidence/qa-batch-20260831`
- **검토일**: 2026-09-02

### 기존 evidence 확인

`docs/evidence/UX-BUILD-01/`는 canonical `origin/main`에도, 현재 evidence branch에도, 확인 가능한 orphan 브랜치에도 존재하지 않았다 — 이 문서가 최초 evidence다.

## 2. 검증 범위와 방법

- Disposable detached `git worktree` + 전용 `venv`/`node_modules`에서 tested SHA를 checkout하여 실행. 원본 checkout(`<repository-root>`)의 working tree, `evidence/qa-batch-20260831` 브랜치, source, test, WBS, 기존 evidence 파일은 모두 무수정 상태로 유지했다.
- 모든 로컬 절대경로는 `<repository-root>`(원본 checkout) 또는 `<temporary-worktree>`(disposable worktree)로 일반화해 기록한다.
- 검증 범위: WBS literal verifier 단독 실행, 실제 `vite build` production 산출물 직접 검사, 로컬 `vite preview` 정적 서버를 통한 curl 기반 runtime 검사, 동일 tested SHA의 canonical GitHub Actions 로그(Frontend checks / deploy / server-health) 교차 확인.
- 범위 제외: 이번 QA에서는 별도의 공개 URL 브라우저 검증을 실행하지 않았다. 실제 배포된 production 서버에 대한 검증은 **동일 tested SHA의 deploy host 내부 loopback smoke 및 server-health CI 로그를 확인**하는 방식으로 수행했으며, 이번 QA 실행자가 공개 인터넷 URL을 직접 호출하지 않았다. 실제 secret/token/password/DSN은 사용·출력하지 않았다.

## 3. WBS 지정 검증 명령과 결과

- 명령: `npm test` (`node --test scripts/*.test.mts && npm run typecheck && npm run build`)
- working directory: `<temporary-worktree>/fe`
- 실행 조건: `npm install` 완료 확인 후, 다른 프로세스 없이 단독 실행.
- exit code: `0`
- 결과:
  - node tests: `tests 42, pass 42, fail 0`
  - typecheck: 성공
  - production build: 성공, build time `2.27s`
- **이 명령 자체에는 UX-BUILD-01의 dev-asset 부재를 직접 검사하는 assertion이 없다** — `fe/scripts/*.test.mts` 전체에서 "vite"/"@vite"/"HMR"/"localhost"/"@react-refresh"/"import.meta.hot" 문자열을 검사하는 test는 0개였다. 따라서 이 literal verifier의 PASS와 Done 조건의 실제 검증은 별개의 사실로 구분해 기록한다(4번, 6번).

## 4. Done 조건별 검증

### Production build 직접 검사 (`npm test`가 생성한 실제 `dist/` 산출물)

| 확인 항목 | 결과 |
|---|---|
| `dist/index.html`의 `/@vite/client` | 0건 |
| `dist/index.html`의 `/src/*.tsx?/jsx?` source module 참조 | 0건 |
| `dist/` 전체의 `localhost` | 0건 |
| `dist/` 전체의 `ws://` | 0건 |
| source map(`.map`) | 0개 |
| `dist/index.html`이 참조하는 JS/CSS asset의 실제 존재 여부 | 존재 확인 |
| 생성 파일 수 | 97개 |
| 총 크기 | 약 3.9 MB |

### 로컬 production preview 검사 (`vite preview`, `dist/` 그대로 서빙)

- `/`: HTTP `200`, 응답 HTML에서 위와 동일한 dev-asset 패턴 재검사 — clean.
- main JS asset(`/assets/index-*.js`): HTTP `200`.
- unknown route(`/retired-internal-route`)의 SPA fallback `200`은 **`vite preview`의 알려진 동작**이며, `fe/scripts/production-gateway.mjs`가 제공하는 실제 production gateway의 backend-owned 404 정책을 재현하지 못하는 **로컬 재현의 한계**로 기록한다(UX-BUILD-01 결함 아님 — 아래 6번 참조).

### 동일 tested SHA 배포 검증 (CI 로그 확인, 공개 URL 직접 호출 아님)

- 정확한 표현: **동일 tested SHA의 deploy host 내부 loopback smoke 및 server-health CI 로그를 확인**했다. 이번 QA에서는 별도의 공개 URL 브라우저 검증을 실행하지 않았다.
- "Frontend checks"(`npm run test`, WBS 명령과 동일): `success`.
- "deploy" job(`https://github.com/sj0618/Quant_agent/actions/runs/33589233752/job/100119958866`): `success`. 로그 확인 결과, deploy host 내부 loopback(`http://127.0.0.1:18000/`)의 실제 배포된 루트 HTML에 대해 `grep -Eq '(/@vite/client|/src/[^"[:space:]]+\.(tsx?|jsx?))'` 검사가 실행됐고, 실패 메시지 없이 다음 단계(`unknown_route_status = 404` 확인 등)로 정상 진행해 job 전체가 성공으로 종료됐다.
- "server-health" workflow의 동일 검사도 이 tested SHA에서 가장 최근 실행(2026-09-02T04:05:04Z)이 `success`.

## 5. Done별 판정

| Done | 판정 | 근거 |
|---|---|---|
| Done A (Vite client 0건) | `PASS` | production build 직접 검사(4번) + 로컬 preview curl 검사(4번) + 동일 tested SHA deploy/server-health CI loopback 검사(4번) 3중 확인, 모두 0건. |
| Done B (source module 0건) | `PASS` | 위와 동일한 3중 검사, 루트 HTML 기준 0건. |
| Done C (localhost HMR 0건) | `PASS` | `dist/` 전체에서 `localhost`/`ws://` 문자열 검색 결과 0건. **다만 localhost/HMR만을 독립적으로 차단하는 전용 CI assertion은 없다** — non-blocking observation으로 기록한다(6번, 8번). |

### EmailTemplatePreviewPage 관련 구분 (숨기지 않고 명시)

Production build의 `dist/assets/EmailTemplatePreviewPage-*.js` chunk에서 `/src/features/reports/DailyDigestEmail.tsx`, `/src/mocks/dailyDigest.mock.ts` 형태의 문자열이 발견됐다. 이 사실을 숨기지 않고 다음과 같이 정확히 분리해 기록한다:

- 이 문자열은 Vite 개발 서버가 source module을 로드하는 HTML 참조가 아니라, `EmailTemplatePreviewPage` 컴포넌트 자신이 사용자에게 보여주는 안내 텍스트(`<code>` 태그로 파일 경로를 설명하는 문서화 문구)다.
- UX-BUILD-01의 "deployed **HTML**" 조건은 배포 서버가 반환하는 루트 HTML을 대상으로 하며, 이 chunk는 그 검사 범위에 해당하지 않는다.
- `/dev/email-template` route와 이 전용 chunk가 production build에 재유입된 문제 자체는 별도 WBS `OD-DIG-02`와 Issue [#75](https://github.com/sj0618/Quant_agent/issues/75)(`[OD-DIG-02][회귀 수정] production bundle에 재유입된 이메일 템플릿 미리보기 제거`)에서 `NEEDS_FIX`로 별도 관리 중이다.
- 이 발견을 UX-BUILD-01의 PASS 근거와 혼합하지 않았고, OD-DIG-02의 결함이 해소된 것으로 표현하지 않는다 — 두 WBS 항목의 판정은 서로 독립적이다.

## 6. 반증 및 예외 확인

1. localhost/HMR 하위 조건(Done C)을 독립적으로 차단하는 전용 자동 assertion은 CI에도 로컬에도 없다 — 현재는 `/@vite/client` 검사가 실질적으로 이를 포괄하지만, 문자 그대로의 "localhost" 문자열만을 검사하는 별도 게이트는 부재하다.
2. WBS의 "실행·검증"(`npm test`)과 UX-BUILD-01의 실제 Done 강제 메커니즘(`deploy.yml`/`server-health.yml`의 배포 후 loopback smoke)은 서로 다른 위치에 있다 — literal verifier 통과가 Done 충족의 직접 증거가 아니라는 점을 CI/로컬 양쪽에서 일관되게 확인했다.
3. 로컬 `vite preview`는 `fe/scripts/production-gateway.mjs`가 제공하는 실제 production topology의 backend-owned 404 라우팅을 재현하지 않는다 — 이는 소스 자체의 주석("Unknown HTML paths therefore retain the backend's real 404 instead of Vite preview's SPA 200 fallback")과 일치하는, 로컬 재현의 알려진 한계다.
4. `EmailTemplatePreviewPage` chunk의 `/dev/email-template` route/preview 재유입 회귀는 이 evidence의 판정 범위 밖이며, 별도 WBS `OD-DIG-02` 및 Issue #75에서 `NEEDS_FIX`로 관리 중이다(5번).
5. subject SHA(`470d33e...`)부터 tested SHA까지 이 Done 메커니즘 자체에는 회귀·orphan 단절이 발견되지 않았다 — 다른 여러 WBS 행에서 반복 확인된 "orphan 계보에만 존재하는 fix" 패턴과 달리, 이번 메커니즘은 canonical `origin/main`에서 끊김 없이 유지됐다.

## 7. 재현 절차

1. `<repository-root>`에서 canonical repository를 clone/fetch한다.
2. tested SHA를 detached 상태로 checkout한다: `git worktree add --detach <temporary-worktree> 6eb756c56c4d3b0bdd60b4109b602cd16960e8b3`.
3. `<temporary-worktree>/fe`에서 `npm install`을 실행하고 완료를 확인한다.
4. WBS literal verifier: `<temporary-worktree>/fe`에서 `npm test` 실행 (다른 프로세스 없이 단독 실행).
5. Production build 직접 검사: `npm test`가 생성한 `<temporary-worktree>/fe/dist/index.html`에서 `grep -Eq '(/@vite/client|/src/[^"[:space:]]+\.(tsx?|jsx?))'` 실행, `dist/` 전체에서 `localhost`/`ws://` 문자열을 검색한다.
6. 로컬 runtime 검사: `<temporary-worktree>/fe`에서 `npm run preview`로 `dist/`를 서빙하고, `curl`로 `/`(루트 HTML 재검사), main JS asset, 임의의 미존재 경로를 확인한다.
7. 배포 검증 교차 확인: `gh api repos/sj0618/Quant_agent/commits/<tested-SHA>/check-runs`로 동일 tested SHA의 "deploy"/"server-health" job 결론을 확인하고, `gh api repos/sj0618/Quant_agent/actions/jobs/<job-id>/logs`로 loopback smoke 단계의 실제 실행 로그를 확인한다.
8. 검증 완료 후 `<temporary-worktree>`와 로컬 preview 프로세스를 제거·종료한다.

## 8. 최종 판정

- **technical result**: `PASS`
- **review verdict**: `APPROVE WITH NON-BLOCKING OBSERVATIONS`
- **blocking issue**: 없음
- **non-blocking observations**:
  1. localhost/HMR 전용 자동 assertion 부재
  2. WBS literal verifier와 실제 deploy-time gate가 서로 다른 위치에 있음
  3. 로컬 `vite preview`는 production gateway의 backend-owned 404를 재현하지 않음
  4. 별도 OD-DIG-02 preview route/chunk 회귀는 이 판정 범위 밖에서 관리 중(Issue #75)
- **approval status**: `승인 완료`
- **승인일**: 2026-09-02
- **WBS 완료 전이 가능 여부**: evidence push 및 immutable permalink 확보 후 가능

## 9. 승인 정보

- QA 실행 및 이 evidence 작성: `WBS 지정 독립 승인자 수행 read-only QA`
- 승인 권한: `WBS 지정 독립 승인자`
- 실제 승인 상태: `승인 완료`
- 승인 기록: `WBS 지정 독립 승인자가 WBS 원문, Done별 검증 결과 및 non-blocking observations를 검토하고 승인 · 2026-09-02`
- Owner와 WBS 지정 독립 승인자가 서로 다른 역할임이 WBS에서 확인되어 self-approval 충돌이 없음.
- Owner와 승인자의 실명·개인 이메일은 기록하지 않는다.
