# `RS-MAT-01` independent QA evidence

- `wbs_id`: `RS-MAT-01`
- `role`: independent QA reviewer — WBS-designated approver이며 owner와 다른 사람. self-QA 아님.
- `tested_baseline_sha`: `2783022c59711353dabe54da0403ae287e8dd4cd` (`origin/main`)
- `execution_method`: 현재 evidence branch·working tree를 건드리지 않고, tested baseline SHA를 대상으로 한 격리된 detached worktree에서 검증을 수행한 뒤 worktree를 제거했다. 이후 evidence branch/HEAD/working tree가 변경되지 않았음을 재확인했다.

## delayed QA 맥락

이번 QA는 owner 작업 수행 시점보다 늦게 수행된 **delayed independent QA**다.

- comparator 기준일: 2026-08-24
- QA 수행일: 2026-08-31
- QA 날짜에 외부 comparator(Freqtrade/Qlib/QuantConnect) 최신 버전을 다시 조회하지 않았다는 사실은 blocker가 아니다.
- 이번 QA는 owner가 당시 만든 artifact가 **기록된 comparator 맥락(2026-08-24) 안에서** WBS 계약을 충족하는지를 검증하는 것이며, QA 날짜 기준으로 comparator를 다시 refresh했다고 주장하지 않는다.

historical cited artifact(아래 "historical reference" 절)는 canonical main의 현재 파일과 byte-identical하게 보존돼 있음을 확인했다.

## WBS contract

- 작업(Work): `comparator 버전과 7축 점수를 릴리스 기준선으로 재실행한다.`
- Done·QA 계약: `각 축에 현재 command·근거·점수·미평가 판단이 있음`
- 실행·검증(literal verifier): `node .omx/scripts/verify-production-readiness-plan.mjs`

## Done 항목별 최종 판정

| # | Done 세부 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | comparator/version 기록 존재 | **PASS** | 대상 문서의 "비교 기준" 표에 comparator 4종과 확인 시점(2026-08-24)이 명시돼 있음. |
| 2 | 7축 근거 | **PASS 7/7** | 대상 문서의 "7축 재평가" 표 7행 전부에 서술형 근거가 존재. |
| 3 | 7축 점수 | **PASS 7/7** | 7행 전부에 점수(총점 11/28) 존재. |
| 4 | 7축 미평가 판단 | **PASS 7/7** | 7행 전부에 "아직 없는 필수 증적" 열이 존재. |
| 5 | 7축 command | **FAIL 5/7** | 아래 "command가 누락된 축" 참조. |
| 6 | literal verifier | **FAIL** | 아래 "literal verifier 실패" 참조. |

### command가 누락된 축

다음 2개 축은 대상 문서에 재현/검증 command 자체가 존재하지 않는다(근거·점수·미평가 판단 열은 존재):

1. 배포 재현성·public topology
2. 관측성·증적

이 두 축은 artifact 자체를 직접 읽어 command 부재를 확인한 것이므로, 외부 조건 때문에 판정 불가능한 BLOCKED가 아니라 **명시적 Done 계약 미충족**이다.

## delayed-QA supplemental verification (tested baseline 재실행)

실행 가능한 5개 축의 command를 canonical tested baseline(`2783022c...`)의 격리 worktree에서 실제로 재실행했다:

| 축 | 결과 |
|---|---|
| 실데이터와 fixture 분리 | offline gate Python 단계 전부 PASS(exit 0). FE 단계는 Windows `spawnSync("npm")` ENOENT로 실패했으나, same-SHA canonical Linux CI에서 FE **68/68 PASS**로 별도 교차 확인. |
| provider 실패 투명성 | **62 passed / exit 0** |
| metric 정의·계보 | **22 passed / exit 0** |
| 백테스트 타당성 | **56 passed / exit 0** |
| 인증·복구 UX | local Windows 62/68(무관한 기존 harness 문제 6건 제외), same-SHA canonical Linux CI **68/68 PASS** |

이 결과는 실행 가능한 5개 축의 현재 코드 상태가 benchmark 문서의 근거·점수와 대체로 일치함을 보여주는 **긍정적 보강 근거**다. 단, **2개 축의 command 누락이라는 Done 미충족을 상쇄하지 않는다.**

## literal verifier 실패

```
$ node .omx/scripts/verify-production-readiness-plan.mjs
Error: Cannot find module '...\.omx\scripts\verify-production-readiness-plan.mjs'
code: 'MODULE_NOT_FOUND'
exit code: 1
```

canonical tested baseline에 `.omx/` 경로 자체가 존재하지 않음을 확인했다. 다른 WBS 항목에서도 반복 확인된 기존 verifier-path gap이라는 사실은 observation으로만 기록하며, 이 사실이 RS-MAT-01의 literal verifier FAIL 판정 자체를 PASS로 바꾸지 않는다.

## historical reference

- historical cited SHA: `caab7cd6882defdf9e4a067f5cf8e7676bb34927`
- 이 SHA는 canonical `origin/main`과 공통 조상이 없는 disconnected/orphan lineage에 위치하며, 이번 QA의 tested baseline으로 사용하지 않았다.
- 다만 cited SHA의 `docs/plans/quantagent-production-maturity-benchmark.md` 내용을 canonical `origin/main`의 동일 파일과 직접 대조한 결과 **byte-identical**함을 확인했다 — artifact 내용 자체는 손실 없이 canonical main에 보존돼 있다.
- tested baseline은 오직 `2783022c59711353dabe54da0403ae287e8dd4cd`로 기록한다.

## S/R/O/C 참고 상태

`QA-WBS-01`(QuantQA:v1)은 여전히 DRAFT/Approval PENDING이므로 참고용으로만 기록하며 새로운 완료조건으로 강제하지 않는다.

- S = **FAIL** — 명시적 Done 계약(각 축 command)이 2/7 축에서 미충족이고, literal verifier도 canonical baseline에서 실행 불가하다. 정적/구조적 축 자체에 결함이 있으므로 PASS로 기록하지 않는다.
- R = **N/A**
- O = **N/A**
- C = **N/A**

## Observations

1. historical reference는 orphan/disconnected lineage이지만, artifact 내용은 current main과 byte-identical하다.
2. 이번 QA는 delayed QA이므로 comparator를 2026-08-31 현재 기준으로 다시 refresh하지 않았다 — 이는 blocker가 아니라 QA 범위의 명시적 한계다.
3. 실행 가능한 5/7 축의 supplemental replay 결과는 benchmark 문서의 근거·점수와 일치한다.
4. 2/7 축(배포 재현성·public topology, 관측성·증적)은 command 자체가 artifact에 없다.
5. literal verifier는 canonical repo에서 실행 불가하다(MODULE_NOT_FOUND).
6. benchmark 문서 내부의 "총점 11/28" / "배포 가능 No"는 QuantAgent 제품 자체의 maturity 평가 결과이며, RS-MAT-01 WBS 항목의 이번 NOT APPROVE 판정 이유와는 별개다.

## Blocker

**none.**

핵심 미충족 여부(2/7 command 누락, literal verifier FAIL)를 외부 조건 없이 canonical artifact 직접 검토와 literal verifier 실행만으로 판정할 수 있었기 때문이다.

## Required fixes

1. **배포 재현성·public topology** 축에 현재 재현 가능한 verification command 또는 WBS 계약상 허용되는 명시적 verifier를 추가한다.
2. **관측성·증적** 축에 현재 재현 가능한 verification command 또는 명시적 verifier를 추가한다.
3. WBS literal verifier `.omx/scripts/verify-production-readiness-plan.mjs`의 canonical 실행 경로를 복구하거나, 실제 canonical validator 또는 명시적 manual-review contract로 WBS verifier 필드를 정정한다.

이번 QA reviewer는 이 fix들을 직접 적용하지 않았다 — source/WBS를 수정하지 않았다.

## Reviewer

- 이 문서는 independent QA reviewer evidence다.
- owner ≠ approver, self-QA 아님.

## Final verdict

**`NOT APPROVE`**

핵심 근거:

- 7축 command 요구 중 2축이 누락됐다.
- literal verifier가 canonical tested baseline에서 실행 불가하다(MODULE_NOT_FOUND).
- 둘 다 canonical baseline에서 직접 재현 가능한 계약 미충족이며, 외부 조건에 좌우되는 판정 불가 상태(BLOCKED)가 아니다.
- 5/7 축의 긍정적 supplemental replay 결과는 강한 보강 근거이지만 위 미충족을 상쇄하지 않는다.
