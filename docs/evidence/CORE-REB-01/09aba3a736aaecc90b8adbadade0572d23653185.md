# CORE-REB-01 independent QA evidence (reviewer draft)

- `wbs_id`: `CORE-REB-01`
- `role`: independent reviewer QA — WBS-designated approver, owner와 상이함. self-QA 아님.
- `tested_baseline_sha`: `09aba3a736aaecc90b8adbadade0572d23653185` (`origin/main`, 이 QA 세션이 실제로 검증을 재현한 기준선)
- `cited_sha`: `33866dac69fe0f8c25ae0b00d556f4d69de40541` — WBS 증적URI 원문이 인용한 커밋(squash-merge 이전 원본 작업 커밋)
- `canonical_merge_result_sha`: `4bc31482f91ec7e75b446dd3464519c95d96439a` — PR #11의 실제 merge 결과 커밋
- `execution_method`: 현재 작업 브랜치·application source 수정 없이, `tested_baseline_sha`를 가리키는 detached git worktree에서 read-only 검증만 수행. 검증 후 worktree 제거, 작업 브랜치/HEAD 무변경 확인.

## WBS 원문

- 작업: 온디맨드 퇴역/리서치 전용 계획을 제품 핵심 경로 복구 계획으로 대체하고, 충돌 행의 supersession을 기록한다.
- Done·QA 계약: deep-research 기록과 OD/RMP→CORE 대체표가 있고, core 경로 삭제가 release gate가 아님을 검증한다.
- 실행·검증(WBS 원문, literal verifier): `git diff --check -- docs/plans`
- Workspace lock: `LOCK-FE-LLM-01` · `docs/plans/**, fe/**`
- 의도된 커밋: `[DOCS] rebaseline core strategy workspace`
- owner ≠ WBS-designated approver 확인, self-QA 아님

## Lineage 재확인 (독립 실행, 기존 조사 결과를 그대로 신뢰하지 않고 재검증)

```
git cat-file -e cited_sha^{commit}                        → EXISTS
git merge-base --is-ancestor cited_sha tested_baseline_sha → NO (main 조상 아님)
gh pr view 11                                              → state: MERGED, mergeCommit = canonical_merge_result_sha
git merge-base --is-ancestor canonical_merge_result_sha tested_baseline_sha → YES
```

`cited_sha`는 canonical repo에 실제 존재하지만 squash-merge 이전 원본 커밋이라 그 자체로는 `tested_baseline_sha`의 조상이 아니다. 그러나 이 커밋이 속한 PR #11은 실제로 **MERGED**됐고, 그 merge-result인 `canonical_merge_result_sha`는 `tested_baseline_sha`의 조상임을 재확인했다. `cited_sha`와 `canonical_merge_result_sha`를 혼동하지 않고 별도 필드로 기록한다.

## literal verifier 실행 결과 (`tested_baseline_sha` 기준)

```
$ git diff --check -- docs/plans
exit code: 0
stdout: (empty)
stderr: (empty)
```

**observation**: `git diff --check -- docs/plans`는 이번 clean detached worktree에서는 검사할 미커밋 diff가 없어 exit 0이었다. 따라서 이 실행 결과만으로 이미 커밋된 문서의 실질 내용을 검증했다고 볼 수 없다. Done 계약의 내용 검증은 아래 절에서 canonical 문서를 별도로 직접 읽어 수행했다.

## Done 계약 3요소 — 근거 문서 직접 검토

근거 문서: `docs/plans/core-strategy-workspace-rebaseline-20260826.md` (`tested_baseline_sha` 기준 canonical repo에서 직접 열람)

| # | 요소 | 판정 | 근거 |
|---|---|---|---|
| 1 | deep-research 기록 존재 | **PASS** | "Why the existing plan is invalid" 절 — claim/evidence/verdict 표(5행) + 별도 "Adversarial check" 소절. 기존 안전장치 인정, 실패 원인 분석, 대체안 근거를 모두 포함한 연구 기록 형식을 갖춤. |
| 2 | OD/RMP → CORE 대체표 존재 | **PASS** | "WBS transformation" 절의 `New ID \| Replaces / reuses \| ...` 표. `CORE-REB-01` 자체 행이 `OD-INV-01, OD-FE-01/02, OD-API-01, OD-JOB-01, OD-E2E-01, RMP-REB-01`을 대체 대상으로 명시적으로 나열. |
| 3 | retirement가 release gate가 아니라 superseded 처리 | **PASS** | 동일 절: "Its retirement rows must be labelled superseded by D-CORE-001 and must not be counted toward product completion." — release gate 배제를 문자 그대로 명시. |

**보강 확인(범위를 구분해 기록)**: supersession 판정의 직접 근거는 위 표에 정리한 canonical 문서의 **ID 기반 대체표(요소 2)와 명시적 문구(요소 3)**이며, 이 둘만으로 요소 2·3의 PASS 판정을 내렸다. 이와 별도로, 대체표가 인용한 `OD-INV-01`, `OD-FE-01`, `OD-FE-02`, `OD-API-01`, `OD-JOB-01`, `OD-E2E-01`, `RMP-REB-01` 7건을 최신 WBS export snapshot에서 조회한 결과 **7건 전부 현재도 유효한 ID로 존재하며 상태=`차단`으로 추적되고 있음**을 확인했다. 이는 단지 해당 7건이 WBS에서 여전히 유효한 ID로 추적되고 있으며 현재 상태가 `차단`이라는 별개의 사실이다. `차단`을 `superseded`와 동일시하지 않으며, supersession의 근거는 canonical 문서의 ID 기반 대체표와 명시적 문구다.

## Observations (비차단)

- **historical row-number drift**: 근거 문서 본문이 인용한 "Live 2_WBS rows 28, 34, 41, 45, 52" 및 "rows 94–103"은 문서 작성 시점의 행 번호이며, 현재 export snapshot에서 해당 ID들의 실제 행 번호와 다르다(WBS가 그 사이 재정렬됨). 그러나 Done 계약의 핵심 요소(대체표, 요소 2)는 행 번호가 아니라 **ID 기반**이다. 그 ID들은 현재도 유효하게 존재하므로 ID 기반 대체표의 참조 대상 자체는 유지된다. 따라서 이 drift는 대체표의 유효성을 깨뜨리지 않는 **단순 historical reference**로 판단했다.
- **literal verifier의 검증 범위 한계**: 위 "literal verifier 실행 결과" 절의 observation 참고 — exit 0은 미커밋 diff 부재에 따른 결과이며 committed 문서의 실질 내용 검증을 의미하지 않는다. 이번 QA는 그 실질 내용을 사람이 근거 문서를 직접 읽어 별도로 확인했다(위 Done 계약 3요소 절).

## 반증/미검증 범위

- 이 문서는 스스로 "decision and source audit" 성격임을 명시하며, 운영 환경·DB·provider·workload 접근이나 operational-ready 주장을 하지 않는다. 이번 QA도 그 범위(S 증적) 내에서만 검증했으며 R/O/C 증적은 대상이 아니다.

## Reviewer verdict

- **verdict: `APPROVE WITH OBSERVATIONS`**
- 근거: owner ≠ WBS-designated approver, self-QA 아님. literal verifier를 `tested_baseline_sha`에서 독립 재현했고(exit 0, stdout/stderr 모두 empty), Done 계약 3요소를 근거 문서에서 직접 확인해 전부 PASS했다. `cited_sha`와 `canonical_merge_result_sha`를 구분해 lineage를 재확인했고, `canonical_merge_result_sha`가 `tested_baseline_sha`의 조상임을 확인했다. `APPROVE`가 아니라 `APPROVE WITH OBSERVATIONS`인 이유는 literal verifier의 검증 범위 한계와 historical row-number drift라는 두 가지 비차단 관찰사항이 있기 때문이다 — 둘 다 Done 계약 충족 여부 자체를 바꾸지 않는다.
- Reviewer: WBS-designated approver (independent reviewer, self-approval 아님)
- Reviewed at: 2026-08-29
