# QA-DATA-AUDIT-01 QA Evidence

## 1. 검증 대상

- WBS ID: `QA-DATA-AUDIT-01`
- 작업 원문: `release source manifest의 source/as-of/freshness/lineage 필드를 표본 감사한다.`
- Done · QA 계약: `필수 필드 누락 0, 표본 hash 재계산 일치`
- WBS 지정 실행·검증: `env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL <venv-python> -m pytest -q ai/tests/test_source_manifest.py`
- tested_sha: `e966587c27db4db5aa8633c19c03ffa2d9541bb3`

## 2. 검증 환경 및 범위

- OS: Windows
- interpreter: repository-local venv python
- Python: `3.12.5`
- pytest: `9.1.1`
- 실행 방식: tested_sha를 detached checkout한 별도 worktree에서 실행 — 현재 작업 브랜치는 건드리지 않았고, application source·test·fixture는 이 QA 과정에서 전혀 수정하지 않았다.
- 검증 범위: `ai/tests/test_source_manifest.py`에 tracked된, source manifest의 필수 필드 구성과 lineage hash 재계산·검증 로직
- 제외 범위: 동일 SHA의 CI(GitHub Actions) 실행 로그는 이번 검증에서 조회하지 않았다

## 3. WBS 지정 명령 실행

```text
env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL <venv-python> -m pytest -q ai/tests/test_source_manifest.py
```

- exit code: `0`
- 결과: `6 passed`, `0 failed`

```text
ai/tests/test_source_manifest.py::test_source_manifest_contains_required_fields_and_matching_lineage_hash PASSED
ai/tests/test_source_manifest.py::test_missing_manifest_fields_are_rejected_for_release PASSED
ai/tests/test_source_manifest.py::test_fixture_and_stale_manifest_are_not_release_eligible PASSED
ai/tests/test_source_manifest.py::test_lineage_hash_mismatch_is_rejected PASSED
ai/tests/test_source_manifest.py::test_release_rejects_a_valid_new_manifest_for_other_loaded_data PASSED
ai/tests/test_source_manifest.py::test_release_profile_fails_before_fixture_analysis_can_return_a_result PASSED
```

## 4. Done 항목별 결과

| Done 축 | 검증 방법 | 실제 근거 | 판정 |
|---|---|---|---|
| 필수 필드 누락 0 | `test_source_manifest_contains_required_fields_and_matching_lineage_hash`(정상 케이스) + `test_missing_manifest_fields_are_rejected_for_release`(누락 케이스) | 정상 manifest는 `assert set(manifest.model_dump()) >= {"source", "as_of", "freshness", "extract_hash", "snapshot_id", "lineage_hash"}`로 필수 필드 집합을 모두 포함함을 확인하고, manifest 자체가 없는 입력(`{"source": "postgres"}`)은 `validate_release_metadata`가 `errors == ("source manifest is missing",)`로 거부함을 확인 | `PASS` |
| 표본 hash 재계산 일치 | `test_source_manifest_contains_required_fields_and_matching_lineage_hash`(일치 케이스) + `test_lineage_hash_mismatch_is_rejected`(불일치 케이스) | 정상 manifest에서 `assert manifest.lineage_hash == compute_lineage_hash(manifest)`로 저장된 lineage_hash와 재계산 hash의 일치를 직접 assert하며, `validate_source_manifest(manifest, release_profile=True) == ()`로 오류 없음(빈 tuple)을 확인. 필드 하나를 변경해 hash가 더 이상 일치하지 않는 manifest는 `validate_source_manifest(changed)`가 `("source manifest lineage_hash does not match canonical fields", "source manifest snapshot_id does not match canonical fields")`를 반환함을 확인 | `PASS` |

## 5. 최종 판정

- QA verdict: `APPROVE`
- Done 충족: `PASS`
- blocking issue: 없음
- non-blocking observations:
  - 동일 SHA CI 로그는 이번 검증에서 조회하지 않았다
  - 이번 검증은 unit-test 수준의 필드·hash 로직 검사이며, 실제 라이브 데이터소스에 대한 end-to-end provenance 검증은 이 evidence 범위 밖이다

## 6. 검토 기록

- 검토일: `2026-08-27`
