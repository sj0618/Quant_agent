# P0-BE-01 증적 묶음

> **Migration note**: 이 문서는 public-safe 이관본이다. pre-migration(비공개) 저장소에서 작성된 원본 QA 증적을 public `Quant_agent` 저장소로 옮긴 사본이며, 새로 QA를 수행한 문서가 아니다. 기존 QA verdict와 tested SHA는 그대로 유지했고, 개인 식별정보와 private repository 참조만 일반화했다.
>
> **Historical evidence**: 이 문서는 pre-migration 시점에 완료된 QA 승인 증적을 public 저장소로 이관한 것이다. 기존 QA 판정과 WBS 완료 상태는 유지하며, 이후 코드 변경분(`backend/tests/unit/test_auth_routes.py`)에 대한 재검증을 의미하지는 않는다.

## 검증 대상

- wbs_id: `P0-BE-01`
- subject_implementation_sha: `897fc83a80555bc42a415c50bfe4e13a291e4514` (WBS `증적 묶음 URI`에 기록된 커밋. **주의**: 이 커밋의 실제 변경 범위는 `.github/workflows/**`와 `ai/**`뿐이며 `backend/**`를 전혀 건드리지 않는다 — 아래 "제한사항" 참조)
- tested_sha: `ba60e9939ccf67e17f097e08c64432e461b046b8` (fetch 시점의 `origin/main` HEAD)
- tested_tree: `b9fd41ffd9f5671032b1e67d00037a0fd6a914fe`
- bundle_revision: 이 파일이 포함된 Git revision. 불변 URI는 WBS 증적 칸에 기록됨.
- output_uri: 동일한 불변 bundle revision에서의 이 파일의 Git blob.

## 최신 Done

2_WBS(gid `2030261433`) `P0-BE-01` 행 기준:

- 단계: 인증
- 작업: 인증·session 실패를 exact error envelope와 no-data-leak test로 고정한다.
- Done · QA 계약: `auth failure code/retry/no-data-leak PASS`
- 실행·검증(WBS 지정): `cd backend && pytest -q tests -k 'auth or session'`
- Owner/승인자: WBS 원문 참조 / 상태·계약: 증적대기

## 기존 증적

WBS `증적 묶음 URI` 칸: `commit only · (원 저장소 커밋 참조, 비공개)`. GitHub Issue 검색(`P0-BE-01`) 결과 이 ID를 다루는 기존 Issue/PR은 없다.

## 실제 추가 검증

### 검증 환경

`origin/main`을 별도 worktree(`qa/p0-be-01-evidence`)로 체크아웃하고 `backend/pyproject.toml`의 `[test]` extra(`pytest`, `pytest-asyncio`) 및 import 체인에 필요한 `numpy`, `psycopg[binary]`를 venv에 추가 설치했다(실 DB·Redis·외부 provider는 사용하지 않음, 테스트는 `FakeRedis` fixture를 사용).

### WBS 지정 명령 실행

```
cd backend
PYTHONPATH="<repo-root>;<repo-root>/ai" pytest -q tests -k "auth or session"
```

(원래 명령에 `PYTHONPATH` 지정이 없어 최초 실행 시 `tests/unit/test_ai_backtest_runtime.py`, `tests/unit/test_combined_main.py`가 `ai_graph`/`combined_main` 모듈을 찾지 못해 수집 오류가 발생했다. 두 파일 모두 `-k 'auth or session'`과 무관하며 다른 WBS 행이 실제로 사용하는 `PYTHONPATH=. ../ai` 패턴과 동일하게 경로를 보강한 뒤 재실행했다.)

결과:

```
139 passed, 232 deselected, 18 warnings
exit 0
```

경고는 전부 `starlette.testclient`의 쿠키 인자 deprecation 경고로 auth/session 로직과 무관하다.

### Done 근거가 되는 대표 테스트

`backend/tests/unit/test_auth_routes.py::test_auth_me_redis_failure_has_a_redacted_envelope_and_allows_retry`가 Done의 세 항목을 한 테스트에서 직접 검증한다.

- Redis 읽기 실패를 주입하면 `503`과 함께 `{"error": {"component": "redis", "code": "redis_read_failed", "message": "Redis read failed", "details": {"error": "...password=<redacted>"}}}` 구조의 exact error envelope를 반환한다.
- 같은 요청을 재시도(retry)하면 `200`과 정상 사용자 payload를 반환한다.
- 원본 예외 메시지에 포함된 마커 문자열과 session_id가 응답 본문에 없음을 직접 단언한다(no-data-leak).

## Done별 결과

| Done 하위 항목 | 결과 | 근거 |
|---|---|---|
| auth failure code (exact error envelope) | **PASS** | `test_auth_me_redis_failure_has_a_redacted_envelope_and_allows_retry`가 `component/code/message/details` 구조를 정확히 단언 |
| retry | **PASS** | 동일 테스트에서 실패 이후 재요청이 `200`으로 성공함을 확인 |
| no-data-leak | **PASS** | 동일 테스트에서 원본 예외 마커·session_id가 응답 본문에 없음을 직접 단언, redis 세부는 `<redacted>` |
| WBS 지정 명령 전체(139개 auth/session 테스트) | **PASS** | exit 0, 139 passed / 0 failed |

## 제한사항 및 non-blocking observation

- WBS `증적 묶음 URI`가 인용한 커밋 `897fc83`는 실제로는 `.github/workflows/**`와 `ai/**`만 변경했으며 `backend/**`는 전혀 건드리지 않는다. `git log -S"redis_read_failed" -- backend/`로 확인한 결과 이 envelope·테스트를 실제로 도입한 커밋은 `9f7f4a1`("[FEAT] Backend Google 로그인 및 세션 인증 구현")이다. 이는 WBS 증적 URI의 커밋 인용 오류로 보이며, Done 자체는 `tested_sha`에서 직접 재실행한 테스트로 독립 확인했으므로 제품 결함이 아니라 증적 메타데이터 정정이 필요한 non-blocking 항목으로 기록한다.
- `cd backend && pytest -q tests -k 'auth or session'`은 `PYTHONPATH` 없이 그대로 실행하면 무관한 두 파일에서 수집 오류가 난다(위 기록). 이 gap은 auth/session Done과 무관하지만, WBS 지정 명령 문구 자체는 실행 환경에 따라 그대로 재현되지 않을 수 있어 별도로 기록한다.
- application source·product test·WBS는 이 QA 과정에서 전혀 수정하지 않았다.

## 검토 기록

- QA 실행/증적 수집: AI-assisted QA (Claude Code)
- WBS 지정 승인자: (WBS 원문 참조)
- 최종 판정: `APPROVE WITH NON-BLOCKING OBSERVATIONS`
- 검토일: `2026-08-24 (KST)`
