# `RMP-HISTORY-01` independent QA evidence

- `wbs_id`: `RMP-HISTORY-01`
- `role`: independent QA reviewer — WBS-designated approver이며 owner와 다른 사람. self-QA 아님.
- `tested_sha`: `c0c0f28ce9f3294d0c97ab4d06054c43348c02b3` (PR #7 head)
- `latest_canonical_main_at_qa_time`: `acf0e54571224fb80d302ad7243afbfd043881ab` (`origin/main`)
- `execution_method`: 현재 evidence branch·working tree를 건드리지 않고, tested SHA를 대상으로 한 격리된 detached worktree에서 검증을 수행한 뒤 worktree를 제거했다. 이후 evidence branch/HEAD/working tree가 변경되지 않았음을 재확인했다.

## 관련 PR

- PR #7, `state: OPEN`, `head SHA`: tested SHA와 동일, `branch: codex/rmp-history-safe-archive`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`
- tested SHA는 `origin/main`의 조상(ancestor)이 아니다 — 아직 병합되지 않았다.

## WBS 원문

- 실행·검증(literal verifier): `cd fe && npm test`
- Done·QA 계약: 이전 결과는 수정 후에도 불변이고 report/export에서 raw prompt·내부 추론·action signal·금지 카피가 0건이다.

## literal verifier 실행 결과 — 로컬(Windows, 격리 worktree)

```
$ cd fe && npm test
> node --experimental-strip-types --test scripts/*.test.mts && npm run typecheck && npm run build && node --experimental-strip-types --test scripts/report-archive-bundle-check.mts

tests 67 / pass 60 / fail 7 / cancelled 0 / skipped 0 / todo 0
exit code: 1
```

`&&`로 체인된 명령이라 첫 단계(`scripts/*.test.mts`)가 실패하면서 이 실행 안에서는 `typecheck`/`build`/`bundle-check` 3단계가 실행되지 않았다(short-circuit). 이 사실을 숨기지 않고 별도로 기록한다.

**7개 실패 원인 — 전부 동일한 Windows 전용 harness 결함**: `new URL(상대경로, import.meta.url).pathname` 사용이 Windows에서 `/C:/...` 형태의 잘못된 경로를 만들어내는 동일 원인에서 비롯된다. 이 결함으로 실패한 테스트는:
- PR #7이 수정한 `fe/scripts/report-archive-safety.test.mts`의 5개 테스트
- PR #7과 무관한 기존 파일 2개(각 1개 테스트) — 이 PR이 만든 문제가 아님을 보여주는 근거

이를 실제 기능 회귀로 표현하지 않는다.

### 개별 후속 확인(같은 worktree, 별도 실행)

| 단계 | 결과 |
|---|---|
| `npm run typecheck` | exit 0 |
| `npm run build` | exit 0 |
| `node --test scripts/report-archive-bundle-check.mts` | 1 pass / 0 fail |

## canonical CI 확인 — 동일 tested SHA

PR #7의 tested SHA(`c0c0f28...`)에 대해 GitHub Actions에서 실제 실행된 CI를 직접 확인했다(추정이 아니라 워크플로 파일 원문 및 CI 로그 원문 대조):

| 항목 | 값 |
|---|---|
| workflow | `Code checks` |
| check | `Frontend checks` |
| runner | `ubuntu-latest`(Linux) |
| conclusion | `SUCCESS` |

`.github/workflows/code-check.yml`의 `Frontend checks` job은 `fe` 디렉터리에서 `npm run test`를 실행하며, 이는 WBS literal verifier `cd fe && npm test`와 동일한 npm script(`scripts/*.test.mts` → `typecheck` → `build` → `report-archive-bundle-check`)를 그대로 실행한다.

CI 로그 원문 기준:
- `scripts/*.test.mts`: **67/67 PASS**(PR #7이 추가한 신규 archive-safety 테스트 포함, 로컬 Windows에서 실패했던 7개 테스트 전부 이름까지 동일하게 PASS)
- `typecheck`: PASS
- `build`: PASS
- `report-archive-bundle-check`: **1/1 PASS**

동일 tested SHA에서 로컬 Windows 실패와 Linux CI 결과가 정확히 대비된다. 따라서 로컬 실패는 **Windows 전용 test harness observation**으로 기록하며, 실제 code defect로 판정하지 않는다.

## Done 계약 직접 검토

소스 코드 직접 판독(테스트 실행 결과가 아닌 코드 자체)을 1차 근거로 사용했다. 대상: `fe/src/features/reports/ReportDetail.tsx`, `fe/src/api/reportActionsClient.ts`, 관련 archive 코드, `fe/scripts/report-archive-safety.test.mts`.

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | immutable result snapshot/history 유지 | **PASS** | 리포트 상세 컴포넌트는 불변 식별자·lifecycle 사실·백엔드 allow-list 검증 섹션만 렌더하도록 명시적으로 설계돼 있으며, 레거시 점수·action signal·candidate 랭킹·생성 서술을 렌더하지 않는다. 액션 클라이언트에는 재발송/수정 API 호출이 없고 CSV 다운로드·인쇄·공유링크 복사만 제공한다. `report-archive-bundle-check`(운영 번들 대상)도 "no report resend write path"로 교차 확인. |
| 2 | raw prompt 노출 없음 | **PASS** | 렌더 로직이 allow-list 구조 — 허용된 두 section id, 그 안에서도 사전 등록된 라벨 집합만 렌더한다. raw prompt류 필드는 컴포넌트 어디에서도 참조되지 않아 구조적으로 렌더 경로에 도달할 수 없다. |
| 3 | 내부 reasoning/thought 노출 없음 | **PASS** | 2번과 동일한 allow-list 구조 — 내부 추론류 필드도 참조되지 않는다. |
| 4 | action signal 노출 없음 | **PASS** | 렌더 직전 unsafe-text 필터가 영어 action 어휘(buy/sell/hold/drop/recommend류)를 정규식으로 걸러 대체 표시하며, action signal류 필드 자체가 allow-list 밖이라 애초에 참조되지 않는다. |
| 5 | archive/report/export에서 매수·매도·추천·관망 등 금지 카피 노출 없음 | **PASS** | 동일 unsafe-text 필터가 한국어 금지 어휘(매수/매도/추천/관망)를 렌더되는 모든 항목에 적용한다. export(CSV) 경로는 결과 ID·보관일·생성시각·상태 4개 필드만 투영하는 allow-list라 금지 카피가 구조적으로 도달 불가하다. |

`report-archive-safety.test.mts`가 이 5개 시나리오(raw prompt·내부 추론·action signal·한국어/영어 금지어 리터럴 포함)를 코드로 정확히 다루고 있음도 확인했으며, canonical CI(Linux)에서는 이 테스트들이 실제로 통과함을 위에서 별도 확인했다.

## PR #7의 실질 성격

PR #7은 신규 기능 구현이 아니다. 확인된 사실:
- PR #7의 diff에는 관련 구현 코드 변경이 없으며, 확인 범위의 구현 파일은 canonical `origin/main`과 동일하다.
- PR #7의 유일한 변경은 이미 canonical repo에 존재하던 `fe/scripts/report-archive-safety.test.mts`에 CSV export 경로까지 포함하는 regression-lock 테스트 1개를 추가하는 것이다.

즉 **직접 검토 및 동일 SHA CI 기준 Done 계약을 충족하는 기존 구현 위에 자동 검증 커버리지를 넓히는 regression-lock 성격의 PR**이다.

## Observations (판정을 뒤집지 않음)

1. PR #7은 아직 `OPEN`이며, 신규 regression-lock 테스트는 아직 canonical `origin/main`에 병합되지 않았다.
2. PR #7 브랜치는 오래된 migration root 커밋을 기준으로 생성된 이후 갱신되지 않아, 현재 `origin/main` 전체 기준으로는 상당히 drift돼 있다(다만 이 WBS가 의존하는 구현 파일 자체는 drift 범위 밖이며, GitHub 기준 텍스트 충돌 없음으로 확인됨).
3. Windows 환경에서는 path handling 관련 test harness 결함으로 literal verifier가 로컬에서 exit 1을 반환한다.
4. 동일 tested SHA의 canonical Linux CI에서는 literal verifier와 동등한 전체 frontend gate가 PASS한다.

PR branch drift를 실제 Done 계약 실패나 blocker로 과장하지 않는다.

## Blocker

없음.

## Reviewer

- 이 문서는 independent QA reviewer evidence다.
- owner ≠ approver, self-QA 아님.

## 최종 판정

**`APPROVE WITH OBSERVATIONS`**

판정 핵심 근거:
1. 동일 tested SHA의 canonical CI(Linux)에서 literal verifier와 동등한 전체 frontend gate(scripts tests + typecheck + build + bundle-check)가 PASS했다.
2. PR #7이 추가한 신규 regression-lock 테스트가 해당 CI 실행 대상에 포함되어 통과함을 확인했다.
3. WBS Done 계약 5개 항목을 소스 코드 직접 검토로 전부 PASS 확인했다.
4. 로컬 Windows에서의 literal verifier exit 1은 실제 기능 결함이 아니라 이 환경에 한정된 test harness observation으로 판단한다 — 동일 테스트가 Linux CI에서는 이름까지 동일하게 전부 통과한다.
