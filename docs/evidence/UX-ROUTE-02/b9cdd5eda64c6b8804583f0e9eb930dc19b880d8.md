# `UX-ROUTE-02` independent QA evidence

- `wbs_id`: `UX-ROUTE-02`
- `role`: independent QA reviewer — WBS-designated approver이며 owner와 다른 사람. self-QA 아님.
- `tested_baseline_sha`: `b9cdd5eda64c6b8804583f0e9eb930dc19b880d8` (`origin/main`)
- `historical_cited_sha`: `d531593d5ca11fe886add6b032fbf2be79364693`
- `execution_method`: 현재 evidence branch·working tree를 건드리지 않고, tested baseline SHA를 대상으로 한 격리된 detached worktree에서 검증을 수행한 뒤 worktree를 제거했다. 이후 evidence branch/HEAD/working tree가 변경되지 않았음을 재확인했다.

## SHA 기준점 설명

historical cited SHA(`d531593...`)는 `origin/main`과 공통 조상이 없는 orphan lineage이며(`git merge-base` 결과 없음), 이번 QA의 기준점으로 사용하지 않았다. 동일 Done 계약을 검증하는 구현·테스트가 현재 canonical `origin/main`에 독립적으로 존재함을 확인했으므로, latest `origin/main`을 tested baseline으로 사용해 독립 재검증을 수행했다.

## WBS 원문

- 작업: `존재하지 않는 URL에서 내부 Figma 문구를 지운다.`
- Done·QA 계약: `actual 404, heading, home action, internal string 0개`
- 실행·검증(literal verifier): `cd fe && npm test`

## Done 계약 항목별 판정

| # | 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | actual HTTP 404 | **PASS** | `backend/app/api/routes/pages.py`의 `frontend_spa_fallback` 핸들러가 allow-list(`FRONTEND_EXACT_ROUTES`)에 없는 경로에 대해 명시적으로 `status_code=404`를 반환하도록 구현돼 있음을 직접 확인. 대응 테스트 `test_backend_hosted_pages.py::test_unknown_frontend_routes_keep_the_not_found_shell_but_return_http_404`가 `response.status_code == 404`를 직접 단언하며 PASS. |
| 2 | heading | **PASS** | `fe/src/App.tsx`의 not-found 분기에서 `<h1>페이지를 찾을 수 없습니다</h1>`가 렌더됨을 소스 직접 검토로 확인. |
| 3 | home action | **PASS** | 같은 분기에서 `<a href={ROUTES.home}>홈으로 가기</a>`가 렌더됨을 소스 직접 검토로 확인. |
| 4 | internal string 0개 | **PASS** | `fe/scripts/api-source.test.mts`의 "public navigation describes the core strategy workflow without fake performance or internal preview copy" 테스트가 `Figma HI-FI`/`EmailTemplatePreviewPage`/`emailTemplatePreview`/`dev/email-template` 문자열 부재를 단언하고, `fe/src/pages/EmailTemplatePreviewPage.tsx` 파일 자체가 더 이상 존재하지 않음을 확인하며 PASS. |

heading/home action은 전용 자동화 테스트를 찾지 못해 소스 직접 검토만으로 판정했다 — 이는 observation으로 별도 기록한다(Done 계약 미충족을 의미하지 않음).

## literal verifier 실행 결과 — 로컬(Windows, 격리 worktree)

```
$ cd fe && npm test
tests 68 / pass 62 / fail 6
exit code: 1
```

실패 6건은 UX-ROUTE-02와 무관한 기존 Windows-only `new URL(...).pathname` harness 문제이며, UX-ROUTE-02에 직접 관련된 테스트("canonical product surface excludes retired history and strategy routes", "public navigation describes the core strategy workflow without fake performance or internal preview copy")는 모두 PASS했다.

### 동일 tested baseline SHA — canonical Linux CI

```
Frontend checks: SUCCESS
tests 68 / pass 68 / fail 0
```

## 직접 대응 backend 테스트 (literal verifier와 별도)

```
$ cd backend && pytest -v tests/unit/test_backend_hosted_pages.py
6 passed
exit code: 0
```

## 회귀 검증

```
$ cd fe && npm run typecheck  → exit 0
$ cd fe && npm run build      → exit 0
$ node --test scripts/report-archive-bundle-check.mts → 1 passed, exit 0
```

## Observations

1. historical cited SHA는 orphan lineage / stale reference다.
2. heading/home action은 전용 자동화 테스트 없이 source 직접 검토로 PASS 판정했다.
3. local literal verifier의 exit 1은 UX-ROUTE-02와 무관한 기존 Windows-only harness 문제이며, same-SHA canonical Linux CI에서는 전체 PASS했다.

## QuantQA:v1 참고 상태

- S = PASS
- R = PARTIAL — same-SHA canonical CI execution은 확인했으나 별도 배포 환경에서 unknown-route runtime probe는 수행하지 않음.
- O = N/A
- C = N/A

`QA-WBS-01`은 DRAFT / Approval PENDING이므로 S/R/O/C는 참고용이며 최종 판정의 필수조건으로 사용하지 않았다.

## Blocker

없음.

## Reviewer

- 이 문서는 independent QA reviewer evidence다.
- owner ≠ approver, self-QA 아님.

## 최종 판정

**`APPROVE WITH OBSERVATIONS`**

판정 핵심:
1. Done·QA 계약 4개 항목 전부 canonical tested baseline(`origin/main`)에서 코드 직접 검토와 대응 테스트로 PASS 확인했다.
2. literal verifier의 로컬 실패는 UX-ROUTE-02와 무관한 기존 Windows-only harness 문제이며, 동일 SHA canonical Linux CI에서는 전체 PASS로 재확인했다.
3. 직접 대응 backend 테스트와 회귀 검증(typecheck/build/bundle-check) 모두 통과했다.
4. historical cited SHA의 lineage 단절은 observation으로 분리했고, 동일 계약이 canonical main에서 독립 재검증 가능하므로 FAIL/BLOCKED 사유로 사용하지 않았다.

---

- **Final verdict: APPROVE WITH OBSERVATIONS**
- **Blocker: none**
- **self-QA: no**
