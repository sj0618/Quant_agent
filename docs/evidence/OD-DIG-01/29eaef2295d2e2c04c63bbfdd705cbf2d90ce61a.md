# OD-DIG-01 증적 번들

- `wbs_id`: `OD-DIG-01`
- `subject_implementation_sha`: `897fc83a80555bc42a415c50bfe4e13a291e4514`
- `tested_sha`: `29eaef2295d2e2c04c63bbfdd705cbf2d90ce61a`
- 실행 환경: [`docs/evidence/_RUNNER.md`](../_RUNNER.md)
- 수용 기준(WBS): 410 또는 route absence, LLM 호출 0회

## 1. 고정 command (로컬 계약)

```sh
cd ai && pytest -q
```

```
751 passed, 9 skipped, 1 warning in 27.41s
```

`exit=0`

### 이 행을 실제로 덮는 테스트

```sh
cd ai && python -m pytest -q \
  tests/test_api.py::test_retired_daily_digest_returns_gone_without_audit_or_llm \
  tests/test_daily_digest.py
```

```
8 passed, 1 warning in 0.41s
```

`exit=0`

`ai/tests/test_api.py:689`이 두 축을 함께 단언한다.

```python
def llm_path_must_not_run(*_args, **_kwargs) -> None:
    raise AssertionError("retired daily digest route invoked the LLM path")

monkeypatch.setattr(
    "ai_graph.nodes.daily_digest.generate_daily_digest_overall_comment",
    llm_path_must_not_run,
)
...
assert response.status_code == 410
assert response.json()["detail"] == {
    "code": "daily_digest_retired",
    "message": "정기 다이제스트 생성은 현재 제공하지 않습니다.",
}
assert sink.sessions == ()
```

- `410` 축: status code와 body를 정확히 일치 비교한다.
- `LLM 호출 0회` 축: LLM 진입점을 예외 발생 함수로 치환했으므로, 호출이 있었다면
  테스트가 실패한다. audit sink도 `sessions == ()`로 0건을 단언한다.

## 2. 배포 서버 실측 (2026-08-24 KST)

```sh
curl -sS -X POST -H 'Content-Type: application/json' -d '{}' \
  https://qt-agent.kro.kr/ai-api/ai-api/ai/daily-digest
```

```
http_status=410
{"detail":{"code":"daily_digest_retired","message":"정기 다이제스트 생성은 현재 제공하지 않습니다."}}
```

같은 시각 `GET /ai-api/ai-api/api-status`의 endpoint 목록도 이 경로를
`{"method":"POST","path":"/ai/daily-digest","state":"retired",
"summary":"Retired daily digest endpoint; no audit, LLM, or subscription work is available."}`
로 보고한다.

## 커버한 축 / 미커버 축

| 수용 기준 축 | 상태 | 근거 |
|---|---|---|
| 410 (로컬) | 커버 | status + body 정확 일치 |
| 410 (운영) | 커버 | 배포 서버 실측 응답 |
| LLM 호출 0회 (로컬 계약) | 커버 | LLM 진입점 치환 + audit sink 0건 |
| **LLM 호출 0회 (운영 관측)** | **미커버** | 서버 로그·프로바이더 사용량을 읽지 않았다. 로컬 단언은 계약 수준이고, 운영에서 호출이 0이었다는 관측은 아니다. |
| 메일 예제 잔존(WBS 위험 항목) | **범위 밖** | `/dev/email-template` 제거는 OD-DIG-02 행이다. |

## 승인

reviewer 미지정. 이 번들을 만든 주체가 승인하지 않는다.
