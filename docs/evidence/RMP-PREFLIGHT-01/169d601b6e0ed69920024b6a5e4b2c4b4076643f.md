# RMP-PREFLIGHT-01 evidence bundle

- `wbs_id`: `RMP-PREFLIGHT-01`
- `subject_implementation_sha`: `c8e73551700c7dd0cb55f11bc3c08d2b3393049e`
- `tested_sha`: `169d601b6e0ed69920024b6a5e4b2c4b4076643f`
- `tested_tree`: `992d1da2d4b83f24163798f40102dc114d375999`
- `bundle_revision`: the Git revision that contains this file; its immutable URI is
  recorded in the WBS evidence cell.
- `output_uri`: this file's Git blob at that same immutable bundle revision.

## Acceptance evidence

Command, run with database and provider secret variables removed:

```sh
env -u AI_DATABASE_DSN -u QUANT_DB_DSN -u DATABASE_URL \
  -u AI_AOAI_API_KEY -u AI_AOAI_ENDPOINT -u AI_AOAI_DEPLOYMENT \
  -u AI_LLM_API_KEY -u OPENAI_API_KEY \
  ai/.venv/bin/python -m pytest -q \
  ai/tests/test_research_request_preflight.py \
  ai/tests/test_research_contract_api.py \
  ai/tests/test_token_auth.py::test_preflight_rejection_is_returned_before_token_quota_or_job_creation
```

Result: exit `0`; the focused independent QA run passed `21` tests.  The consolidated
post-fix local run passed `152` tests with `3` intentionally skipped server-integration
tests.  The transport regression verifies one read-only identity lookup at most and zero
quota mutation, job-store, signer, audit-runner, analysis-runner, LLM, and data calls for
a rejected request.

## Review record

- Reviewer kind: `independent_codex_owner_delegated` (non-human)
- Reviewer: Codex independent QA; product changes authored by reviewer: `false`
- Owner delegation: the product owner explicitly delegated QA closure while the assigned
  QA reviewer was unavailable.
- Assigned WBS reviewer: `조은채` (preserved in the WBS; not impersonated)
- Verdict: PASS for the stated local boundary contract
- Reviewed at: `2026-08-24 00:32:34 KST`

This is local boundary evidence, not an assertion of production data or deployment health.
