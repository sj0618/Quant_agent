# Codex 실행 기준

## 실제 실행은 서버 기준

- 실제 스크리닝, 백테스트, 추천, 현재 시세·지표 조회는 반드시 서버 실행 환경과 서버 DB/API를 사용한다.
- 로컬 fixture, mock, 테스트 출력물, `prompt_semantics_output`, 캐시 결과는 코드 점검·단위 테스트용으로만 사용한다. 실제 결과처럼 보고하지 않는다.
- 실행 전에 서버 데이터 소스가 연결되었는지 확인한다. 결과의 `pipeline_data_source.source`가 `postgres`이거나 서버 API가 명시적으로 실데이터 응답을 반환해야 한다. `source=fixture`, DSN 미설정, 로컬 mock이면 실제 실행을 중단하고 그 사실을 보고한다.
- 서버 연결이 확인되지 않으면 로컬 fixture로 대체하지 않는다. 서버 접근 불가를 명확히 알리고, 서버 기준 결과를 낼 수 없다고 답한다.
- 로컬에서 허용되는 작업은 읽기 전용 코드 분석과 명시적으로 로컬용이라고 표시된 테스트뿐이다. 서버 기준 결과와 로컬 테스트 결과를 섞지 않는다.

## 검증 원칙

- 서버 실행 결과에는 데이터 소스, 기준일, 조회 행 수 또는 후보 수를 확인할 수 있는 메타데이터를 함께 확인한다.
- 스크리닝 후보가 0개이면 필터 탈락인지, 서버 데이터 미연결인지, 기준일·지표 데이터 부족인지 원인을 구분해서 보고한다.
- 비밀값은 출력·커밋·응답에 노출하지 않는다.
